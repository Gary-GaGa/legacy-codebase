# 舊→新 頁面對應 & 補完清單（控制中心）

> 重構專案（前後端兩個獨立專案）已完成 **~70%、多頁已實作**；目標 = 補完剩餘 **30%**。
> 重構小組會把**多個舊畫面合併成一個新頁** → **工作單位 = 新系統頁**（一新頁可能對多舊頁）。
> ⚠️ 原始碼**不外流、不上 GitHub**：本 repo 只放**對應/規格/backlog，不放原始碼**；實作在前/後端專案內進行（Copilot 或本機 Claude Code），以本檔規格 + 既有 70% 模式為準。

## 0. 開發工作模式
- **規格/backlog**：本 repo 產出。
- **實作**：前/後端兩專案內，依「規格 + 既有 code 模式」完成。
- 每完成一頁 → 更新本檔狀態。

## 1. 對應清單（使用者提供，待貼）
> 來源：使用者「舊頁→新頁」清單（含完成狀態 + 合併關係）。

| 新系統頁 | 合併的舊頁（EPRO funcId） | 模組 | 狀態（完成/未/半） | 後端 endpoint | 對應 TB_ 表 | 備註 |
|---|---|---|---|---|---|---|
| _(待貼)_ | | | | | | |

## 2. 剩餘 30% 補完 backlog（依清單彙整後產出）
> 只列「未完成 / 半成品」的新頁，排優先序 + 相依 + 估點。

## 3. 我需要的輸入（unblock 開發）
- [ ] **對應清單**：舊頁 → 新頁 + 完成狀態 + 合併關係（最關鍵）
- [ ] 未完成新頁的**版面**（合併後一頁含哪些區塊/頁籤）
- [ ] 各未完成頁對應的 **TB_ 表**（可 JIT 抽 schema 產 entity）
- [ ] 開發載具：在前/後端專案用 **Copilot**，還是有**本機 Claude Code**？
