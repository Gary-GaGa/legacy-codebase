# PRD→SRS pipeline 硬化 saga 決策歸檔（2026 H1）— frozen

> **凍結快照（2026-06-25 壓縮自 [`../decisions.md`](../decisions.md) §一）**。本檔＝2026-06-16~24 PRD→SRS pipeline 硬化 saga **已結案**決策的 verbatim 搬遷，目的＝瘦身主流水帳之 context 量；**append-only 史實、不改寫**（同 `completion-ledger`/`archive/` 凍結慣例）。
> 範圍＝批判輪2/3（gateⓈ/Ⓔ/Ⓡ 加裝）、owner 裁定輪（RP9/AUD-6）、GitHub Copilot 第三軌移除、`docs/legacy/` 去留、00800 spec 層重置/封存、Bible v1.1 ingest、legacy-parity SOP、砍掉重來 vs 逐頁驗證裁定、local 對比輸入盤點/改名/重掃、文件矛盾掃查多輪、STATUS 納 PRD→SRS、EPROZ00100/EPROC00118 產出+pilot+全清重跑+重產+Blocker 修正+contract closeout+Approved、drain 模式、orchestrator 度量化等 saga 列。
> **現狀 SSOT 不在此**＝[`../feature-inventory.md`](../feature-inventory.md)（狀態）＋[`../build-tasks/refactor-audit/per-page-reinventory-matrix.md`](../build-tasks/refactor-audit/per-page-reinventory-matrix.md)（PRD→SRS 佇列/ledger 與覆蓋 SSOT）。新決策仍寫 `../decisions.md`（append-only 續流）。
> 列原序保留；列內 cross-ref（`supersede 上列`等）指同批搬遷列、語意自洽。

| 項目 | 內容 |
|---|---|
| SRS gate 升級＝破假綠＋安全-Approved 判準（2026-06-16，源 00800 蘇格拉底批判）| 對 worked example 00800 批判性重審得 6 類問題；**2 類為轉換/方法論固有**（批量 PRD→SRS 會系統性複製）：**gate⑤ 假綠**（多分支 Rn 單 case 當覆蓋，00800 R15 僅 rollback／R16 僅 transaction）、**帶未承載 Bible 安全缺口仍 Approved**（BR-014 災難情境降 BP1 seam）。修：`scripts/check-srs-bundle.py` ① gate⑤ 加分支覆蓋 partial-warn ② 新增 **gateⓈ**（Status 含 Approved + `BPn-PENDING`→warn）；`prd-to-srs` DoD＋`spec-architecture §9`（教訓 13/14/15）＋`specs/srs/README.md` gate 對照同步。**重跑 00800 證實**：從「靜默全綠」→「PASS＋顯式 2 warn」（R15/R16 partial、BP1-4 Approved）。其餘 4 類（假 Approved 標籤／RP4 一證關兩事／RP1 工程論點關業務風險／as-is 不回填實作）＝**迭代 progress bias**（教訓 15），初稿不會有、Phase 2 人裁立紀律（裁定權留 owner）。**批量 PRD→SRS 前置＝此 2 固有缺陷已 gate 化**。 |
| owner 裁定輪（2026-06-16）| **RP9（init-query method）＝Follow PRD §6.1 ＝ GET**（RD/架構）：method recon 證據全站 280/282 POST,但依 PRD 走 RESTful GET（少數例外、同 00600 樣板）→ 00800 R2/Endpoints/openapi 定版 GET、get-body sweep #3 解鎖（reviseditem 改 GET query）。**AUD-6（財評精度縮減）＝接受**（DBA/domain）：`TB_FINANCIAL_EVALUATION_INFO` 6 金額欄 `(28,2)→(20,2)`、利率 `6→2` 位以新 DB 為準、不還原（entity 對 DB、無需改碼）;⚠️ caveat：利率僅 2 位,日後現需 >2 位再另開。兩裁定 pending 移已關區、spec/handoff 同步。 |
| PRD→SRS 轉換層硬化＝批判輪2（2026-06-16, 源 00800 重審）| 00800 砍掉重建在即（Bible/PRD 待更新）→ **病治在轉換層、非 artifact**（owner 洞察）。蒸餾 4 轉換固有病：①Bible 安全/災難條件未承載卻 Approved subset（BP1）②契約收『後端為準』決策欄（`checkPointMap`/`isNotSame` vs R11）③mutating 端點 FE-only 強制（R3/R5/6/7 on execute）④Status 混『規格定版 vs 實作完成』。**關鍵洞：①③原則早在 skill/DoD 卻無逐條 check＝虛設**。硬化：機械層 `check-srs-bundle` gateⓈ 加 **(b) Status 雙軸 warn**；判斷層 `prd-to-srs` DoD 加 4 條逐條 check、`spec-reviewer` 加「批判輪2 紅旗」、`spec-architecture §9` 教訓 16-19。**不動 00800 本體（待重建），僅 Status line 改雙軸示範**。一次性發現（RP9 GET 不一致、QA-014 enum）跳過。重跑 00800：gateⓈ(b) warn→PASS（已拆兩軸）、(a) 仍 warn（BP1 安全 seam 真開、正確）。 |
| GitHub Copilot 第三軌移除（2026-06-16, owner）| owner：「github 已經不能使用了，相關檔案可以移除」→ **刪 `.github/`**（`copilot-instructions.md`＋`instructions/{backend,frontend}.instructions.md`，3 檔）、**回歸 Claude↔Codex 雙軌**。de-reference：CLAUDE.md §2 註、AGENTS.md preamble/部署、repo-structure §0/§2（三→兩個載具、刪 Copilot 欄）/§3、README §治理、SETUP-codex、本檔 preamble。**supersede** 早先「.github/copilot-instructions.md 已起草」「三個載具」相關敘述。保留：`page-mapping` 歷史 cross-check 教訓敘事（指 Copilot 關鍵字搜尋系統性低估、與軌道檔無關）、`archive/` 凍結快照（不改）。parity gate（Claude↔Codex）不受影響（未 anchor Copilot）。 |
| PRD→SRS 轉換層硬化＝批判輪3（2026-06-16, owner「硬化閘門」）| 機械閘門綠（`check-srs-bundle.py` exit 0）後依 CLAUDE.md §4「先機械、再語意」跑 `spec-reviewer` 審 00800 worked example → 抓 **2 個未登記 Blocker**（已坐實）：**SR-B1** PRD §6.4 `MSG_OVER_COUNT_LIMIT`(400)/`MSG_QUERY_FAIL`(500) 未承載進 R15+openapi、REQ-007 未完整；**SR-B2** R15 把「查詢失敗 500」誤併入「輸入錯誤 400」。**機械盲區坐實**：既有 xfile#3 只查 spec→openapi（spec 有、openapi 漏），結構上抓不到「PRD 有、spec/openapi **皆**漏」。**owner 裁「硬化閘門」**→ 加 **gateⒺ 錯誤碼承載**：PRD Error Response 表（上游權威）→ spec.md Rn 錯誤規則 + openapi responses，漏承載=warn、PRD↔openapi 同碼 HTTP status 不一致=warn（皆 warn，disclaim 由人/reviewer 認）。**關鍵實作＝解析前去 markdown 底線跳脫 `\_`**（PRD 表格寫 `MSG\_X`，literal `_` 比對會漏抓＝SR-B1 根因；我自己 spot-check 時也踩過此坑、當場由「疑 reviewer confabulate」修正為「reviewer 對、我的 grep 漏跳脫」）。重跑 00800：gateⒺ 顯式 2 warn（漏 2/7 碼）、exit 0（warn 級不破綠）＝**機械重現 SR-B1**。檔頭 canonical 清單 + `specs/srs/README.md` gate 對照同步（Ⓔ=Error碼承載）。**判斷層**（`prd-to-srs` DoD / `spec-reviewer` 紅旗 / `spec-architecture §9`）propagation＝待 owner 核 follow-up（同批判輪2 全層模式，本輪 owner 僅授權機械閘門）。findings＝`build-tasks/done/00800-spec-review-findings.md`；SR-B1/B2 登 pending-register。 |
| 批判輪3 判斷層 propagation 落地（2026-06-16, owner 續授權）| 承上批判輪3（gateⒺ 機械層）：owner 續授權後補**判斷層**（同批判輪2 全層模式）→ `prd-to-srs` DoD 加「PRD 錯誤碼逐碼承載」check、`spec-reviewer` 紅旗②③④ 升為 ①–⑤（加 ⑤ 漏承載=🔴／status conflation=🔴🟡）、`spec-architecture §9` 加教訓 20。**機械(gateⒺ)+判斷(DoD/reviewer/教訓)三層齊**——SR-B1/B2 類（PRD→SRS 漏承載/混 status 錯誤碼）守在「機械 warn + DoD 逐條 + reviewer 紅旗」三層。三層皆內建「PRD 表格去 `\_` 跳脫」提醒（SR-B1 根因）。supersede 上列「判斷層待 follow-up」。 |
| `docs/legacy/` 去留評估（2026-06-17）| owner 問「有 DB + 功能頁清單後 legacy/ 可否移除」。盤點＝**不移除**：7 檔全 load-bearing（被引 5–25×）、屬**分析層非原始資料**。逐檔：**`legacy-function-inventory.md`＝owner 06-16 權威盤點＝upstream SSOT**（即 owner「功能頁清單」本身；後 4 欄留白待回填驗證進度，必留）；**`db-schema-catalog.md`** 已 06-12 降「輔助索引」（schema 權威已換軌＝`build-tasks/schema-diff-findings.md` DB 實查——「有 DB→以 DB 為準」此交接早做）；**`page-mapping.md`**(被引 25·控制中心·合併規律/補完清單/RPC API 慣例)、**`migration-backlog.md`**(R1–R8 track)、**`module-{is-iu,cs-cu}-shell.md`/`module-i0-c0-scoring.md`**(舊模組內部結構)＝DB+頁清單取代不了，T24/撥貸端到端驗證 + M4–M8 重 grep（drift re-check §4）仍引用。**archive 觸發條件**：遷移+驗證全完（T24 UAT 過、M4–M8 重 grep、AUD 全關）後 → 移 `docs/archive/`（**凍結·非刪除**，同 `completion-ledger` 慣例）。現驗證進行中（~80%）→ 維持現狀、不刪。 |
| 00800 C 組裁定（2026-06-17, owner）| owner 對 pending C 組（00800 SRS）裁：**SR-B1/B2（spec-review 2 Blocker：PRD §6.4 `MSG_OVER_COUNT_LIMIT`(400)/`MSG_QUERY_FAIL`(500) 未承載進 R15+openapi、且 R15 把查詢失敗 500 誤併入輸入錯誤 400）＝(b) 折進 00800 重建 backlog**（不立即小修；**gateⒺ 每跑必 warn 這 2 碼＝安全網**，重建不可能漏承載）；**RP8（R6/R7 as-is 判據等價性：`secureAttribute==='U'` vs `isCU`、auth list vs `isEdit`）＋ RP11（execute DTO 形狀 `itemMap.item1..14` vs 平鋪、含 S-3 checkPointMap 權威）＝備 RD 派工卡**（需產品碼、planning repo 解不了、RD-only；判定結果餵重建）。新卡＝`build-tasks/00800-rp8-rp11-rd-closeout.md`。回填 findings §處置、pending-register（SR-B1/B2 ✅折進重建、RP8/RP11 來源指卡）、spec §@PENDING、STATUS §五。 |
| Legacy-parity SOP 立（2026-06-17, owner）| owner 與團隊比對「已完成項」見落差（成因：①畫面/模組縮編②以舊為基礎+調整〔T24〕③新 DB 變動）→ 定調**重構期一律以舊系統為主（behavior parity-first）+ 差異按頁登記 + owner 判調整 + 確定樣板**。立 **standing SOP**＝`docs/process/legacy-parity-sop.md`。核心防呆＝**「舊系統≠絕對正確」每差異分三判**：(a) regression→照舊修回／(b) 刻意演進·縮編→keep 新+登錄／(c) DB 結構差→code 對齊舊行為·**schema 以新為準（不回滾 DB**；AUD-6/schema-diff「以 DB 為準」）。配套：risk-tier 比對（金錢/checkpoint/T24/授權先、低風險頁 UAT 觸發）、intended 差異登錄防 churn、調整需求若屬業務邊界回灌 PRD/Bible。教訓來源＝**A-5/KHR**（舊通吃·新刻意收窄，無腦照舊＝反向 regression）、**0921**（結構在≠行為對）。|
| c0 老系統 parity 補比 reopen（2026-06-17, owner）| 回頭確認已完成項該不該重開：**c0 評分線 00114–00120 當初只對 i0 鏡像 audit、未對舊企金 c0 行為比**（0921 列「撥貸收斂後評估補比」）→ **撥貸本 session 收斂、補比到期** → **reopen pending（🟡）+ 派工卡 `c0-legacy-parity-recheck.md`**（risk-tier 00118/00120 先）。**E1/E2 係此缺口先聲**、與 T1 00118 合流。其餘已完成項 reopen 判定：**撥貸端到端/T24 UAT、Phase F-G FE re-grep（M4/M5/M7/M8）＝已知 pending**（非隱藏、已追蹤）；**AUD-10 B005 inline-取代＝inference，可 spot-check**；**AUD-6 利率2位/A-5 KHR 收窄＝keep-new caveat**（方法論 (b)/(c) 下正確、不重開）；**00800 RP1–RP10＝重建重訪**（不另開）。|
| 砍掉重來 vs 逐頁驗證＝選 A 證據驅動（2026-06-17, owner）| owner 考慮「砍全部前後端、以回報 70% 重新盤點、用成熟 spec workflow 重建」。分析：**規格覆蓋僅 1/67**〔→ 同日稍晚 0800 封存後＝**0/67**；現行權威＝`per-page-reinventory-matrix.md`，本列為封存前快照、論點不變〕（00800，且自身待重建）、**70% 是 gap 驅動 migration**（含大量已決架構/水管：Angular shell/deputy/cub-lib、Spring EPROResponse/JWT/JPA/c0 自足鏡像）、**硬題（E1/E2/AUD/BP/T24/A-4/M6）重寫照撞**、**已修 bug（langType/get-body/T24/00600）會回吐**、新 DB/頁合併已在 70% 裡（砍＝繞圈）→ **wholesale 砍掉重來不被證據支持**。**裁＝A 逐頁證據驅動**：建 per-page 盤點矩陣（`build-tasks/refactor-audit/per-page-reinventory-matrix.md`），每頁套 `legacy-parity-sop` 三判 → disposition keep/fix/rebuild，**rebuild 是逐頁結果、非前置賭注**。矩陣 seed 結論：**確定 rebuild 僅 00800**；最大 parity 風險＝**企金線（CSU 主流程 9 + c0 評分 9 ＝18 頁，多由鏡像個金 ISU/i0 twin 建、未對舊企金 cs/cu 驗）**；其餘多 FIX/KEEP（撥貸在修、z0 多已修、i0/deputy 較穩）。配套：owner 有**新版 Bible/PRD** → 按 risk-tier 跑 `prd-to-srs`（企金線/撥貸/00800 重產先；**需先把新版放進 `docs/specs/`**）。**c0-parity reopen 擴及 CSU 主流程**（企金線整體對舊驗）。|
| Bible v1.1-draft ingest（2026-06-17, owner 提供）| owner 提供新版 Bible（88/100）取代 v1.0 → 寫入 `specs/bible/bible-eproposal.md`（v1.0 見 git history）。**大幅補強**：UT DB（`OCHUBKH`/`OVSLXLON02`）實查錨點〔`TB_LON_SUMMARY_INFO` LON_ATTRIBUTE(I/C)/LON_TYPE_CODE(01-05)/SECURE_ATTRIBUTE(S/U)/CASE_PROGRESS、`TB_PROCESS_CODE` 狀態字典、`TB_ROLE_DEFINE` 角色、`TB_API_AUTH`〕、E2E 8 情境+災難情境、BR-001~030、SC-001~031、SIT/UAT 矩陣骨架。**對既有工作意義**：①**BP-1~5 seam 之 Bible 側已承載**（BR-014~017 案件類型 gating/影響 0150/顯示 0173、SC-002~005、災難情境「EPROZ00800 不該顯示卻顯示」）→ 待 PRD/SRS 下推（00800 重產直接用）②**型三軸坐實**（客戶 `LON_ATTRIBUTE` I/C→ISU/CSU、案件 `LON_TYPE_CODE` 01-05、擔保 `SECURE_ATTRIBUTE` S/U）＝BP-2 解③**撥貸/契據確認個金 only**（Q-002 企金待確認）＝對齊我方撥貸個金 `EPROISU0920-0922` 範圍④**`EPROZ00670`=TLOD report API**（Q-001）＝對齊 AUD-5⑤狀態碼字典 27 碼＝撥貸/workflow SRS 金礦。→ PRD→SRS 的 Bible 上游就緒（risk-tier 見矩陣 PRD→SRS backlog；owner local 用 Codex 跑）。|
| 0800 移除＝spec 層重置（2026-06-17, owner）| owner「把 0800 移除」→ **封存**（非硬刪）stale v0.9 SRS bundle（`specs/srs/EPROZ00800/`）+ 舊 PRD `CDC-EPRO-0001 v1.1` + trace → `archive/EPROZ00800-v0.9-superseded/`（凍結·git 可回復）。**為何**：spec 層重置——新 Bible v1.1 + 新版 PRD（owner local）+ 既有重構 spec（local `docs/refactor/`）+ 新舊 DB（local `docs/db-schema/`）→ 00800 從新上游**重產**，舊 bundle 退場避免混淆。**EPROZ00800 這頁仍是真頁**（Bible BR-014 展期/展變）→ 留 inventory/matrix（disposition=REBUILD）；封存內 RP1-10/SR-B1/B2/RP8/RP11 為重產輸入。de-reference：skill 範本/dispatch §5/specs READMEs/pending-register/feature-inventory/matrix/STATUS 改指封存或「待重產」；歷史 done findings 不動。**連帶：PRD→SRS 對比輸入改 local 資料夾**——`docs/db-schema/`（新舊 DB 差異+新 table schema）、`docs/refactor/`（70% baseline spec；「更動需求」＝新 PRD⟷此 baseline delta）取代原 repo schema-diff/feature-inventory 之 DB/refactor-spec 角色（repo decisions/pending/matrix/parity-sop 仍為約束）；兩資料夾 **local-only**、結構待 owner 回報後補抽取細則（dispatch §5 已標）。|
| 文件矛盾掃查＋三輪複驗收口（2026-06-17, owner 指示）| owner「開多 sub agent 找矛盾→一次修掉→再開 agent 確認」。**第一輪 5 agent** 掃出 A–E ~20 項不一致（多源於 0800 封存、Bible v1.1 ingest、A-1 stub 已實作、AUD-10 結案後的 **stale 敘述**未同步）→ PR #101 一次修齊。**第二輪 3 agent 複驗**：4 主修全 ✅、**0 新斷鏈/0 新矛盾**，但揪出 5 處殘留→本批補：①`build-tasks/local-phase-v-bringup:42` authorize「仍 throw-stub」→改「A-1 stub 已實作（`daae4c3`）、剩 T24 外部整合」②`feature-inventory:59` 0922 cell「A-1 throw-stub」→「A-1 ✅ 已實作；殘＝T24 端到端·B-1 接值」③`verification-handoff §2.1/§2.3` 就地加 06-17 更正 banner（KHR=收窄·精度 C-1 關·`T24_COMPANY` 非死路·匯率源 `OVSLXLON01`）+ §2.2「🔴 其他真 bug: EXCHANGE_RATE 換源」就地刪線（已裁 parity、非 bug）④`README:72` 既有斷鏈 `../STATUS.md`→`STATUS.md`（早本批一天、非本批引入）⑤`drift-recheck §3` AUD-10 自相矛盾 checkbox→標已回填（feature-inventory §1:15/§4⑩ 三處現一致）。**性質**：殘留全屬「結論已定、敘述未同步」型 doc-rot（非決策衝突）；方法＝掃→修→複驗→補殘的收斂回路。|
| local 對比輸入結構盤點＋PRD→SRS §5 定稿（2026-06-17, owner 回報結構）| owner 在本機跑結構盤點 prompt、回報兩 local 資料夾結構 → dispatch §5「抽取細則」由佔位改為**實據定稿**（修正先前假設）。**①`docs/db-schema/`（187 檔）＝新 schema snapshot**（`02_tables/TB_*.md` 156 表、index＝`table_name`、markdown 欄位表**非 DDL**），**非完整新舊 diff**——舊→新只散在 per-column `note`/`transpose_method` + per-table `usage_status`/`match_status`＝change-hint；無 funcId→table 反查（由 as-is 碼/`TB_CHECK_POINTS_*`/Bible 錨點推表）；無 `OVSLXLON01/02` 標註（與 runtime 來源裁定正交）→ schema.sql 以新 snapshot 為權威、hint 當註記、**真 parity 回 `legacy-parity-sop`**。**②`docs/refactor/`（881 檔）＝70% baseline 重構 spec**（`02_specs/` 834、index＝funcId/`epl-*`、版本鍵 doc_status/version/date 多版並存→**取 latest**；一頁多檔）；**含 API 欄位表「最大長度/必要」＝maxlength 量化來源（解 D5 split-brain）**、~285 `epl-*`、894 validation_rule（非正式 QA→QA 仍新撰）、**無正式 Rn/REQ**（Rn 由新 PRD 合成）；delta 靠 doc_status/`修訂紀錄`/`open_issue`/`待確認項目`。**③覆蓋交叉驗證確認風險模型**：refactor 缺 **`EPROC00119/00120`**（＝企金線 T1 parity 風險頁，無 baseline→走 i0-mirror+parity）；額外有 **`EPROZ0B001-0B007` 批次 spec**（撥貸批次層 AUD-10 B001-B008 可取 as-is）+`EPROCSU0140`/`EPROZ00420`/COMMON/FUNCTION。回填 dispatch §5A/§5B/做法、SKILL input#5/DoD。兩資料夾仍 **local-only**、Codex 母資料夾跑時讀。|
| 5-agent 全 repo 廣掃（一致性 + 失效檔/死內容，2026-06-17, owner 指示）| owner「開多 agent 複驗 repo + 找失效檔案/內容」→ 5 隻分域並行（核心 SSOT／治理雙軌／撥貸驗證／build-tasks 盤點／失效檔專掃）。**結論：repo 高度自洽、無真死檔/真孤兒/整檔過時**（封存·de-reference·audit-trail 凍結紀律到位）。**真修 3 項（本批）**：①`decisions.md:68`「1/67」加「封存後＝0/67」標記（與 matrix SSOT 對齊；append-only 不改原論點）②`ADR-0001:8` 唯一漏網 de-reference：`docs/specs/srs/EPROZ00800/`（已封存不存在）→改指 `archive/EPROZ00800-v0.9-superseded/`③封存 0800 bundle 內部 4 處相對連結搬家後斷→archive README 加「內部連結凍結於搬家前佈局」註（不動凍結快照內文）。**1 項需 owner 裁（另問）**：T24 B-group commit 狀態——`done/t24-bgroup-*-findings.md` 各欄 Commit=`pending` vs `triage:78`/`escalations:19`+B-2/B-4「已 commit/push 06-17 人審過」打架，真值取決 product repo git log。**判為不 churn（advisory/cosmetic/凍結，列供 override）**：`diff-vs-inventory:23` 166/168 已自註；`spec-architecture §9` #21/#22 排序（編號唯一·引用對·純觀感）；`page-mapping:4,124,126` "Copilot"＝產品側 IDE 工具非治理軌（非矛盾）；`manifest LT-1`「DB=92」帶「曾」已標歷史；`done/a1-spec:33` `FAILED_E303` 凍結且有「→見 OQ-3」指標。gates exit 0。|
| STATUS 進度盤納入 PRD→SRS 規格產出軌（2026-06-17, doc 整理）| 把 PRD→SRS（spec 層重產）從只埋在 `STATUS §六` 提升到**頂層進度盤**：`STATUS §一` 加「規格產出(PRD→SRS)」bar（~10%）＋一句話、`STATUS §二` 加「spec 層（PRD→SRS 重產）」列、**`feature-inventory §5` 補 `Phase S`**（排程 SSOT，補上 §二「排程見 §5」原本的斷點）。狀態＝**前置就緒→待重產**：轉換層硬化（批判輪2/3 gate：gateⓈ/Ⓔ＋DoD＋`spec-reviewer` 紅旗）✅・Bible v1.1 已入 repo ✅・local 對比輸入（`docs/db-schema/`＋`docs/refactor/`）盤點＋抽取細則定稿 ✅・dispatch 卡 `build-tasks/prd-to-srs-codex-dispatch.md` ✅；**SRS bundle 覆蓋 0/67**（唯一數字 SSOT＝`refactor-audit/per-page-reinventory-matrix.md`；`docs/specs/prd|srs/` 現各僅 `README.md`＝0 bundle 落地實況、Bible 已在）。**Phase S 阻於 owner 提供新 PRD**（Bible→PRD 由 PM 產、未入 repo）→ Claude 無法自跑 `prd-to-srs`；risk-tier 企金線/撥貸/00800 重產先。read-only sub-agent 複驗 PRD→SRS 五面（§一 bar／§二 phase／§5 Phase S／§六 detail／matrix SSOT）一致 5/5 PASS、無斷鏈。|
| EPROZ00100 SRS bundle 產出（2026-06-18, Phase S 首件）| owner 提供 `CDC-EPRO-0001 EPROZ00100` PRD（TO DO LIST/Dashboard）→ 跑 `prd-to-srs` 產**首個 SRS bundle**（`specs/srs/EPROZ00100/`：spec 14 Rn〔R2/R11/R12=@PENDING〕、openapi 6 `epl-*`、schema、qa 22 cases；PRD 快照+trace 入 `specs/prd/`）。**brownfield as-is 接地**：主端點＝實際 `epl-list-todolist`（合併 initQuery+queryCAD role 分支）、langType regression 已修（`bbbaa19`）、popup reason 已修（`2599752`）；**疑 regression 標記**＝R6 CAD proxy scope（as-is 只 `CURRENT_USER_ID`、未含 active proxy）。**8 PRD TBD 全轉 @PENDING、不自裁**（Bible v1.1 `TB_ROLE_DEFINE`/`TB_PROCESS_CODE` 僅列裁定參考、未代裁）。**兩層驗證**：機械 `check-srs-bundle.py` exit 0（全 PASS）；語意 spec-reviewer **round-1 抓 3 Blocker（B1 `REDISTRIBUTION_FAILED` 漏承載／B2 `REQ-003-03` 無 Rn／B3 代理人 history 無 QA）+ 6 Should-fix → 全修 → round-2 確認無 Blocker、無新斷鏈**。Status=In Review（待 8 TBD，blocking TBD-002/004/006）→ 未升 Approved。**覆蓋 0/67→1/67**。回填 matrix（覆蓋 SSOT）/STATUS/feature-inventory §2E:116·§5 Phase S/pending-register。下一頁續跑 risk-tier（企金線/撥貸/00800）。|
| PRD→SRS 流程審查與硬化（2026-06-18, owner 指示「規模化前審 prompt 找遺漏/矛盾/錯誤」）| owner 要把 PRD→SRS 規模化（main codex orchestration，依序每頁、必到 db-schema/refactor reconcile、易疏漏處與完成後多開 sub-agent 驗）。**4 隻 sub-agent 審查**找出：plan ①④⑤ 編排層**形似神不在**（`orchestration-playbook` 是 code 階段設計，迴圈吃 code 板、三軸驗 git diff/build）、plan ③ **reconcile 無強制點**（機械/語意皆不查，**EPROZ00100 首跑實證被靜默跳過**：schema.sql 整段「待 RD」、無 delta 段，兩閘門皆放行）。**分 B+C+A+D 修，每批 2 隻 agent 複驗+修**：**A**＝`check-srs-bundle.py` 加 **gateⓇ reconcile**（warn 級，spec 須有「新舊 DB/更動 delta」段）＋ `spec-reviewer` 紅旗⑥（查內容真確/「只寫待 RD」）；**B**＝`orchestration-playbook §4b` **SRS N 軸驗證**（A–G 七正交軸〔綜合=spec-reviewer／as-is parity／錯誤碼〔含裸名〕／安全·授權／DB reconcile／**金錢·精度·截斷**／可測試性〕取代單一 spec-reviewer＝correlated-blindness；6 易疏漏陷阱逐軸對應）；**C**＝matrix「PRD→SRS 佇列 + ledger」表（逐 funcId｜risk｜prd｜status｜srs＝orchestrator 機械迭代來源+完成 ledger）＋ §5b/§6b SRS 軌迴圈/prompt；**D**＝一致性（PRD 檔名規約、worked example 升 `EPROZ00100`、N 軸 vs 單一 spec-reviewer 全檔對齊含 CLAUDE §4/AGENTS）。**pilot 派工卡**＝`build-tasks/prd-to-srs-orchestrator-pilot.md`（一頁、含對帳/拆佔位列/N 軸/回填 ledger/停等審）。複驗修：checkpoint key 軸盲區、佇列 bundle 列邏輯洞（佔位·待拆）、覆蓋計數衍生（頁數非列數、分母 67 外置）、N 軸計數正名（七正交）、降級漂移。**全程 `check-srs-bundle --all` exit 0 + `check-dualtrack-parity` exit 0**；commit `6dd69f6`→`f444273`。**意義**：plan ①④⑤ 補機械/結構件（佇列 ledger＋N 軸＋pilot），plan ③ 從「靠自律」變「過不了門」（gateⓇ＋紅旗⑥）。**剩**：owner 放 PRD（如 `EPROC00118`）+ 標佇列 prd-ready → 跑 pilot 一頁 → 過了再批量。|
| EPROC00118 pilot 跑通（2026-06-18, SRS 軌規模化首驗）| owner 放 00118 PRD + 標佇列 prd-ready → 母資料夾跑 SRS orchestrator pilot（`prd-to-srs-orchestrator-pilot.md`）→ **整條 pipeline 跑通**：取頁→產 SRS（4 檔、含 reconcile delta 段）→`check-srs-bundle` exit 0（8 gate 全 PASS；xfile camelCase 假警經 `088afaa` 硬化後消失）→**N 軸 A–G 各獨立 sub-agent 複審全 PASS**（**F 金錢軸實抓** deRatio `NUMBER(9,2)` 逐欄 numeric contract+QA-021A~E；C 裸名碼／D BE authz·E998·FE-only bypass／E `TB_API_AUTH`/`TB_ROLE_TASK` reconcile）→回填 ledger=in-review→**停等審**。**四守則守住**（停等審/不自宣 approved/不碰 C 類/N 軸真獨立）＝B+C+pilot 機械驗證有效。產 10 @PENDING TBD（TBD-002 與 c0 E1/E2 合流；TBD-006/007 closed）。我方接手：補 `trace-*EPROC00118*.md`、回填 SSOT（覆蓋 **1/67→2/67**、STATUS/feature-inventory/pending-register/matrix ledger）。PRD 快照 `PRD-CDC-EPRO-0001-EPROC00118-v1.0.md` 已 owner 補 push（`8542d19`）→ `spec.md:10` 上游連結解開、gateⒷ 對照通過＝**bundle 完整在 repo**。**pilot 過了→清場放大**（依佇列 risk 序逐頁）。〔gate 盲點**已裁（owner：以 PRD 為主）**：gateⒷ `REQ_RE` 放寬認 `(REQ|FR)-\d{3}`（commit 待補），FR 追溯改為真驗；00118 FR 鏈驗過 clean、00100（REQ-###）不受影響——PRD 編號慣例權威、gate 跟隨。〕|
| 00100/00118 全清重跑（2026-06-18, owner）| owner 決把已產的 `EPROZ00100`(v0.2)＋`EPROC00118`(v0.1) **全清重跑**——**理由＝兩頁是在 pipeline 硬化前/中產的**（00100 走舊流程：無 N 軸、reconcile 是「待母資料夾複核」placeholder；00118 是 pilot），現 pipeline 已完整（gateⓇ reconcile／N 軸 A–G／gateⒷ 認 FR／xfile camelCase／pilot 卡），用最終版在母資料夾**重產一致**。**砍**：兩頁 SRS bundle（`specs/srs/{EPROZ00100,EPROC00118}/`）＋ PRD 快照＋trace（`specs/prd/`）git rm；SSOT 退回 **0/67**（matrix 覆蓋/ledger 兩列→not-started、STATUS bar/一句話/Phase S、feature-inventory §2D:105/§2E:116/§5、pending-register 兩 TBD 列、prd/README、worked-example 5 處指標→「重產中」）。**留**：pipeline 本身（gateⓇ/N 軸/orchestrator/pilot 卡/gateⒷ FR/xfile）全保留＝pilot 已驗證有效、與 bundle 去留無關。**歷史**：本表 00100 產出／pilot 跑通列＝append-only 史實、不刪。**下一步**：owner 母資料夾重放 PRD（`PRD-*<funcId>*`）＋標佇列 prd-ready → 跑 pilot/orchestrator 重產 → push → 我重驗＋回填。|
| 00100 全清「留 PRD」修正（2026-06-18, owner via 跨模型稽核）| 稽核發現 00100 全清未做完：`decisions`〔上一列〕要 `git rm` PRD+trace、ledger→not-started，但實際 **PRD/trace 還在、ledger 仍 in-review**（覆蓋假成 1/67、且 orchestrator 會跳過重產）。owner 裁**留 PRD**（regen 必要 input、實檔在）：→ trace `git rm`、ledger→**prd-ready**（非 not-started，使重產可立即觸發）、`matrix:116`/`prd/README` 敘述改「00100 PRD 留」。**修正上一列對 00100 的 rm-PRD/not-started 意圖**（00118 仍照原全清、PRD 已不在）。連帶清理（同 commit）：`STATUS:9` 覆蓋退回 0/67+日期、`ADR-0001` gate ①–⑦、`decisions` `.github` de-backtick、`legacy-parity-sop` 補指 `spec-architecture §5b`、§5b 表補「先三判」。**gate 號漂移**（SKILL/repo-structure/spec-reviewer 的 ③）另案做 gate-taxonomy 對齊、不在本批。|
| local 對比輸入資料夾改名（2026-06-18, owner）| owner 在母資料夾把 **`docs/db-schema/`→`docs/db-diff/`**、**`docs/refactor/`→`docs/refactor-spec/`** 改名並**納入更多 spec**。**repo 側調整**（本批）：所有 live 引用（path＋概念標籤）改新名——`spec-architecture §5b`／`ADR-0002`／`legacy-parity-sop`／`orchestration-playbook`／`srs/README`／`SKILL`／`spec-reviewer`／`dispatch`／pilot 卡／`CLAUDE`／`AGENTS`。**保護未動**：`legacy/db-schema-catalog.md`（另一檔、非本夾）、`refactor-audit/`、`archive/`（凍結快照）、本表歷史列（append-only）。**⚠️ 待 owner**：兩夾**不在 repo**（母資料夾-local）→ 無法重掃內部結構；`SKILL §5`／`dispatch §5` 的結構描述（snapshot vs 完整 diff、`02_tables`/`02_specs`、檔數）為改名前快照、恐因「db-diff 改名＋更多 spec」失效——owner 把夾放進 repo 或回報新結構後再 refresh。`check-dualtrack-parity` exit 0、`check-srs-bundle --all` PASS。|
| db-diff/refactor-spec 結構重掃 refresh（2026-06-18, owner 掃描＋3 sub-agent 交叉驗）| 解上一列「待 owner 重掃」：owner 在母資料夾掃兩夾、3 agent 交叉驗 count 一致。**結果**：① **db-diff＝snapshot+change-hint**（187 檔/156 表、02_tables/01_groups、index `table_name`、markdown 非 DDL、**非完整 diff**、vs 舊 oldOnly/newOnly=0）→ **role 不變、`schema.sql` 規則照舊**。② **refactor-spec＝latest-only spec/artifact map**（881→737 檔、重排：03_artifacts 466〔latest〕／02_specs 146〔legacy 多版〕／02_modules 77；**index funcId→`module_code`+`artifact_id`**；epl-* ~285→270；validation→`04_rules/validation.md` 713〔literal=0〕；`open_issue` 已無；無正式 Rn/REQ；缺 `EPROC00119/00120`〔只 `EPROI*`〕）。**refresh**：`SKILL §5`/§DoD ＋ `dispatch §5` 對齊新結構、拿掉「待重掃」flag。`parity` exit 0、`check-srs-bundle --all` PASS。|
| EPROZ00100+EPROC00118 重產 pilot 驗收（2026-06-18, Option-1 試點；4 跨模型 N 軸 agent）| owner 母資料夾用最終 pipeline 重產兩頁、push 回 main。**機械**：`check-srs-bundle` 兩頁 exit 0（全 gate PASS、gateⓇ reconcile 段在）。**跨模型 N 軸（opus+sonnet, A–G）抓到單一 N 軸漏的真 Blocker**：① **00100** 🔴 R4/QA-016~019A 把 legacy page-id（`EPROIS_0171/0910/0920`）當 to-be route、違 Bible:793-794 `EPROISU/CSU` invariant（§5b 升級觸發#1 未升級）；🟡 `PENDING-Z001` 混 role-name fact（DB-resolvable）與 policy、`showRelated IS_Y` vs schema 欄名、`TB_API_AUTH` PK 單欄。② **00118** 🔴 F 軸（金錢/精度）`CUR_RATIO` 小數輸入未定（靜默截斷風險）/`debtRatio maxLength:22`⊥`NUMBER(6,2)`/item `_SCR` 無具體 QA 斷言；🔴 D-01 `TB_API_AUTH` seed 缺→端點未保護（`PENDING-012` 升 pre-deploy）。**誤報排除**：00118 三端點 xfile advisory＝refactor 檔名路徑非契約（contract camelCase 對齊 openapi）；00100 db-diff「dangling」＝母資料夾-local 設計、非缺陷。**意義**：機械層穩、但**單一 N 軸漏抓、跨模型複驗抓到**＝correlated-blindness 實證、`一次一頁+跨模型 N 軸+人審` 紀律有效。**下一步**：owner 母資料夾修 Blocker→重產/重驗→Approved；**修好前先別放大到 67 頁**。ledger 暫 in-review（覆蓋 2/67）、Blocker 待修。|
| 00100/00118 重產 Blocker 修正＋跨模型複審（2026-06-19, owner 修＋opus×2 採納後複審）| 解上一列「Blocker 待修」：owner 依 nfix 派工卡（`build-tasks/done/EPROZ00100-EPROC00118-nfix-card.md`）在母資料夾修兩頁、push `fdbeca2`。**修了什麼**：① **00100**＝R4/QA route 改回 refactor page-family（legacy `EPROIS_*` 降為 trace label、引 Bible:793-794 invariant）、Role Display Baseline 表補（role-name fact DB-resolvable 與 policy 分流）、openapi `role maxLength:3`、`TB_API_AUTH` 單 PK + ROLE allow-list、`showRelated` 改 `IS_CUB_RELATED/IS_CUBC_RELATED/IS_TCP`（對齊 schema 欄名）。② **00118**＝`CUR_RATIO` integer-only（不靜默截斷）、`debtRatio maxLength:8`／`totalAsset|totalLoanAmt maxLength:21`（對齊 numeric contract）、`TB_API_AUTH` seed 補（`PENDING-012` 升 pre-deploy）、F 軸金錢/精度 QA 斷言補。**兩層驗證**：機械 `check-srs-bundle` 兩頁 exit 0（gateⓇ reconcile 段在；gate② 殘警經查＝跨層同名 false-positive：openapi `role`(3)↔`TB_ROLE_DEFINE.ROLE_ID`(3) 非 `TB_API_AUTH.ROLE`(100)）；語意 **跨模型複審（opus×2，A–G 全軸，真獨立）採納修正後再審一輪＝無新 Blocker、無 fix-induced regression**（00118：4🔴+9🟡 全修實＝可進人審；00100：Z-B1+Z-S1~S7 全修實＋3🟢 nit〔DB-D4 `IS_Y` 措辭 attribution 略陳舊，非 blocking〕）＝「採納修正後必複審」紀律閉環、correlated-blindness 二度防護有效。**仍 Draft/in-review、未標 Approved**：00100 留 8 TBD、00118 留 `PENDING-001~018`（含 002/012/016/017/018 等 owner/RD/DBA closure）＝**不應放大或標 Approved**。覆蓋仍 2/67（兩頁 in-review）。回填 STATUS/feature-inventory/matrix/pending-register/本表。**限制揭露**：owner 修正端 agent＝gpt-5.5（非 opus+sonnet）＝我方獨立跨模型複審補位；本機缺 pyyaml、gate① 深驗略過（CI/母資料夾覆蓋）。**下一步**：owner/RD/DBA 裁定剩餘 pending → 升 Approved → 才依佇列 risk 序放大到 67 頁。|
| 00100/00118 owner-decision pending 全關＋N 軸再審（2026-06-20, owner gpt-5.5 修＋我方獨立驗收回填）| 解上一列「裁定剩餘 pending」：owner 母資料夾把兩頁**所有 owner-decision @PENDING 裁定/關閉**（00100 `PENDING-Z001~Z010`、00118 `PENDING-001~018`），push `d056803`；**bundle 內已無 open @PENDING**（`gateⓅ` PASS）。**N 軸再審**：scope 軸抓 00100 DB 查證行號錯位（已修）；spec-reviewer 抓 00118 cross-role Save map blocker → 改 **side-scoped save**（BE 依 session role 推可寫側、client 提供非可寫側 map 視為 tampering 拒絕、scorecard/summary/checkpoint 不動，`spec.md:119`）＋補 QA；regression 軸 PASS。**contract 軸 FAIL 但非 pending 未裁**：原因＝FE/BE 實作（DTO/status/`TB_API_AUTH` seed/save guard）尚未追上 to-be SRS——例 00118 四端點 c0 DB row 缺（`TARGET_COUNT=0/SOURCE_COUNT=4`）、`epl-info` 缺 `mainBorrowerName/checkpointStatus`、`parseScore` 仍吞 NFE→`0`、`getScoreDatetime` 無 `ZoneId`、save 無 `!isAO&&!isCR` reject＝**RD/DBA implementation gap**，**不以把 SRS 改回現況掩蓋**（owner 明示）。**證據（母資料夾 push 進 repo）**：`build-tasks/done/pilot-srs-pending-verification.md`（逐 pending closure＋DB SELECT-only 證據）＋`.sql`（schema `OVSLXLON02`、SELECT-only、未跑 INSERT）＋`build-tasks/c0-authz-sql-findings.md`（c0 00110–00120 `TB_API_AUTH`/`TB_ROLE_TASK` seed precheck：16 insert/1 UNSURE）。**兩層驗證（我方接手、獨立）**：機械 `check-srs-bundle` 兩頁 exit 0（8 gate 全 PASS、gateⓅ pending 同步 PASS）；**本環境有 pyyaml 6.0.1→`$ref`/required 深驗實跑 PASS**（補上 owner 本機略過那層）；`git diff --check` PASS；殘 3 advisory `!`＝00118 spec 提 `epl-*-c0-corporatescorecard` 散文／openapi 無 path（as-is 敘述、非契約缺）。**仍 Draft/In Review、未 Approved**：owner-decision 全清，但 **Approved 唯一殘 blocker＝contract 軸 implementation conformance**（RD/DBA 把 DTO/status/seed/guard 實作到 to-be＋測試證據；部署最硬＝00118 `PENDING-012` 四 seed＋非 AO/CR save reject 證據 "before any testing deployment"）。覆蓋仍 **2/67**。回填 STATUS/feature-inventory/pending-register/本表。**限制揭露**：owner 修正端 agent＝gpt-5.5；DB 證據母資料夾-local（SELECT-only）。**下一步**：RD/DBA 補 implementation/seed/測試證據 → contract 軸轉 PASS → 升 Approved → 才依佇列 risk 序放大到 67 頁。|
| 00100/00118 最終 spec-reviewer post-fix 複審＋contract closeout 派工卡（2026-06-20, opus×2 跨模型獨立、唯讀）| 解「採納 side-scoped save 修正後須再審一輪」(CLAUDE §4 blocking 語意關)：兩頁各跑獨立 `spec-reviewer`（opus、跨模型、不改檔）post-fix 複審。**結果：兩頁皆無 Blocker、無 fix-induced regression、可進人審/Approved-gating**。① **00118**：side-scoped save（`spec.md:119`）+QA 自洽、traceability 完整、錯誤碼承載齊；4🟡（`CalcCodeMap.required` 22 欄 vs R4 Default short-circuit〔calc 無 `loanDefDayFlag`〕、calc/save 錯誤碼分軸、`riskLevel/actionDate` 可寫側仍 client-writable、`23 items` 分解未明示）+3🟢。② **00100**：R4 route/seed/欄名修正自洽、Bible:793-794 EPROISU/CSU invariant 守住、8 端點真實 `epl-*`；2🟡（`TB_API_AUTH` 欄位集三處不一致〔schema 4/spec 3/evidence 5〕、trace sidecar `@PENDING` 失步）+2🟢。**我方收（記帳、非改 bundle）**：兩頁 trace sidecar 殘留 `@PENDING` 活錨（`PENDING-Z004/Z005/Z006`、`PENDING-012`）→ 改「已裁（owner 06-19/06-20）→ 殘實作」（兩 reviewer 共同點名、且 sidecar 維護規則本就要求 closure 時更新狀態；gateⒷ 仍 PASS）。**bundle-side Should-fix＝報而不改**（審者報·人審·審者不改）：TB_API_AUTH 欄位集對齊、CalcCodeMap/Default〔SA 裁〕、riskLevel/actionDate BE-derived 註——全列入 closeout 卡 §E，由母資料夾 pipeline／SA 收。**產 RD/DBA contract-軸 closeout 派工卡**＝`build-tasks/done/EPROZ00100-EPROC00118-contract-closeout-card.md`（逐端點 deliverable＋acceptance＋recheck SQL：00118 四 seed/save guard/info payload/parseScore fail-fast/UTC+8；00100 seed 去403/init/token/bridge/reasonList/六月 guard/docNo1）。**狀態**：spec 側 Approved-gating **語意關已過**（兩頁無 Blocker）；剩 contract 軸 RD/DBA 實作＋測試證據才升 Approved。覆蓋仍 **2/67**。限制揭露：複審＝opus（與 owner 修正端 gpt-5.5 不同模型）＝correlated-blindness 補位；DB 證據母資料夾-local。**下一步**：RD/DBA 照 closeout 卡實作 → contract 軸 PASS → 人類拍板 Draft→Approved。|
| 00100/00118 RD/DBA contract closeout complete and Approved (2026-06-20) | Follow-up to the closeout dispatch: DBA direct-applied and SELECT-only rechecked `OVSLXLON02.TB_API_AUTH` 12 rows; RD completed EPROC00118 DTO/status/save guard/parseScore fail-fast/UTC+8/numeric guard and EPROZ00100 B2 init redistribution, B3 one-time proposal download token + GET consume, B4 current-application session bridge, reason/range/docNo1. Verification: backend `mvn compiler:compile` PASS, frontend Angular build PASS, temporary SELECT-only JUnit PASS then removed, both `check-srs-bundle` runs PASS, `git diff --check` PASS. Both `spec.md` files now use dual-axis `Approved`; PRD-to-SRS ledger is `approved`; closeout card archived under `build-tasks/done/`. Full manual QA/UAT remains Phase V/regression, no longer an SRS Approved blocker. |
| 00100/00118 post-closeout 獨立複審＋00100 定向補洞（2026-06-20, 我方 opus×2 spec-reviewer 唯讀＋pyyaml 深驗）| 對母資料夾升 Approved 後的兩 bundle 跑「採納修正後再審一輪」(CLAUDE §4)。**本機有 pyyaml 6.0.1→`$ref`/required 深驗實跑兩頁 PASS**（補上母資料夾本機略過那層）；parity PASS。**00118：無 Blocker、維持 Approved**（新 `mainBorrowerName`/`checkpointStatus` 為 PRD FR-001 backed、QA-001/002/026/027 覆蓋、schema/openapi 長度對齊；兩既有 🟡〔CalcCodeMap.required 22 欄、save 端 riskLevel/actionDate〕未惡化）。**00100：複審坐實 3 缺口**（被標 Approved 卻殘留）→ **我方定向修補**（審者報·我方主流程改·經 PR 人審）：① `epl-file-z0-proposal-download` **GET token-consume 分支零 QA**（QA-034~036A 全測 POST 發 token；openapi GET 定義 200/403-expired/reused/unknown 但無 acceptance；機械 gate⑤ 因 R7 已有 5 covers 假綠）→ 補 QA-036B（happy 串 PDF/不洩本地路徑/標 consumed）+QA-036C/D/E（expired/reused/unknown→403 `ACCESS_DENIED`，驗一次性/短效），matrix REQ-007 同步；② `openapi.yaml:5` `Draft...Not approved` 橫幅 vs Status Approved 矛盾→改反映 Approved；③ `schema.sql:10` `implementation artifacts remain missing` vs REF-D5 已實作→改已實作+已 seed/verified。**修後**：`check-srs-bundle` 00100 PASS、`ACCESS_DENIED` 已宣告無 gateⒺ 漏承載、focused spec-reviewer 確認「缺口已補、無 Blocker、可維持 Approved」（殘 🟢 nit：GET 400/500 亦無 QA＝optional 非 DoD）。**限制揭露**：DB seed apply＋B2/B3/B4 backend code 本 repo 無 source→未獨立碼驗，仍 RD/DBA 回報（compile/build+SELECT-only JUnit）；QA/UAT 執行歸 Phase V（owner 裁，非本輪 blocker）。**價值**：再審一輪在母資料夾自檢＋機械 gate 都放行下，攔到 1 測試缺口＋2 自相矛盾橫幅。覆蓋仍 2/67、兩頁維持 Approved。本批走 PR 請 owner 審。〔2026-06-20 後續：owner「都處理」→ 併入同步和諧 `00118` `openapi.yaml:5` 殘留 `Draft SRS contract` 描述為 Approved 措辭（鏡像 00100；全 SRS bundle 已無 stale 橫幅，`grep Draft/Not approved/remain missing` 零命中），合入 PR #123 後合併入 main；repo 無 CI 閘門。〕 |
| SRS 軌 orchestrator 改 drain 模式（2026-06-20, owner）| owner：「多個 PRD ready、一次處理一頁、但處理到既有 prd-ready 都轉成 in-review」。pilot 已過（00100/00118→Approved）→ 把 SRS 軌迴圈從「每頁停 checkpoint」改為 **drain 迴圈**：允許多頁同時 `prd-ready`、**序列一次一頁**（非並行，保 context 衛生）逐頁過機械 gate＋N 軸＋ledger 回填，**一頁達標即接下一頁、不在每頁停**，直到所有既有 `prd-ready` 都 → `in-review`（或 FAIL/blocked 標因）才停在**單一 batch checkpoint** 一次交人審/裁 TBD。**守則不變**：終點 `in-review`（orchestrator 不自升 approved，升 approved 仍須人裁 TBD）、不碰 C 類、單頁 FAIL 不擋整批。落點：`orchestration-playbook §5b/§6b/§8`（迴圈權威）＋`prd-to-srs-codex-dispatch §用法/備註`＋`prd-to-srs-orchestrator-pilot §放大`＋matrix ledger 註。`check-dualtrack-parity` exit 0。|
| drain 模式 3-agent 審查＋Blocker 修正（2026-06-20, 多 agent 唯讀審）| owner「先多開 agent 確認流程」→ 3 隻並行審 drain（軸①邏輯/終止性·軸②跨檔一致性·軸③治理/安全）。**抓 1 真 🔴 Blocker**：FAIL 頁原設計「留 prd-ready」+ 迴圈「取最前 prd-ready」＝**同頁無限重取、終止條件自相矛盾** → 修：FAIL→`status=blocked`（離開 prd-ready 集合 → 每輪 |prd-ready| 嚴格遞減＝**保證終止**）、ledger status enum 加 `blocked`、附終止保證。**採納治理 🟡**：加 🛑circuit-breaker（系統性/重複失敗→暫停整批、修根因再重啟）、首批 ≤5 頁同 risk-tier 漸進、不中途改 ledger 排序、resume 冪等說明、不為吞吐降 N 軸。一致性審 🟢（殘留檢查無、職責邊界清；其報「`c0-legacy-parity-recheck.md` 缺檔」＝**誤報**，已查證檔存在）。落點：`orchestration-playbook §5b/§6b`＋`prd-to-srs-orchestrator-drain.md`＋matrix ledger 註/enum。`check-dualtrack-parity` exit 0。|
| orchestrator 度量化 + prompt parity + pilot 歸檔（2026-06-24, 黃仁勳視角批判採納 1+3）| 對 PRD→SRS orchestration 做批判檢視後採納兩項低風險優化：**① N 軸度量化（斷言→量測）**＝新增 `docs/process/n-axis-findings-ledger.md`（逐軸逐頁逐輪記 {提出 Blocker／確認為真／誤報}），`orchestration-playbook §4b` 加「度量驅動軸配置」bullet＋drain 殼加回填步驟與守則速查列；目的＝累積 ≥10 頁(T1 ≥5)後用實測 catch-rate 回調軸 profile／再審輪數，不再憑單一事件拍板（如 2026-06-22「全頁不降軸」）。**③ 消三處迴圈重複漂移**＝pilot 卡（功能已被 drain 取代、2026-06-20 起）`git mv`→`done/`（單頁首跑紀律併入 drain 首批段）；新增 `scripts/check-prompt-parity.py` 機械驗 `playbook §5b/§6b`（迴圈權威）↔ `prd-to-srs-orchestrator-drain.md`（運行殼）的 11 個迴圈不變式 anchor（同 `check-dualtrack-parity` 模式），掛 `docs/env/codex/hooks.json` Stop；active 檔 pilot 引用改指 drain/`done/`（`decisions`/`archive` 歷史列不動）。**未動**＝序列→pipeline（影響 playbook 核心語意，待討論）、人審瓶頸前置卡、風險校準軸 profile（待 ledger 數字）。`check-prompt-parity`/`check-dualtrack-parity`/`check-srs-bundle --all` 皆 exit 0。|
