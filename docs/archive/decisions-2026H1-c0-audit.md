# c0/評分 audit saga 決策歸檔（2026 H1）— frozen

> **凍結快照（2026-06-18 壓縮自 [`../decisions.md`](../decisions.md) §一）**。本檔＝c0 評分線（00117/00118/00120/CSU0130/0920-鄰）**已結案**audit 決策的 verbatim 搬遷，目的＝瘦身主流水帳之 context 量；**append-only 史實、不改寫**（同 `completion-ledger`/`archive/` 凍結慣例）。
> **現狀 SSOT 不在此**＝[`../feature-inventory.md`](../feature-inventory.md) §2C/§2D（c0 評分頁狀態）。E1/E2 escalation＝`../pending-register.md`＋`../build-tasks/c0-crediteval-e1-e2-escalation.md`。對舊企金 c0 parity 補比 reopen＝`../build-tasks/c0-legacy-parity-recheck.md`。新決策仍寫 `../decisions.md`。
> 列原序保留。

| 項目 | 內容 |
|---|---|
| 00118 calc 兩決策（2026-06-05 人審定案） | Codex recon 確認：**計分算法不在既有 `CsuCreditEval*`**（只做讀/submit gate/return 清欄位），唯一算法在 i0 `FunctionServiceImpl.funcGetRate`。**① calc 引擎＝注入 `FunctionService`**（§6.1 唯一例外，循 00114 precedent；複製進 c0 = 分叉算法、違反 §6.6）→ `verify-c0.py` 加 `ALLOW_SHARED_FUNC` allowlist、`AGENTS.md` §6.1 記錄例外。**② CU-return checkpoint 衝突＝另案上報**：既有 `CsuCreditEvalAndCreditDecisionServiceImpl` Return(98) 硬編碼只清 `TB_CHECK_POINTS_CS`、無 CU 分流，§6.1 禁改既有 `Csu*` → 不在 00118 修，列 `page-mapping.md` §2B escalation。shell/DTO/checkpoint 仍全自足。詳見 `build-tasks/done/EPROC00118-corporate-scorecard.md`。 |
| 00118 gate-b 裁決（2026-06-05 收工） | 獨立語意審查 9 PASS / 1 FAIL。FAIL 在 item 5「無第三者寫 crScoreCardCompleted 第2碼」——經人審**不阻擋出貨**：00118 自身鏡像 i0 正確（只動第2碼、保留第1碼），FAIL 點是**既有** `CsuCreditEvalAndCreditDecisionServiceImpl:2890` 整欄覆寫 `"NN"`（既有碼、§6.1 不改、corporate 決策生命週期）→ 與 CU-return 同類，**升級為 escalation 2**。`EPROC00118` ✅ 完成（build 綠 + verify-c0 + 語意審查）；2 條 escalation 交 CreditEval owner + 整合驗證。教訓：review 放行條件勿把「既有碼行為」寫成新頁的契約。 |
| CSU0130 audit 裁決（2026-06-05） | 既有前端＝半做（非空白、同構鏡像個金 twin）。**裁決：本頁是案件編輯流程子頁（展開式編輯），oracle＝個金 twin `EPROISU0130`、非 deputy 樣板** → deputy-template 缺項（search-item/app-table-search/add-config/role-id/popup-dir/檔名）**豁免**（twin 也沒有、硬塞會破壞案件編輯 shell 一致性）。真正修（對齊 twin）：① `getGuarantorCorpResume()` 端點 bug `epl-resu-isu-guarantor`→`-csu-` ② 補 error handling+loading reset ③ popup `enumGuarantor` 去個金 model 耦合 ④ 狀態對齊 twin、勿 gold-plate。**通則**：案件編輯流程子頁鏡像其 twin（isu↔csu），**不套 deputy CRUD 樣板**；deputy 樣板僅適用 z0 式獨立 CRUD 頁。**FE-gate 想法**：①的「corporate 誤引 `-isu-/-i0-` 端點」正是 deterministic gate 可抓——未來可把 `verify-c0` 式檢查延伸到前端（corporate feature 禁引 `-isu-/-iu-/-is-/-i0-` 端點字串）。**✅ 已收尾（2026-06-05，ng build 綠、3 檔；端點掃描僅 1 筆誤引、已清；無 gold-plate）。** |
| 00117 audit 結案（2026-06-05） | 唯讀 audit 判定**既有 `CsuFinancialStaff*` 模組已滿足 00117 規格（缺無、錯無）→ 早已完成**（cross-check 誤判為缺）：5 端點 `-c0-`、只取 00117 分支（無 00120 夾帶）、INFO/INFO_S/GI 三組、`info-financial-business` 寫回 GI side-effect 保留、checkpoint `Cs/Cu.EPROC00117`、DTO 1:1、無 reflection/individual（負向 grep 未命中）。audit 本身即 evidence-cited 語意審查 → 既有非本期生成碼**不再疊 gate-b**。順帶印證使用者早先「5 頁已完成」（115/116/117/119/120）正確。**c0 評分線 `00115–00120` 全數結清**；剩 `0920`(後端·無 i0) + `CSU0130`(前端)。 |
| 00117 盤點兩裁示（2026-06-05） | Codex Step-1 唯讀盤點發現：**① 既有 c0 `CsuFinancialStaff*`/`csuFinancialStaff` 模組已存在**（page-mapping 原記「缺」有誤）→ 改**唯讀 audit vs 00117 規格**、勿重建；完整則結案、有缺再裁示 patch（改既有檔另核可）。**② `commonFunctionService.funcIsStaffLoan` 在 `service.common`（非 `individual`）→ 直接可用、非 §6.1 耦合、非例外**（與 00118 的 `service.individual.FunctionServiceImpl.funcGetRate` 不同支；`verify-c0` 只擋 `.individual.`）。已記入 `AGENTS.md` §6.1。**教訓**：「未做其實已完成」後端也會發生 → 任一頁開做前一律先唯讀盤點實際完成度。 |
| 00117/00120 staff-vs-business 裁決＝**B business-only**（2026-06-09） | 對舊 source 驗畢：舊 corporate c0 00117/00120 為 BusinessOwner-only（舊 c0 trx/module 無 staff 分版/staff save、staff token grep 全空）；新 `funcIsStaffLoan` 對 CS/CU 恆 false＝忠實沿用舊 i0（staff 僅 IS+03 / IU+01，`legacy-epro/.../EPRO_I00111.java:267`）、**非 regression** → 只建 business 版；判定來源＝容器 `businessType`/`pageMap`（FE 不打 funcIsStaffLoan）；c0 staff 端點＝鏡像多餘物 → cleanup（已清 `dcd9602`）。另：Step 1 容器 DTO 對齊 c0＝只 `businessType`/`pageMap`（無 i0 `assessmentType`/`isStaffLoan`）→ 容器只留 businessType radio。（原記於 feature-inventory §2D，2026-06-11 健檢依「狀態表不放敘事」紀律移入本檔） |
