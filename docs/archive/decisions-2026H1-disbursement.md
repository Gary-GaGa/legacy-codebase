# 撥貸 saga 決策歸檔（2026 H1）— frozen

> **凍結快照（2026-06-18 壓縮自 [`../decisions.md`](../decisions.md) §一）**。本檔＝撥貸/T24/批次層**已結案**決策的 verbatim 搬遷，目的＝瘦身主流水帳之 context 量；**append-only 史實、不改寫**（同 `completion-ledger`/`archive/` 凍結慣例）。
> **現狀 SSOT 不在此**＝[`../feature-inventory.md`](../feature-inventory.md) §2F（撥貸狀態）＋[`../disbursement/disbursement-domain-escalations.md`](../disbursement/disbursement-domain-escalations.md)（待決）。新決策仍寫 `../decisions.md`（append-only 續流）。
> 列原序保留；列內 cross-ref（`supersede 上列`等）指同批搬遷列、語意自洽。

| 項目 | 內容 |
|---|---|
| 0920 盤點裁決（2026-06-05） | Codex 唯讀盤點：**撥貸後端已存在**——`0921 DataInputController`(6)+`0922 SummaryController`(4 FE-used) 服務 FE 全部 9 個 `epl-*`，0920 僅頁框；**「BE 未做」係 stale（第 3 次 recon 救援，繼 00117/CSU0130）**。**裁決：方案 A 認列既有、不新建 0920 facade（FE 不打 0920 專屬端點）、不改既有 service**。⚠️ **正確性無法在 workspace 認證**：舊 `EPROIS_0920` source 不在、無 i0/twin oracle → 撥貸狀態機（`CASE_PROGRESS` 24/25/26/98、T24 授權檔/SFTP/mail、`EPORIS_0921` flag、checkpoint 走 `CASE_PROGRESS` 非 `TB_CHECK_POINTS`）**走撥貸 domain + 整合驗證**（§6.6 不臆測、不重寫）。Codex 正確地未幻覺舊行為、改以既有 migrated service 為可讀基準並標 unverified。cleanup 候選：`/epl-case-submit-isu-summary` stub（FE 未用）。**結論：30% 的 CODE 結構性全到位，殘留＝驗證階段。** 使用者 2026-06-05 確認：① 認列既有、撥貸正確性走 domain+整合驗證 ② 殘留驗證項彙整成 [`verification-handoff.md`](verification/verification-handoff.md)。 |
| 0921 Step B 比對結果（2026-06-05） | 舊系統比對（Step A 抽 → A2 schema-map → B 比）首戰：撥貸 `0921 Data Input` **「結構在、行為不對等」7 PASS / 15 FAIL / 5 UNSURE**（詳 `verification-handoff.md` §2.1；逐項表本機 gitignore）。**推翻「結構到位＝完成」**：migrated service 存在 ≠ 忠實。關鍵裁決原則：**舊系統 ≠ 絕對正確**——`KHR` fee rounding 等屬刻意在地化演進，**不可一律改回舊版**，每項須分「regression vs 演進」。信心分層：🔴 資料完整性（collateral 寫 null、fee 過刪、fee 公式 null）較高；🟠 檢核對等較低（部分倚賴舊 spec UNKNOWN）。**使用者裁決：先跑完 `0922`（Summary + T24 組檔，含 D8 的 `T24_COMPANY`/金額精度）再整體 triage**，避免 piecemeal。連帶待議：c0 頁（00115–00120）當初只對 i0 audit、未對舊系統行為比 → 撥貸收斂後評估是否補比。 |
| 🔴 撥貸 authorize 核心未完成（2026-06-05 複驗確認，里程碑更正） | B-0922-main + 複驗確認：新 `authorize`（`caseIsuSummaryAuth`）的 `funcGetExchangeRate`（`FunctionServiceImpl:1221`）在 `0000` 成功分支寫完 `TB_DISBUR_DATE`/`TB_EXCHANGE_RATE` 後**無條件丟** IDE stub `UnsupportedOperationException`、全方法無 return → **T24/SFTP/record/狀態(26) 全部到不了，authorize 從未端到端跑過**。**正式更正先前「`0920` 認列既有＝30% 結構到位」**：撥貸後端 controller/service 在，但**執行核心（換匯→組 T24→SFTP→定案）未完成**。範圍有界（兩檔內唯一 stub，修點侷限該方法尾段），但屬金錢核心 + 同路徑另有真 bug（匯率源 ID `OVSLXLON01→02`、submit mail 送空清單、history 碼 `25→24`）+ partial-write 風險。**此發現直接證明 old-system 比對的價值**：只看「有 controller/service」會把 throw-stub 當完成、UAT 首次撥貸才爆。處置：併入撥貸整體 triage（B-0922-t24 後）。 |
| 撥貸返工驅動裁決（2026-06-05） | 全撥貸 triage（0921+0922-main+0922-t24）見 `disbursement-triage.md`。**使用者裁決：Codex 先修「機械、無歧義」項、判斷題升級。** scoped 授權（`backend/AGENTS.md` §6.7）：Codex **得**編輯撥貸 service，**僅限** `disbursement-triage.md` §7 allowlist（Tier1 結構 M1–M4、Tier2 來源/欄名 M5–M9、Tier3 金錢刪除 M10）；逐項引舊 spec、單項 commit、**先回報 diff 供人審才推產品 repo**、不外擴。**🚫 升級不得順手改**：`funcGetExchangeRate` stub 實作、`T24_COMPANY` 值來源、匯率源 ID、檢核嚴格度、架構/精度、`KHR`（刻意演進勿改回）。原則仍是 §6.6「遇判斷題就停」。 |
| 撥貸 Tier-1 修正落地方式（2026-06-06） | M2/M3/M4（commit `2417cfe`/`49ebcb1`/`1d39e1d`）diff 經人審對齊舊 spec、scope 乾淨、`package` 綠 → 核可。**產品 repo 落地＝feature branch + PR**（非直接推 master）：金錢碼多一道產品 repo 審查/CI、可單獨 revert。同時裁決：**c0 + 共用 `FunctionServiceImpl` 的 runtime-stub 掃「現在平行派」**（唯讀、不衝突）——因 `funcGetRate` 與已坐實的 `funcGetExchangeRate` 同檔，是把「c0 已實作」從 build-綠**推定**收成**確認**的唯一動作（見 `completion-ledger.md` §4 盲點）。 |
| ⚠️ 落地方式更正（2026-06-06，supersede 上一列前半） | 實際上三筆**已被 push 到 `origin/master`**（先於分支決策、繞過 PR gate）→ 從 master 開 PR 為空 diff、不可行。重新裁決：**接受在 master、不開 PR、不 revert**（理由：diff 已人審 `91ecd0c`、stub 未修前 authorize 不會跑→修正暫 inert、revert+重上純 churn 且 force-push-free 的 revert 路徑亦不值得）。**教訓/前向約定**：落地方式須**開工前定**，否則會重演「已在 master 才發現」；**Tier 3 M10 金錢刪除建議強制 feature branch + PR**（過產品 repo CI/審）。stub-sweep 平行派之裁決不變。 |
| 撥貸落地紀律最終定案（2026-06-06） | 背景：先審後推 gate **連 3 批未守住**（修正在放行前已上 master）；本批（Tier 2）甚至連帶把**未授權的 H8 換匯邏輯**＋**疑無效的 M7**（Oracle 下 `get("loanAmount")` 多半回 null）一起推上 `a353d10`。user 拍板：**master-direct 全程（含 M10 金錢刪除）＋ 前向修模式**（接受風險，不改用 PR/protected master）。⚠️ 代價＝**M10 無平台 gate**，故其 pre-push 人審採**最嚴**標準（確認只刪 `CON_TYPE=FN`、單獨 commit）。Tier-2 收尾：M5/M8 OK；M1 的 H8 越權已 `4089978` 還原；**M7 往前修成 `get("LOANAMOUNT")`**（比照 native-query 大寫先例 `:1781`）；M6 升級 domain；M9 授權界定擴充（注入 `TBDistrictRepository`，鍵含 `UPD_DATE` 須選對列）。附帶：map-key 大小寫混用列獨立 sweep。 |
| A-5 幣別收窄裁定（2026-06-16, owner）| owner 答 A-5 domain 題「撥貸有無 USD/KHR 以外有效幣別」＝**無（柬埔寨只 USD+KHR）→ 收窄無害、keep + 補規格**。即新系統對 non-USD-non-KHR 的 fee→null/E21→0 path＝**by-design-unreachable**（業務上無該幣別）、非 bug，**不做對等修**（不恢復 non-USD 通吃）。**補規格**＝撥貸有效幣別 USD+KHR 寫 triage（正式撥貸 spec 補）。殘小（非阻擋）：KHR rounding `DOWN`（keep）、G/H 幣別來源（USD/KHR 內 disbursement vs account 差異→資料約束待 RD）。→ **A-5/A-3 關**。 |
| KHR 幣別坐実完成（2026-06-16, Codex 唯讀）| 解 A-5「疑似」→ **推翻「新增 KHR 在地化」**：實為**收窄**——舊 non-USD **通吃**（全換匯/`Math.round`），新只明確 USD/KHR、其他 non-USD → null（0921 fee）/輸出 0（E21）＝**regression risk**；E21 意圖 UNFOUND；T24 G/H 幣別來源舊 `DISBURSEMENT_CURRENCY`→新 main-borrower account `CURRENCY`。**→ A-5 升 domain**：撥貸有無 USD/KHR 以外有效幣別？**有→對等修**（照舊恢復 non-USD 通吃，＝owner「T24 照舊」自然修 regression）、**無→keep+補規格**；另 KHR rounding（新 `DOWN` vs 舊 round）、G/H 來源。**教訓**：A-5 本就「疑似」、我的「維持新」更反了——**坐実才見真相（收窄+regression，非新增在地化）**，不可由推斷代替碼證。findings＝`khr-currency-handling-recon-findings.md`（Codex 工作樹，待 push）。 |
| T24 全欄照舊系統規格（2026-06-16, owner）| owner：「T24 都照舊系統規格」→ 撥貸 T24 組檔 outbound 欄值/來源/格式/截斷**一律對齊舊**（延伸 A-1/A-2 parity 原則到整個 T24 檔）。**收編 B-group**：B-2 `A16`、C12 來源表、C13 日期源、A15 空值、G11-12 fee remark、**B-4 `AGREEMENT_NO` 取後 16**、**B-5 行尾/C26**、**A-3 `E21` 非 USD 全換匯（非輸出 0）**、G4/G10/H8 換匯欄位來源 → 全照舊。**邊界（修正 08:57——原述過度宣稱，owner 質疑後校正）**：KHR 幣別分支＝**疑刻意在地化但未確認**，非確定保留。事實：新 fee rounding 有 `KHR`+`RoundingMode.DOWN`、E21 有 KHR 處理（`handoff §2.1:54`/`§2.3:85` Step-B 坐実）＝**真有新碼**；但「刻意演進」係推斷（A-5「🟢 疑似」、handoff「多半」）、**「舊僅 USD」只對 fee rounding 分支**（舊 `getExchangeRate` 仍 `CcyCode=KHR` 查匯率，`a1-oq:8`）。→ T24 batch-fix **先不動 KHR 換匯/fee 幣別分支**（不假設保留、不自動 revert），**另派 Codex 坐実新舊幣別處理差異 + domain 確認**。其餘 T24 欄照舊。→ B-group 轉**執行階段**＝Codex 逐欄坐実舊行為+對齊（金錢欄最嚴人審），卡＝`t24-bgroup-legacy-parity-fix.md`。殘撥貸 domain（非 T24）：A-4 檢核嚴格度、M6 完工日 DTO 來源、**KHR 幣別分支（坐実+domain）**。 |
| 批次層 AUD-10 碼驗完整回（2026-06-16, Codex 唯讀；findings 完整版已上 main `23aff35`）| **新系統批次重編號≠legacy 號**（職責對映）。**6/8 FOUND**（app 碼存在，全在新 `TaskController` + `EPROZ0B00x ServiceImpl`，`@Scheduled`）：B001 branch→新B001、B002 emp→新B002、**B003 自動結案→新B007**、**B004 暫存清理→新B003**、B006 結果檔→新B006、**B007 SFTP→新B004**＋authorize inline 上傳 T24。🔴 **B005 匯率排程＝唯一 app 碼缺**：新只有 inline 換匯（A-1 `funcGetExchangeRate`），無 scheduled 等價物（新 `EPROZ0B005`＝mail sender、`@Scheduled` 註解掉、不能抵）→ **待 domain 判「inline 取代每日批次 vs 漏建」，判準＝有無非-authorize 路徑讀當日 `TB_EXCHANGE_RATE`**。⚪ **B008 log 歸檔 UNFOUND→ops**（logrotate/平台）。findings＝`done/aud10-batch-layer-reverify-findings.md`（Codex 完整版，含 file:line + 新後端批次入口盤點）。**B005 後續判定（同日 Codex 唯讀查證）：銷案**——backend **無任何 `TB_EXCHANGE_RATE` read 點**（entity/repo 無呼叫、無 native `FROM/JOIN`、`FunctionServiceImpl:1208/:1218` 純 write）；T24 組檔/報表讀的是個案 `TB_DISBUR_DATE.EX_RATE_*`（inline 寫），非每日 `TB_EXCHANGE_RATE`（write-only vestigial）→ **inline 取代每日批次成立**。**⇒ AUD-10 結（app 層完整：6/8 FOUND + B005 inline-取代）**；**B008 log 歸檔＝ops 追蹤**（非 app 碼，見 escalations 附）。連帶：B001 FOUND → B-1 `T24_COMPANY` 有 branch import 資料路徑。card+findings 收 `done/`。 |
| A-1 spec-conformance PASS（2026-06-16，Codex 唯讀碼驗）| **A-1 實作 `daae4c3` 經 4/4 conformance PASS**（file:line 齊）：OQ-1 `IdNo=OVSLXLON01`(`:1181`)、OQ-3 非0000拋錯＋authorize 中止（T24 前；`SummaryServiceImpl:2324/2370`）、OQ-4 callApi catch throw＋null guard throw、無 return null（`:1262/1267/1271`）、§5 兩表同 `@Transactional`（`:1156`＋`DynamicUpdateSqlUtils:27` REQUIRED）。**OQ-3 nuance**：錯誤碼＝**`FAILED_E304`**（新系統專屬代碼，非 recon 警告的泛用 `E303`；語意對等舊 `EPROIS0921_UI_RAET_FIND_ERROR`）→ 合格。**A-1 全結**（金錢核心驗證過）→ 規格/recon/investigation 三檔收 `done/`。撥貸端到端真完成另需 T24 正確性＋批次層（AUD-10）。 |
| A-1 換匯 stub 落地（2026-06-16, product `daae4c3`）| **`funcGetExchangeRate` 已實作**（依 a1-spec OQ-1~5：補 return＋移尾端 throw；mvn clean package 綠；附 3 測試修＝CaseDistribution 8 參數簽名/Scorecard export 改 POST/FileService @TempDir）→ **原唯一 🔴 coding 缺口（撥貸 authorize 換匯總開關）收**。⚠️ **terse 回報未涵蓋 spec-conformance**＝金錢核心須碼驗/Phase V 逐項確認：OQ-1 `IdNo=OVSLXLON01`(parity 裁定)、OQ-3 映射 `EPROIS0921_UI_RAET_FIND_ERROR`(非泛用 FAILED_E303)、OQ-4 catch 明確 throw 勿回 null、兩表同交易。撥貸**端到端真完成**另需 T24 正確性(domain-gated)＋**批次層 B001–B008**(F-OWN-1 新查出、待碼驗)。回填 STATUS/feature-inventory §2F/escalations A-1/triage P0-1/a1-spec。 |
| 撥貸 outbound 參數先對齊舊系統 parity（2026-06-16, owner）| **「先固定跟舊系統送出參數相同」** → 撥貸對 T24 的 outbound 參數，凡新/舊分歧者**預設取舊 parity**。落地：**OQ-1 `IdNo=OVSLXLON01`**（非新 stub `OVSLXLON02`）、**A-2 匯率源 ID=`01`**（同一分歧）、**OQ-3 映射 `EPROIS0921_UI_RAET_FIND_ERROR`＋非 0000 中止 authorize**、**OQ-4 catch 明確 throw 勿回 null**（OQ-5 G/H 讀 `EX_RATE_BUY` 已修 `581e717`）。**A-1 由「卡 owner」轉施工-ready**（撥貸 domain RD 補 stub，非 Codex 逕補）。⚠️ **唯一復驗點**＝schema-diff 證據略偏「02=新庫刻意」，故定性為「先固定 parity」；若新環境 T24 拒收 `01`，改 `02` 並回填（OQ-1）。回填：`a1-funcGetExchangeRate-spec.md` §2/§7/§8、escalations A-1/A-2、triage §2、recon OQ-1、pending-register、STATUS。 |
| 撥貸殘 domain 全裁＝照舊系統處理（2026-06-17, owner）| owner 對 pending 清單 A 組逐項裁：**A.1 T24 端到端**＝先等 batch-fix 完成後**到 UAT 測**（不在 workspace 驗、維持 parked，唯一復驗點仍含 `IdNo=01` 拒收→改 `02`）；**A.2~A.5＝照舊系統處理**——**A-4 `0921` 檢核嚴格度**：各檢核（`CheckMainBorr/CheckCoBorr`、`CO_CHECK='Y'` vs 舊 `!='N'`、Finished gate `mbCheck`、law firm `IS_SHOW`、address `UPD_DATE` 源）一律**對齊舊系統**＝差異視為 regression 修齊、非刻意演進；**M6（D-1）完工日 DTO 來源**：照舊系統來源接回（Codex trace legacy FE/衍生點→寫回非 null、解 `setEstComDate(null)`）；**B-1 `T24_COMPANY`**：兩 schema 取 **`OVSLXLON01`**（＝A-1 OQ-1 已裁舊 parity 源）→ RD 補 entity 映射＋`B8/C9` 接值；**KHR 幣別 G/H（`G4/G10/H8`）**：換匯欄幣別**來源**照舊（坐實舊 `DISBURSEMENT_CURRENCY` 源、對齊）——**A-5 的 USD/KHR 收窄 + KHR rounding `DOWN` 仍 keep 不變**（兩軸：收窄 keep、來源照舊）。**⇒ 撥貸 owner-decision 全清空**（A-1/批次層/T24/殘 domain 全裁）→ 撥貸剩**執行**（Codex B-group batch-fix＋B-1 entity＋M6 trace＋A-4 對齊＋G/H 來源坐實）＋**T24 UAT**。supersede line 55「殘撥貸 domain：A-4/M6/KHR 待裁」。回填 escalations A-4/D-1/B-1/B-3、pending-register、STATUS。 |
| T24 B-group commit 狀態查證結案（2026-06-17, product git log）| 承上列「**1 項需 owner 裁**：T24 B-group commit 狀態—findings `Commit=pending` vs `triage:78`/`escalations`「已 commit/push」打架，真值取決 product repo git log」開項 → 母資料夾 git log 查證回報：**整批已 push**，commit **`3d6f446`** "fix(disbursement): align t24 b-group fields with legacy"（2026-06-17 10:41:12 +0800，已在 `origin/master`、HEAD `9a5a9fe`、工作樹乾淨、無 working-tree-only 殘留）。9 欄（`A16`/`C12`/`C13`/`A15`/`G11-G12`/`A31`/`G7`/row-ending/`C26`）git blame 各指該 commit；新 T24 組檔集中於 `SummaryServiceImpl.funcIsuT24Authorize`（`:887`），legacy `createTransferA–H` 無 1:1 對應方法。**裁定＝findings 卡 `Commit=pending` 過期** → 全 repo 回填 `3d6f446`（findings 卡 9 列＋Scope／`triage:78`／`escalations:19,23-26`／`feature-inventory:139,143,181,232`（SSOT）／`STATUS:6,12,19,31`）。supersede 上列「真值待裁」。撥貸 B-group 由「commit 狀態存疑」→**已 push 確認**；剩端到端/T24 接收驗證（UAT）不變。|
