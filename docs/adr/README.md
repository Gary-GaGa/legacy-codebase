# ADR — 架構決策記錄

> 重大架構/流程取捨寫成一則 ADR（不可變、append-only）。`decisions.md` 仍是**逐輪事實 log**；ADR 是**離散、可連結、可追溯**的決策單元（SRS §Trade-offs 連到對應 ADR）。
> 命名：`ADR-NNNN-<slug>.md`（slug 用 funcId 或主題）。Status：`Proposed` / `Accepted` / `Superseded by ADR-XXXX`。
> 範本：`_TEMPLATE.md`。新決策用 ADR；既有 `decisions.md` 不回頭遷移。
