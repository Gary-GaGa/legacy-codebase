# QA Cases - EPROZ00100 To Do List

| QA ID | Covers | Type | Given | When | Then | Test-ready |
|---|---|---|---|---|---|---|
| QA-001 | covers: R1 | happy | Role `001` enters dashboard with prior application session. | POST `epl-init-z0-todolist`. | Prior session is cleared; response includes `role`, `isAO`, `isCA`, `isCRCOSCO`, `isCOSCO`, `isCADMERCER`. | Yes |
| QA-002 | covers: R1 | happy | CA role `101` has a case requiring CA redistribution. | POST `epl-init-z0-todolist`. | `TB_LON_SUMMARY_INFO` updates current owner fields and `TB_APP_HISTORY` inserts one redistribution row. | Yes |
| QA-003 | covers: R1 | happy | CR role `102` has stale proxy case requiring redistribution. | POST `epl-init-z0-todolist`. | Current user is reassigned according to CA/CR source rule and history is inserted. | Yes |
| QA-004 | covers: R1 | error | Redistribution update succeeds but history insert fails. | POST `epl-init-z0-todolist`. | Response carries `REDISTRIBUTION_FAILED`; summary and history changes roll back. | Yes |
| QA-004A | covers: R1 | error | Dashboard init role/session/setup query fails before redistribution can commit. | POST `epl-init-z0-todolist`. | Response carries `MSG_QUERY_FAIL`; no redistribution side effect is committed. | Yes |
| QA-005 | covers: R1, R9 | error | User has no API authorization for dashboard init. | POST `epl-init-z0-todolist`. | Request is rejected; no redistribution side effect occurs. | Yes |
| QA-006 | covers: R2 | happy | Current AO user has active tasks and excluded-status tasks. | POST `epl-list-todolist` with `queryMode=GENERAL`. | Response includes only allowed tasks and excludes `03`, `D1`, `R0305`, `R0313`, `R0397`. | Yes |
| QA-007 | covers: R2 | happy | A task has `TB_RELATED_PARTY_INFO` rows where `IS_CUB_RELATED`, `IS_CUBC_RELATED`, or `IS_TCP` is `Y`. | POST `epl-list-todolist` with `queryMode=GENERAL`. | `showRelated=Y`; otherwise `showRelated=N`. | Yes |
| QA-008 | covers: R2 | happy | A task has AO loan condition rows in USD and KHR, including decimal cents at the `NUMBER(17,2)` boundary. | POST `epl-list-todolist` with `queryMode=GENERAL`. | `usdSumLoanAmount` and `khrSumLoanAmount` match exact `TB_LOAN_CONDITION_DETAIL` sums by currency with two-decimal precision and no silent rounding/truncation. | Yes |
| QA-009 | covers: R2 | edge | `APPLICATION_DATE` is the sentinel `01/01/1900`. | POST `epl-list-todolist`. | Display date is blank while raw DB remains unchanged. | Yes |
| QA-010 | covers: R2 | error | General list query repository fails. | POST `epl-list-todolist`. | Response carries `MSG_QUERY_FAIL`; no DB mutation occurs. | Yes |
| QA-011 | covers: R3 | happy | CAD/TLOD role `404` has active proxy employees. | POST `epl-list-todolist` with `queryMode=CAD` and valid condition. | Query scope includes current user and active proxied employees only. | Yes |
| QA-012 | covers: R3 | happy | CAD search uses `docNo` for a corporate case. | POST `epl-list-todolist` with `queryMode=CAD` and `docNo`. | Request maps to legacy-compatible document/register search across identity document fields plus `REGISTER_NO`; response `docNo1` comes from `V_MAIN_BORROWER_INFO.DOC_NO_REGISTER_NO` and equals `REGISTER_NO` for the corporate case. | Yes |
| QA-013 | covers: R3 | error | CAD query has no effective filter. | POST `epl-list-todolist` with `queryMode=CAD`. | Response is 400 with `INVALID_CAD_QUERY_CONDITION`; no full-table query is executed. | Yes |
| QA-014 | covers: R3 | error | CAD query provides only `startDecisionDate` or only `endDecisionDate`. | POST `epl-list-todolist` with `queryMode=CAD`. | Response is 400 with `INVALID_CAD_QUERY_CONDITION`. | Yes |
| QA-014A | covers: R3 | error | CAD query provides `startDecisionDate=2026-01-01` or any non-`dd/MM/yyyy` string. | POST `epl-list-todolist` with `queryMode=CAD`. | Response is 400 with `INVALID_CAD_QUERY_CONDITION`; no query executes. | Yes |
| QA-015 | covers: R3 | edge | CAD query date range has `endDecisionDate` later than `startDecisionDate` plus six calendar months. | POST `epl-list-todolist` with `queryMode=CAD`. | Backend rejects the request with 400 `INVALID_CAD_QUERY_CONDITION`; no query executes. | Yes |
| QA-016 | covers: R4 | happy | CA opens IS/IU or CS/CU case from list. | Route using list item page metadata. | Target route family is `EPROISU0171` for IS/IU or `EPROCSU0171` for CS/CU; legacy IDs remain trace labels only. | Yes |
| QA-017 | covers: R4 | happy | CAD/TLOD Maker role `404` opens cases with `CASE_PROGRESS=21` and `22`. | Route using list item page metadata. | Response page family is `EPROISU0910` for both statuses, with legacy `EPROIS_0910` only as trace evidence. | Yes |
| QA-018 | covers: R4 | happy | CAD/TLOD Maker role `404` opens cases with `CASE_PROGRESS=24` and `25`. | Route using list item page metadata. | Response page family is `EPROISU0920` for both statuses, with legacy `EPROIS_0920` only as trace evidence. | Yes |
| QA-019 | covers: R4 | happy | CAD/TLOD Checker role `405` opens case with `CASE_PROGRESS=23`. | Route using list item page metadata. | Response page family is `EPROISU0910`, with legacy `EPROIS_0910` only as trace evidence. | Yes |
| QA-019A | covers: R4 | happy | CAD/TLOD Checker role `405` opens case with `CASE_PROGRESS=25`. | Route using list item page metadata. | Response page family is `EPROISU0920`, with legacy `EPROIS_0920` only as trace evidence. | Yes |
| QA-020 | covers: R4 | edge | CAD/TLOD case does not match maker/checker routing statuses. | Route using list item page metadata. | Response page is exactly `MANUALAPP`. | Yes |
| QA-020A | covers: R5, R9 | happy | AO role `001` is authorized to query delete reasons. | GET `epl-sele-z0-delete-reason`. | Response reason options exactly carry the approved DB baseline `D01`-`D12` plus `D99`; unauthorized roles receive platform auth rejection and query failure returns `MSG_QUERY_FAIL`. | Yes |
| QA-020B | covers: R6, R9 | happy | CAD/TLOD role `404` or `405` is authorized to query close reasons. | GET `epl-sele-z0-close-reason`. | Response reason options exactly carry the approved DB baseline `C01`-`C11` plus `C99`; unauthorized roles receive platform auth rejection and query failure returns `MSG_QUERY_FAIL`. | Yes |
| QA-021 | covers: R5 | happy | AO role `001` selects delete reason list `['D01']`. | POST `epl-case-insert-delreason`. | `TB_DEL_REASON.REASON_CODE` stores the DB semicolon form, `TB_APP_HISTORY` inserts, and `TB_LON_SUMMARY_INFO.CASE_PROGRESS=D1` (Delete) commits together. | Yes |
| QA-021A | covers: R5 | happy | Proxy user performs an allowed delete action. | POST `epl-case-insert-delreason`. | `TB_APP_HISTORY.PROCESS_EMP_CODE` / `PROCESS_EMP_NAME` store the actual acting employee; no `PROCESS_AGENT_CODE` / `PROCESS_AGENT_NAME` columns are required. | Yes |
| QA-022 | covers: R5 | happy | AO role `001` selects reason list containing `D99` and provides a 100-character `otherReason`. | POST `epl-case-insert-delreason`. | `OTH_REASON` stores the submitted 100 characters exactly and history is inserted. | Yes |
| QA-022A | covers: R5 | error | AO role `001` selects `D99` with `otherReason` over 100 characters. | POST `epl-case-insert-delreason`. | Request is rejected with `E102` field validation under `REF-D3`; no silent truncation and no DB write. | Yes |
| QA-023 | covers: R5 | error | Delete request omits `applicationNo`. | POST `epl-case-insert-delreason`. | Response is 400 with `MISSING_APPLICATION_NO`; no DB write occurs. | Yes |
| QA-024 | covers: R5 | error | Delete request has no reason. | POST `epl-case-insert-delreason`. | Response is 400 with `MISSING_REASON`; no DB write occurs. | Yes |
| QA-025 | covers: R5 | error | Delete request selects `D99` without `otherReason`. | POST `epl-case-insert-delreason`. | Response is 400 with `MISSING_OTHER_REASON`; no DB write occurs. | Yes |
| QA-026 | covers: R5, R9 | error | AO Manager role `003` calls delete API directly. | POST `epl-case-insert-delreason`. | Response is 403 with `FORBIDDEN_ACTION`; no reason/history/summary write occurs. | Yes |
| QA-027 | covers: R6 | happy | CAD/TLOD role closes case with reason list `['C01']` and `IS_AUTODIS=Y`. | POST `epl-case-insert-cloreason`. | `TB_CLO_REASON.REASON_CODE` stores the DB semicolon form; `TB_APP_HISTORY` inserts; summary sets `CASE_PROGRESS=C1`, clears `CURRENT_USER_ID`, and sets `IS_AUTODIS=YC`. | Yes |
| QA-027A | covers: R6 | happy | Proxy user performs an allowed close action. | POST `epl-case-insert-cloreason`. | `TB_APP_HISTORY.PROCESS_EMP_CODE` / `PROCESS_EMP_NAME` store the actual acting employee; no `PROCESS_AGENT_CODE` / `PROCESS_AGENT_NAME` columns are required. | Yes |
| QA-028 | covers: R6 | happy | CAD/TLOD role closes case with `IS_AUTODIS=M`. | POST `epl-case-insert-cloreason`. | Summary sets `IS_AUTODIS=MC`. | Yes |
| QA-029 | covers: R6 | error | Close request contains malformed `reasonList` items, such as blank item text or item length over 10. | POST `epl-case-insert-cloreason`. | Response carries field-validation code `E102`; semantic absence of reasons is covered separately by `QA-031` / `MISSING_REASON`; no DB write occurs. | Yes |
| QA-030 | covers: R6, R8 | error | Close request omits `applicationNo`. | POST `epl-case-insert-cloreason`. | Response is 400 with `MISSING_APPLICATION_NO`; session context is unchanged. | Yes |
| QA-031 | covers: R6 | error | Close request has no reason. | POST `epl-case-insert-cloreason`. | Response is 400 with `MISSING_REASON`; no DB write occurs. | Yes |
| QA-032 | covers: R6 | error | Close request selects `C99` without `otherReason`. | POST `epl-case-insert-cloreason`. | Response is 400 with `MISSING_OTHER_REASON`; no DB write occurs. | Yes |
| QA-032A | covers: R6 | error | Close request selects `C99` with `otherReason` over 100 characters. | POST `epl-case-insert-cloreason`. | Request is rejected with `E102` field validation under `REF-D3`; no silent truncation and no DB write. | Yes |
| QA-033 | covers: R6, R9 | error | Non-CAD/TLOD role calls close API directly. | POST `epl-case-insert-cloreason`. | Response is 403 with `FORBIDDEN_ACTION`; no close/history/summary write occurs. | Yes |
| QA-034 | covers: R7 | happy | CA role downloads proposal for parameterized `type` values `CS`, `CU`, `IS`, and `IU`, each matching the case `LON_ATTRIBUTE` / `SECURE_ATTRIBUTE`. | POST `epl-file-z0-proposal-download`. | Response returns a one-time `downloadToken` with expiry, no reusable local path; no business DB mutation occurs; audit records print/download action. | Yes |
| QA-034A | covers: R7, R9 | error | CA role requests `type=IS` for a corporate case whose DB attributes resolve to `CS`/`CU`. | POST `epl-file-z0-proposal-download`. | Response is 403 with `FORBIDDEN_ACTION`; no file reference is generated. | Yes |
| QA-035 | covers: R7 | error | Download request omits `applicationNo`. | POST `epl-file-z0-proposal-download`. | Response is 400 with `MISSING_APPLICATION_NO`. | Yes |
| QA-036 | covers: R7, R9 | error | Unauthorized role calls download API. | POST `epl-file-z0-proposal-download`. | Response is 403 with `FORBIDDEN_ACTION`; no `downloadToken` or reusable local path is exposed. | Yes |
| QA-036A | covers: R7 | error | Proposal generation or encryption/tokenization fails after authorization succeeds. | POST `epl-file-z0-proposal-download`. | Response is 500 with `DOWNLOAD_FAILED`; no business DB mutation occurs and audit records failure only. | Yes |
| QA-037 | covers: R8 | happy | User clicks an application from the list while legacy page still needs session. | POST `epl-session-z0-current-application`. | Current session stores `APPLICATION_NO` and optional action; subsequent route can read it. | Yes |
| QA-038 | covers: R8 | happy | User leaves current application context. | DELETE `epl-session-z0-current-application`. | Current application session is cleared. | Yes |
| QA-039 | covers: R8 | error | Session set request omits `applicationNo`. | POST `epl-session-z0-current-application`. | Response is 400 with `MISSING_APPLICATION_NO`; no session is set. | Yes |
| QA-039A | covers: R8, R9 | error | Platform authorization rejects POST set or DELETE clear for `epl-session-z0-current-application`. | POST or DELETE `epl-session-z0-current-application`. | Response is `ACCESS_DENIED` or equivalent; existing session state is unchanged, with no session created or cleared. | Yes |
| QA-040 | covers: R9 | error | `TB_API_AUTH` row exists for `epl-case-insert-delreason`, but its `ROLE` allow-list does not contain role `201`. | POST `epl-case-insert-delreason` with otherwise valid delete request. | Request is rejected with platform auth (`ACCESS_DENIED` or equivalent) before service mutation; `TB_DEL_REASON`, `TB_APP_HISTORY`, and `TB_LON_SUMMARY_INFO` remain unchanged. | Yes |
| QA-041 | covers: R9 | regression | Transaction fails after reason insert but before summary update. | POST `epl-case-insert-delreason` or `epl-case-insert-cloreason`. | Response carries `MSG_UPDATE_FAIL`; reason and history inserts are rolled back; `TB_LON_SUMMARY_INFO` remains unchanged. | Yes |
| QA-042 | covers: R9 | regression | Request contains sensitive reason text and borrower data. | Execute any action while capturing logs. | Logs contain action, masked application number, role, result code, and rollback reason only; no full borrower name or full reason text is logged. | Yes |
