# SRS - EPROC00116 Financial Statement GI

## Metadata
| Field | Value |
|---|---|
| FuncId | EPROC00116 |
| Status | 規格定版: Approved (2026-06-24, owner — axis A–F 獨立確認 Blocker 全清); 實作完成: pending RD (QA 暫拔除) |
| N-axis review | 2026-06-24 source/domain patch + Should-fix closeout PASS. R12 pending items resolved via owner/RD-contract decisions (service-level auth, runtime parity, report ownership, DB/API mapping); QA-021..QA-032 references are dormant per 2026-06-24 QA removal, not verification evidence. Mechanical gate PASS. Axis A–F independent cross-model confirmation 2026-06-24: prior Blockers (E130 semantics, error-enum carry, pxls auth baseline-to-reconcile, provenance disclaimer) confirmed closed, no regression. Owner-approved 2026-06-24 (規格定版); implementation/tests remain RD code-stage DoD. |
| PRD | `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md` |
| Legacy source | `EPROC0_0116`, `EPROC0_0216`, `EPROC00116.jsp`, `EPROC00216.jsp`, GI financial statement DAOs |
| Output files | `spec.md`, `openapi.yaml`, `schema.sql`（`qa-cases.md` 2026-06-24 隨 QA 暫拔除） |

## Scope
- In scope: C0 Financial Statement GI option lookup, current-case query, reference-application copy query, calculation, Save/Finished save, PDF/Excel export contracts, GI statement table writes, checkpoint update, and DB/schema reconcile for EPROC00116.
- Out of scope: FI Financial Statement, Financial Evaluation GI ratio/scoring details, report template redesign, implementation code edits, and approval promotion beyond In Review.
- Current contract uses real RPC endpoints `epl-sele-c0-financial-statement-comments`, `epl-info-c0-financial-statement-comments`, `epl-quer-c0-financial-statement-comments`, `epl-calc-c0-financial-statement-comments`, `epl-save-c0-financial-statement-comments`, `epl-ppdf-c0-financial-statement-comments`, and `epl-pxls-c0-financial-statement-comments`; legacy `inti`, `search`, `calculation`, `save`, `print`, and `printPDF` are source evidence, not public API paths. Owner decision 2026-06-24 for `PENDING-EPROC00116-LEGACY-ACTION-AND-MESSAGE-COMPAT`: do not expose a legacy action adapter; keep legacy typo/message keys only as compatibility aliases or provenance where already returned/mapped.

## Source Evidence
| Area | Evidence | SRS disposition |
|---|---|---|
| PRD | Scope and legacy programs at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:9-23`; PRD TBDs at `:55-62`; FRs at `:175-185`; data/error rules at `:628-764`; acceptance cases at `:785-817`. | FRs map to R1-R11; PRD TBDs are closed by owner/RD-contract decisions under R12. |
| Dispatch | Real endpoint style and four-file bundle rules at `docs/build-tasks/prd-to-srs-codex-dispatch.md`. | Use only `epl-*` RPC endpoints and produce exactly four files. |
| Legacy actions | `EPROC0_0116.java` exposes AJAX actions `inti`, `search`, `calculation`, `print`, `printPDF`, and `save` at `legacy-epro/JavaSource/com/cathaybk/epro/c0/trx/EPROC0_0116.java:50`, `:96`, `:133`, `:171`, `:205`, `:233`; `EPROC0_0216.java` mirrors them at the same line ranges. | Legacy names establish behavior and error-key provenance; current SRS uses `epl-*` endpoints and no legacy action adapter. |
| Legacy module | Option/query/reference/calc/save/export methods at `EPROC0_0116_mod.java:302`, `:327`, `:379`, `:426`, `:449`, `:567`, `:625`; reference eligibility at `:382-387`; transaction and delete/insert sequence at `:497-552`; formulas at `:61-68`, `:96-104`, `:161-162`, `:903-923`, `:945-970`, `:1005-1059`; highlight split at `:800-805`. | Formula and transaction behavior carried; reference eligibility and highlight migration are closed by R12 decisions. |
| Legacy JS | Five `BALANCE_DATE_0..4` validation fields at `EPROC00116_JS.jsp:213-217`; FI-looking validation key at `:270-272`; payload skips blank year rows at `:612-617`; old highlight pipes at `:711-721`; PDF/calc/save event flow at `:846-917` in `EPROC00216_JS.jsp`. | Validation and delimiter risks are closed by R12 decisions; payload shape informs API arrays, with implementation/test DoD where stated. |
| Current backend | Controller POST endpoints at `backend/src/main/java/khd/svc/epro/controller/corporate/CsuFinancialStatementController.java:34`, `:47`, `:60`, `:73`, `:86`, `:99`, `:112`; service option/query/save/export details at `CsuFinancialStatementServiceImpl.java:83-133`, `:156-182`, `:270-340`, `:352-466`. | Current backend is API grounding. |
| Current frontend | GI API service calls select/info/query/save/calc endpoints at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/services/api.service.ts:37-100`; amount changes set `needCalculate=true` and hide print at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/financial-statement-gi.component.ts:316-329`; Finished blocks while `needCalculate` is true at `:692-715`; build task confirms ppdf/pxls POST blob endpoints at `docs/build-tasks/done/phase-f-step2-00116-financial-statement-gi.md:13-27`. | FE uses backend calculation and export APIs; BR-006 is carried as R11; report template ownership is closed by R12, with shared-template implementation/test DoD where stated. |
| Refactor spec | Module index lists seven BE artifacts and FE artifact at `docs/refactor-spec/02_modules/EPROC00116.md:22-29`; FE artifact lists API calls at `docs/refactor-spec/03_artifacts/fe-corporate/EPROC00116/eproc00116-financial-statement-and-comments-gi.md:77-83`; calc/select artifacts define POST and body fields at `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-calc-c0-financial-statement-comments.md:145-159` and `epl-sele-c0-financial-statement-comments.md:184-188`; reference-query FE handling uses API `epl-quer-c0-financial-statement-comments` and code `E116` at `docs/refactor-spec/03_artifacts/fe-corporate/EPROC00116/eproc00116-financial-statement-and-comments-gi.md:200-204`. | Refactor contracts quantify current DTO direction and support the `epl-*`-only public API; stale source conflicts are closed by R12 decisions, with implementation/test DoD where stated. |
| DB diff and latest reverify | db-diff marks four GI tables active/exact at `docs/db-diff/02_tables/TB_FIN_STATEMENT_MAIN.md:13-16`, `TB_FIN_STATEMENT_BALANCE_GI.md:13-16`, `TB_FIN_STATEMENT_INCOME_GI.md:13-16`, `TB_FIN_STATEMENT_CASHFLOW_GI.md:13-16`; latest reverify confirms current main/detail columns and PKs at `C:/Users/00596357/Documents/project/kh/epro/epro-db/out/legacy_schema_reverify_new02_columns.tsv:1223-1273`, `:1287-1299`, `:1330-1393` and PKs at `..._pk.tsv:151-160`; checkpoint columns at `..._columns.tsv:128`, `:144`. | `schema.sql` follows latest reverify/current entity, with stale-source deltas called out. |
| Auth and inventory | c0 auth findings list seven endpoints at `docs/build-tasks/c0-authz-sql-findings.md:153-159`, with pxls gap at `:192` and calc note at `:196`; inventory marks EPROC00116 present and asks export-template confirmation at `docs/feature-inventory.md:103`; matrix leaves parity gated at `docs/build-tasks/refactor-audit/per-page-reinventory-matrix.md:38`; parity task says calc/comments need source recheck at `docs/build-tasks/c0-legacy-parity-recheck.md:27`. | API seed, service auth, and parity risks are closed as R12 contracts with RD/Security/DBA code-stage DoD where applicable; this bundle does not close approval. |

Local evidence disclaimer: latest reverify/db baseline paths under the sibling mother folder are treated as source baseline for this SRS, but this planning repo cannot independently reproduce every mother-folder artifact in isolation. Before owner approval, RD/DBA must reconcile the mother-folder baseline with repo-visible db-diff/refactor/auth evidence.

## Endpoints
| Endpoint | Method | Purpose | Evidence |
|---|---|---|---|
| `/epl-sele-c0-financial-statement-comments` | POST | Return currency, currency unit, and type-of-year options. | Controller `:34`; service `:83-100`; refactor select artifact. |
| `/epl-info-c0-financial-statement-comments` | POST | Query current case summary, main, Balance GI, Income GI, Cashflow GI, and `haveData`. | Controller `:60`; service `:156-205`. |
| `/epl-quer-c0-financial-statement-comments` | POST | Query a reference application and return copyable GI financial statement data. | Controller `:47`; service `:111-145`; legacy `search` at `EPROC0_0116_mod.java:379-408`. |
| `/epl-calc-c0-financial-statement-comments` | POST | Calculate derived Balance/Income/Cashflow values and validation messages. | Controller `:73`; service `:208-260`; legacy formulas `EPROC0_0116_mod.java:903-1059`. |
| `/epl-save-c0-financial-statement-comments` | POST | Save draft or Finished GI statement rows and update checkpoint in one transaction. | Controller `:86`; service `:270-340`; legacy transaction `EPROC0_0116_mod.java:497-552`. |
| `/epl-ppdf-c0-financial-statement-comments` | POST | Generate GI PDF report blob/path using current platform download response. | Controller `:99`; service `:352-390`; build task `:19`, `:24-27`. |
| `/epl-pxls-c0-financial-statement-comments` | POST | Generate GI Excel file blob. | Controller `:112`; service `:395-466`; build task `:20`, `:24-27`. |

## Rules

### R1 Load options and current-case data - 強制點: both - covers-prd: FR-001
The page must call select and info endpoints when loading EPROC00116. Select requires `applicationNo` and `langType` and returns option lists for `CCY`, `CCY_UNIT`, and `TYPE_OF_YEAR`. Info requires `applicationNo` and `isQuery`; it must query loan summary, `TB_FIN_STATEMENT_MAIN`, `TB_FIN_STATEMENT_BALANCE_GI`, `TB_FIN_STATEMENT_INCOME_GI`, and `TB_FIN_STATEMENT_CASHFLOW_GI`, then return the current-case maps plus `haveData`.

Owner decision 2026-06-24 for `PENDING-EPROC00116-HAVEDATA-CASHFLOW-PARITY`: `haveData=Y` MUST require the main record plus all three GI detail groups needed for print/export: Balance, Income, and Cashflow. If any of the three detail groups is missing, including Cashflow-only missing, info MUST return `haveData=N` and FE MUST keep Print/Excel/PDF actions hidden. Current backend sets `haveData` from main, Balance, and Income only, and omits Cashflow from that flag; this is a code-stage implementation gap. RD must include `sql5Entities` in the info `haveData` predicate and add regression tests before code approval. If the current case is not query mode and the loan summary/application date is missing, the API returns business error `E126` / `FAILED_E126`.

Evidence: PRD FR-001 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:203-219`, AC at `:226-227`, and BR-004 at `:727`; legacy query sets `haveData` from Balance, Income, and Cashflow at `legacy-epro/JavaSource/com/cathaybk/epro/c0/module/EPROC0_0116_mod.java:349-353`; refactor info contract requires SQL2/SQL3/SQL4/SQL5 at `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-info-c0-financial-statement-comments.md:160-162`; current service option/info code at `CsuFinancialStatementServiceImpl.java:83-100`, `:156-205`, with the current `haveData` gap at `:181`, `:185-186`; current export requires SQL5 at `CsuFinancialStatementServiceImpl.java:419-422`; latest DB shape at reverify columns `:1223-1393`.

### R2 Query reference application without mutating current case - 強制點: both - covers-prd: FR-002
The reference query endpoint must accept `reApplicationNo`, validate the reference case through the current repository rule, and return main, Balance GI, Income GI, and Cashflow GI maps only when all source data is present. Owner decision 2026-06-24 for `PENDING-EPROC00116-REFERENCE-STATUS-CODES`: the official migrated eligibility rule is the current repository/application-date rule, which requires an eligible `TB_LON_SUMMARY_INFO` row with `CASE_PROGRESS in ('08','09','14','20','21','22','23','24','25','26','27','C1')` and non-null `APP_CON_TYPE`, then requires main plus all three GI detail groups. Missing loan-date/status or missing any financial statement group returns `FAILED_E116` and must not write to current-case tables or checkpoint.

Legacy allowed reference copy only for specific status/contract conditions and used `COMMOM_MSG_NO_DATA`; those source names remain provenance only for this decision. Error-key compatibility is resolved separately under `PENDING-EPROC00116-LEGACY-ACTION-AND-MESSAGE-COMPAT` in R10/R12.

Evidence: PRD FR-002 and AC at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:176`, `:802-804`; legacy reference status check at `EPROC0_0116_mod.java:382-387`; current service reference query and not-found behavior at `CsuFinancialStatementServiceImpl.java:111-145`.

### R3 Maintain main record and five financial-highlight text areas - 強制點: both - covers-prd: FR-003
Main data must carry `applicationNo`, `currency`, `currencyUnit`, company/audit fields, three criteria flags, audited-FS answers, and five highlight text areas: `loanRepaymentHT`, `financialSituationHT`, `businessRiskHT`, `borrowerRiskHT`, and `summaryHT`. Current API fields allow each highlight section up to 6000 characters; persistence splits each section into `_1` and `_2` columns of 3000 characters each.

On Finished, `currency`, `currencyUnit`, company name, audited-FS answers, and the five highlight text areas are required by current service validation. Owner decision 2026-06-24 for `PENDING-EPROC00116-HIGHLIGHT-MIGRATION`: the migrated public contract is the five structured API fields only; legacy pipe-delimited `HIGHLIGHT` is not a public API field. Persistence source of truth is the five split column pairs (`LOAN_REPAYMENT_1/2`, `FINANCIAL_SITUATION_1/2`, `BUSINESS_RISK_1/2`, `BORROWER_RISK_1/2`, `SUMMARY_1/2`). `HIGHLIGHT_CLOB` is reserved for storing the raw legacy `HIGHLIGHT` string as migration provenance when such a raw value exists, and it is not the primary read source for current save/query.

Legacy migration from one raw `HIGHLIGHT` value MUST split by the first four literal pipe (`|`) delimiters into the five API fields in order: loan repayment, financial situation, business risk, borrower risk, and summary. Missing segments map to empty values. Records with fewer than four delimiters, more than four delimiters, or any segment exceeding 6000 characters MUST be flagged in the migration/test report for RD/DBA follow-up; because legacy provided no delimiter escaping, ambiguous embedded pipes cannot be losslessly inferred without preserving the raw value in `HIGHLIGHT_CLOB`. Implementation and migration tests are code-stage DoD, pending RD.

Currency formatting carries PRD BR-016: when `mainMap.currency = KHR`, FE amount inputs/display for Balance, Income, and Cashflow must use zero fraction digits; USD keeps decimal formatting. This is presentation/input formatting only and does not change DB numeric precision.

Evidence: PRD FR-003/data mapping at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:177`, `:628-635`; PRD highlight TBD/sections at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:61`, `:282`, `:391-410`, `:635`; PRD BR-016/TC-008 at `:739`, `:807`; legacy textarea and pipe concatenation at `legacy-epro/WebContent/html/cathaybk/system/epro/c0/EPROC0_0100/EPROC00116_JS.jsp:574-586`, `:711-723`; DTO lengths at `SaveCsuFinancialStatementCommentsRequest.java:19-92`; entity split/CLOB columns at `TBFinStatementMainEntity.java:20-72`; split/reassemble behavior at `CsuFinancialStatementServiceImpl.java:1194-1198`, `:2106-2151`, `:2345-2349`; current FE currency formatting state at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/financial-statement-gi.component.ts:143`, `:153`, `:246`, `:459-461`, and shared numeric fraction digits at `frontend/src/app/core/components/search-item/sub-components/input-number/input-number.component.html:13-14`.

### R4 Calculate and persist Balance Sheet GI rows - 強制點: BE - covers-prd: FR-004, FR-007
Balance rows are ordered by `DATA_SEQ`, use `BALANCE_DATE`, `PERIODS`, and `TYPE_OF_YEAR`, and persist the active GI columns in `TB_FIN_STATEMENT_BALANCE_GI`. Calculation must derive at least:

- `TOTAL_CUR_ASSETS` from current-asset fields.
- `NET_FIXED_ASSETS = LAND + BUILDING + EQUIPMENT + OTH_FIXED_ASSETS - DEPRECIATION_AMORTIZATION`.
- `NON_CUR_ASSETS` from fixed/non-current asset groups.
- `CUR_LIABILITIES`, `NON_CUR_LIABILITIES`, and `TOTAL_EQUITY` from their component fields.
- `TOTAL_ASSETS = TOTAL_CUR_ASSETS + NON_CUR_ASSETS`.
- `TOTAL_LIAB_EQUITY = CUR_LIABILITIES + NON_CUR_LIABILITIES + TOTAL_EQUITY`.
- `DIFFERENCE = TOTAL_ASSETS - TOTAL_LIAB_EQUITY`.

Physical DB/source typo fields MUST remain mapped as physical names in persistence: `AP_RELATED_PARITIES` and `SHARDHOLDERS_LOAN`. Public API/FE DTO names MUST use canonical business names such as `apRelatedParties` and `shareholdersLoan`; BE mapping is responsible for translating between the canonical JSON names and physical DB typo columns.

Finished state must reject rows where required Balance fields are blank. Calculation/result messages must include the unbalanced-sheet condition when first-year totals are zero or `DIFFERENCE != 0`.

Evidence: PRD formulas at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:853-861`; PRD typo/mapping TBD and formulas at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:60`, `:311-312`; legacy formulas at `EPROC0_0116_mod.java:61-68`, `:903-923`; db-diff typo columns at `docs/db-diff/02_tables/TB_FIN_STATEMENT_BALANCE_GI.md:69`, `:80`; current entity/DTO bridge at `TBFinStatementBalanceGiEntity.java:117-151`, `SaveCsuFinancialStatementCommentsRequest.java:250-307`; current FE canonical names at `frontend/src/app/core/models/pages/case-edition/corporate/credit-investigation/financial-statement/gi/data.model.ts:157-168`; current calculation and result messages at `CsuFinancialStatementServiceImpl.java:716-918`; latest reverify Balance columns at `legacy_schema_reverify_new02_columns.tsv:1223-1273`.

### R5 Calculate and persist Income Statement GI rows - 強制點: BE - covers-prd: FR-005, FR-007
Income rows are ordered by `DATA_SEQ`, use the physical date column `IMCOME_DATE`, and persist the active GI columns in `TB_FIN_STATEMENT_INCOME_GI`. Calculation must derive:

- `GROSS_PROFIT = REVENUES - COST_OF_GOOD_SOLD`.
- `OPERATING_EXPENSES` from salary, commission, marketing, transportation, and utilities.
- `OPERATING_PROFIT = GROSS_PROFIT - OPERATING_EXPENSES`.
- `NON_OPERATING_INCOME` and `NON_OPERATING_EXPENSES` from their component fields.
- `PROFIT_BEFORE_TAX = OPERATING_PROFIT + NON_OPERATING_INCOME - NON_OPERATING_EXPENSES`.
- `ATTRIBUTED_TO_PARENT_COMP = CUR_PERIOD_PROFIT - MINORITY_INTERESTS`.

Physical DB/source typo fields MUST remain mapped as physical names in persistence: `IMCOME_DATE` and `IMPAIMENT_LOSS`. Public API/FE DTO names MUST use canonical business names such as `year`/`incomeDate` semantics and `impairmentLoss`; BE mapping is responsible for translating between the canonical JSON names and physical DB typo columns. This closes `PENDING-EPROC00116-FIELD-TYPO-MAPPING` as a spec-side RD/DBA contract; implementation and automated mapping tests are code-stage DoD, pending RD.

Evidence: PRD FR-005/formulas at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:179`, `:853-861`; PRD typo/mapping TBD and formulas at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:60`, `:344`, `:659`, `:666`; legacy income formulas at `EPROC0_0116_mod.java:96-104`, `:945-970`; db-diff typo columns at `docs/db-diff/02_tables/TB_FIN_STATEMENT_INCOME_GI.md:40`, `:70`; current entity/DTO bridge at `TBFinStatementIncomeGiEntity.java:26-117`, `SaveCsuFinancialStatementCommentsRequest.java:505-507`; current FE canonical name at `frontend/src/app/core/models/pages/case-edition/corporate/credit-investigation/financial-statement/gi/data.model.ts:327`, `:425`; latest reverify Income columns at `legacy_schema_reverify_new02_columns.tsv:1330-1371`.

### R6 Calculate and persist Cashflow Statement GI rows - 強制點: BE - covers-prd: FR-006, FR-007
Cashflow rows are ordered by `DATA_SEQ`, use `CASHFLOW_DATE`, `PERIOD`, and `TYPE_OF_YEAR`, and persist the active GI columns in `TB_FIN_STATEMENT_CASHFLOW_GI`. Calculation must derive:

- For year N > 1, `OPENING_BALANCE(N)` from previous year `END_BALANCE`.
- `END_BALANCE = NET_I_D_IN_CASH + OPENING_BALANCE`.
- `OPERATING_NET_CASHFLOW = NET_I_D_IN_CASH - FINANCING_NET_CASHFLOW - INVESTING_NET_CASHFLOW` where applicable.

Finished state must reject rows where required Cashflow fields are blank. Calculation/result messages must include the cash-equivalent mismatch condition when `END_BALANCE` differs from Balance `CASH_EQUIVALENT`.

Evidence: PRD FR-006/formulas at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:180`, `:853-861`; legacy cashflow formulas at `EPROC0_0116_mod.java:161-162`, `:1005-1059`; current check at `CsuFinancialStatementServiceImpl.java:920-953`; latest reverify Cashflow columns at `legacy_schema_reverify_new02_columns.tsv:1287-1299`.

### R7 Save draft/Finished as full replacement in one transaction - 強制點: FE; BE hardening pending - covers-prd: FR-008
Save and Finished use the same save endpoint. The backend must validate `applicationNo` and `isFinish`, load loan summary, validate fields according to draft vs Finished mode, save the main record, delete existing Balance/Income/Cashflow rows for the `applicationNo`, insert the submitted rows in request order, and update the checkpoint in one transaction.

Owner decision 2026-06-24 for `PENDING-EPROC00116-FIVE-YEAR-REQUIREDNESS`: Finished requires at least one submitted annual GI row and allows up to five annual rows. Empty annual columns are not submitted and are not persisted; validation applies to every submitted Balance/Income/Cashflow row, but the contract MUST NOT require all five years to be filled. Because save is delete-and-rebuild, reducing the submitted year count intentionally removes previously persisted years that are omitted from the payload.

Owner decision 2026-06-24 for `PENDING-EPROC00116-SAVE-LIST-BE-VALIDATION`: Save and Finished requests MUST include a non-null `mainMap` and non-null `balanceMap`, `incomeMap`, and `cashflowMap` arrays. The three detail arrays MUST be non-empty, contain no null elements, have the same length, and contain one to five submitted annual rows. These shape checks run before any main/detail delete, insert/update, or checkpoint write. Missing maps, null lists, empty lists, null row elements, length mismatch, or more than five rows MUST return controlled validation/business errors and leave existing DB rows/checkpoint unchanged. Implementation and automated negative tests are code-stage DoD, pending RD.

Current checkpoint polarity is `isFinish=false -> EPROC00116 = "Y"` and `isFinish=true -> EPROC00116 = "N"` in the new `TB_CHECK_POINTS_CS` or `TB_CHECK_POINTS_CU` table. Any failure after delete must roll back the main/detail/checkpoint writes. Save is not safe for automatic client retry.

Owner decision 2026-06-24 for `PENDING-EPROC00116-CALC-CHECKPOINT-SIDE-EFFECT`: Calculation is a read-only business operation. `epl-calc-c0-financial-statement-comments` MUST return only derived Balance/Income/Cashflow maps and result messages, and MUST NOT update `TB_CHECK_POINTS_CS`, `TB_CHECK_POINTS_CU`, main, Balance, Income, or Cashflow tables. The current backend calc checkpoint write is a code-stage implementation gap; RD must remove it or otherwise make calc no-mutation and add regression tests before code approval.

Finished may only proceed after successful Calculation and a balanced result: Balance `DIFFERENCE` must be zero and Cashflow `END_BALANCE` must equal Balance `CASH_EQUIVALENT`. Current FE blocks Finished while recalculation is required and displays the calculation-required warning, but current backend save evidence does not prove save-time recomputation or revalidation of `DIFFERENCE` / `END_BALANCE` vs `CASH_EQUIVALENT`.

Owner decision 2026-06-24 for `PENDING-EPROC00116-PARITY-CODE-VERIFY`: final SRS disposition is `FIX`, not rebuild. Keep the current EPROC00116 FE/BE API structure, but to-be BE save MUST harden `isFinish=true` by recomputing or verifying the submitted/calculated Balance `DIFFERENCE` and Cashflow `END_BALANCE` against Balance `CASH_EQUIVALENT` before any completed checkpoint or main/detail mutation. Direct imbalanced Finished payloads MUST return controlled validation/business errors and leave existing main, Balance, Income, Cashflow, and checkpoint rows unchanged. Implementation and automated negative tests are code-stage DoD, pending RD.

Evidence: PRD transaction/idempotency at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:621-624`; PRD calculation vs save/checkpoint split at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:136-137`, `:181-182`; PRD save request-map/detail rules at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:425-430`, `:449-450`, `:590-594`; PRD AC-006/AC-007 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:790-791`; PRD TBD-005 and boundary TC at `:59`, `:806`; PRD "up to five years" and "entered years" wording at `:93`, `:298`, `:331`, `:362`, `:730-731`; legacy calc returns `reList` without checkpoint writes at `legacy-epro/JavaSource/com/cathaybk/epro/c0/module/EPROC0_0116_mod.java:426-439`; legacy first-year/format validation and payload skip at `legacy-epro/WebContent/html/cathaybk/system/epro/c0/EPROC0_0100/EPROC00116_JS.jsp:213-217`, `:613-617`; legacy save input and loop use submitted `balancePar.size()` and same-index detail arrays at `legacy-epro/JavaSource/com/cathaybk/epro/c0/module/EPROC0_0116_mod.java:449-482`, `:504-511`; legacy save transaction/checkpoint at `:497-547`; refactor calc/save split at `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-calc-c0-financial-statement-comments.md:9-11` and save SQL11/SQL12 checkpoint update at `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-save-c0-financial-statement-comments.md:121`; current DTO leaves `mainMap` and detail lists as `@Valid` only and `init()` converts null to empty object/list at `SaveCsuFinancialStatementCommentsRequest.java:30-47`, `:611-623`; current FE finish schema validates row arrays without fixed length at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/config/validate-rule.ts:65-69`; current calc result check and current calc checkpoint side effect at `CsuFinancialStatementServiceImpl.java:223-257`, `:920-953`; current save path calls `validateFields` and then writes rows/checkpoint at `CsuFinancialStatementServiceImpl.java:271-340`, with required-field validation at `:1358-1397` and no save-time `checkResult`; current FE Finished block at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/financial-statement-gi.component.ts:692-715`; latest checkpoint columns at reverify `:128`, `:144`.

### R8 Export Excel and PDF only from complete persisted data - 強制點: both - covers-prd: FR-009, FR-010
PDF and Excel export endpoints must require `applicationNo`, load loan summary, main record, and all three GI detail groups from persisted data, and fail with `FAILED_E116` or platform-equivalent download error when any required group is missing. Export must be POST and must not rely on unsaved UI state. Successful Excel uses a binary download response; successful PDF uses the platform PDF/download response.

Export visibility MUST align with R1 `haveData`: Print/Excel/PDF actions may be shown from persisted current-case state only when main, Balance, Income, and Cashflow are all present. Missing Cashflow is incomplete persisted data, not a printable state.

Owner decision 2026-06-24 for `PENDING-EPROC00116-REPORT-TEMPLATE-OWNERSHIP`: c0 public API and output ownership remain `EPROC00116` through `epl-ppdf-c0-financial-statement-comments` and `epl-pxls-c0-financial-statement-comments`. Current i0/`EPROI00116` template paths and names are approved as shared report assets for this bundle; this SRS does not require a c0-specific template rename. QA must verify exported content comes from c0 persisted GI data and that no i0 public API route is exposed. Future report-service R2/template redesign may rename assets without changing this API contract.

Evidence: PRD FR-009/010 and export AC at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:183-184`, `:813-814`; PRD TBD-008 and template-maintenance scope exclusion at `:62`, `:105`; refactor c0 report artifacts at `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-ppdf-c0-financial-statement-comments.md:9-10`, `:141`, `docs/refactor-spec/03_artifacts/be-corporate/EPROC00116/epl-pxls-c0-financial-statement-comments.md:9-10`, `:141`, and FE report API calls at `docs/refactor-spec/03_artifacts/fe-corporate/EPROC00116/eproc00116-financial-statement-and-comments-gi.md:82-83`, `:159-160`; current PDF/Excel logic and template paths at `CsuFinancialStatementServiceImpl.java:352-390`, `:395-466`; build task export constraints/template reuse note at `docs/build-tasks/done/phase-f-step2-00116-financial-statement-gi.md:23-27`.

### R9 Preserve GI downstream contract and checkpoint routing - 強制點: both - covers-prd: FR-011
EPROC00116 must preserve persisted GI data for downstream Financial Evaluation GI. The active schema route is unified to `TB_CHECK_POINTS_CS.EPROC00116` for `LON_ATTRIBUTE + SECURE_ATTRIBUTE = "CS"` and `TB_CHECK_POINTS_CU.EPROC00116` otherwise. The legacy `EPROC0_0117`/`EPROC0_0217` next-page checkpoint behavior is source evidence; the new system must not alter Balance/Income/Cashflow row order, `DATA_SEQ`, or physical column names without an owner decision and migration plan.

Owner decision 2026-06-24 for `PENDING-EPROC00116-PARITY-CODE-VERIFY`: the final disposition is `FIX`, not rebuild. The broader c0 parity task remains historical context but no longer blocks this SRS disposition. RD code-stage DoD tracks BE save-time Finished hardening in R7/QA-030 while preserving the downstream GI schema, row order, `DATA_SEQ`, and checkpoint routing contracts here.

Evidence: PRD downstream and checkpoint rules at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:155-160`, `:185`, `:724-738`; current checkpoint route at `CsuFinancialStatementServiceImpl.java:331-340`; matrix/parity evidence at `per-page-reinventory-matrix.md:38`, `c0-legacy-parity-recheck.md:27`.

### R10 Enforce validation, authorization, safe errors, and safe logging - 強制點: both - covers-prd: FR-001, FR-002, FR-007, FR-008, FR-009, FR-010
All seven endpoints require platform authorization using their exact `TB_API_AUTH.API_ID` rows. Owner ratified 2026-06-25 (was RD-proposed) for `PENDING-EPROC00116-PXLS-AUTHZ-SEED`: `epl-pxls-c0-financial-statement-comments` MUST have a `TB_API_AUTH` row before code-stage approval. The target row must use `API_ID=epl-pxls-c0-financial-statement-comments` and `REF_FUNCTION_ID=EPROC00116`. Because the current repo-visible DB precheck has no exact `epl-pxls-i0-financial-statement-comments` source row, the reviewed equivalent for this RD cycle is the existing c0 `epl-ppdf-c0-financial-statement-comments` row; copy its `ROLE` to the pxls target row. RD/DBA code-stage DoD requires applying the seed and rerunning DB/API authorization checks before `rd-done`.

Owner decision 2026-06-24 for `PENDING-EPROC00116-SERVICE-AUTHZ`: `TB_API_AUTH` endpoint authorization is necessary but not sufficient. Backend service logic MUST also enforce case/page authorization for the requested `applicationNo` and EPROC00116 page before business execution. Info, reference query, PDF, and Excel endpoints require backend case/page read access; calculation, draft save, and Finished require backend case/page edit access. Save/Finished authorization MUST run before field validation, main/detail delete or insert/update, and checkpoint writes. Export authorization MUST run before loading/generating/download-returning report files. Unauthorized direct API calls MUST return platform authorization/access-denied errors and leave main, Balance, Income, Cashflow, checkpoint, and generated report artifacts unchanged. The current FE pageAuth/button filtering remains UI control only; implementing the backend guard and automated negative tests is code-stage DoD, pending RD/Security.

Request validation must produce controlled EPRO/API errors for blank `applicationNo`, blank `isFinish`, missing calculation lists, invalid numeric/date fields, missing Finished fields, no-data, save failure, calculation failure, and download/export failure. `E130` is retained as the malformed request-validation code for invalid save/calc-style payloads and must be returned only from those validation branches, not from successful business processing.

The contract must recognize current service errors and compatibility aliases: `MSG_DATA_NOT_FOUND`, `MSG_OVER_COUNT_LIMIT`, `MSG_QUERY_FAIL`, `MSG_ERROR_APPLICATION_NO`, `E130`, `COMMON_MSG_TOTAL_FAIL`, `COMMON_MSG_ERROR_LON`, `COMMON_MSG_PRINT_FAIL`, `COMMON_MSG_DOWNLOAD_SUCCESS`, `COMMON_MSG_DOWNLOAD_FAIL`, `COMMON_MSG_SAVE_SUCCESS`, `COMMON_MSG_SAVE_FAIL`, `MSG_EXPORT_SUCCESS`, `COMMOM_MSG_NO_DATA`, `EPROI0_0160_MSG_ERRO_CURRENCY`, `EPROI00119_MSG_VALIDATE_004`, `E116` / `FAILED_E116`, and `E126` / `FAILED_E126`. Current `ReturnEnum` business errors use the EPRO `code/message/data` envelope with HTTP 200; HTTP 401 is reserved for authorization errors such as `E405`/`E498`. Logs must not include full financial statement payloads or sensitive personal data.

Owner decision 2026-06-24 for `PENDING-EPROC00116-LEGACY-ACTION-AND-MESSAGE-COMPAT`: the public routing contract MUST expose only the `epl-*` endpoints in this SRS and MUST NOT require adapter routes or action inputs named `inti`, `search`, `calculation`, `save`, `print`, or `printPDF`. Legacy typo/message keys such as `COMMOM_MSG_NO_DATA` and `EPROI00119_MSG_VALIDATE_004` remain allowed only as compatibility aliases or mapping provenance; the canonical migrated reference-query no-data error remains `E116` / `FAILED_E116`.

Evidence: PRD TBD-002/003/004 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:56-58`; PRD page permission/query-mode, security, and audit requirements at `:166-168`, `:718`, `:754-774`; legacy action names at `EPROC0_0116.java:50`, `:96`, `:133`, `:171`, `:205`, `:233`; legacy typo/error keys at `EPROC0_0116_mod.java:386-409` and `EPROC00116_JS.jsp:270-272`; refactor `epl-*` artifacts at `docs/refactor-spec/02_modules/EPROC00116.md:22-28` and reference-query `E116` handling at `docs/refactor-spec/03_artifacts/fe-corporate/EPROC00116/eproc00116-financial-statement-and-comments-gi.md:200-204`; current DTO validation at `SaveCsuFinancialStatementCommentsRequest.java:19-47` and `CalcCsuFinancialStatementCommentsRequest.java:13-27`; auth findings define `TB_API_AUTH` endpoint access and runtime matching at `docs/build-tasks/c0-authz-sql-findings.md:19-22`, with EPROC00116 endpoint evidence at `:153-159` and repo-visible contrary evidence at `:191-192`; the 2026-06-24 mother-folder SELECT-only DB recheck is a baseline-to-reconcile input until committed into repo-visible evidence; current controller dispatch evidence at `CsuFinancialStatementController.java:73-88`, `:99-114`; current save path writes and checkpoint update at `CsuFinancialStatementServiceImpl.java:271-340`; current FE pageAuth/button/form filtering at `financial-statement-gi.component.ts:278-285`, `:634-688`.

### R11 Recalculate after amount edits and hide export actions - 強制點: FE - covers-prd: FR-007, FR-009, FR-010
After a successful Calculation, any user edit to Balance, Income, or Cashflow amount fields must mark the page as needing Calculation again, show/enable the Calculation action for editable users, hide Print/Excel and PDF actions, and block Finished until Calculation is rerun successfully. Reference-application copy also counts as data replacement and must follow the same "needs Calculation / hide Print/PDF" behavior before Save/Finished.

Print/Excel and PDF may only be visible again when the page has complete persisted data per R8 and the current unsaved UI state is not marked as needing recalculation. This carries PRD BR-006 and AC-005 instead of leaving them as pending.

The same FE state must preserve BR-016 currency formatting: changing currency calls `setNumberIsDecimal`, KHR amount fields show/input zero decimals, and USD fields show/input decimal formatting.

Evidence: PRD BR-006 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:729`; PRD AC-005 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:789`; PRD BR-016/TC-008 at `:739`, `:807`; reference-copy hide/recalculate behavior at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:248`; current GI FE `valueChanges` handler sets `needCalculate=true` and `canShowPrintBtn=false` at `frontend/src/app/pages/case-edition/sub-pages/corporate/credit-investigation/components/financial-statement/financial-statement-gi/financial-statement-gi.component.ts:316-329`; Calculation clears `needCalculate` at `:584-608`; Finished blocks with `E_CHECK_CALCULATE` while `needCalculate` is true at `:692-715`; current FE currency formatting state at `:143`, `:153`, `:246`, `:459-461`, and shared numeric fraction digits at `frontend/src/app/core/components/search-item/sub-components/input-number/input-number.component.html:13-14`.

### R12 Closed decision register PENDING-EPROC00116-OPEN-DECISIONS - 強制點: both - covers-prd: FR-001, FR-002, FR-003, FR-004, FR-005, FR-006, FR-007, FR-008, FR-009, FR-010, FR-011
The following items were open owner/RD/Security/DBA decisions and are now closed in this bundle. Implementation and automated tests remain code-stage DoD where explicitly stated.

| ID | Owner | Impact | Evidence | Status |
|---|---|---|---|---|
| PENDING-EPROC00116-REFERENCE-STATUS-CODES | PM / SA | Reference application copy can accept or reject the wrong cases. | PRD TBD-001 at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROC00116-v1.0.md:55`; PRD source behavior at `:244`-`:247`; legacy status/contract check at `legacy-epro/JavaSource/com/cathaybk/epro/c0/module/EPROC0_0116_mod.java:182`, `:386`-`:387` and `legacy-epro/JavaSource/com/cathaybk/epro/c0/module/EPROC0_0216_mod.java:182`, `:388`-`:389`; current repository condition at `backend/src/main/java/khd/svc/epro/repository/TBLonSummaryInfoRepository.java:971`-`:974`; current reference query/detail completeness path at `backend/src/main/java/khd/svc/epro/service/corporate/impl/CsuFinancialStatementServiceImpl.java:115`-`:133`. | Closed: `REFERENCE_COPY_CURRENT_REPOSITORY_ELIGIBILITY`. Use the current repository/application-date eligibility rule plus main/Balance/Income/Cashflow GI completeness; source status names remain provenance, not an open business-name TBD. |
| PENDING-EPROC00116-LEGACY-ACTION-AND-MESSAGE-COMPAT | SA / RD | Compatibility tests may require legacy typo names/keys or new canonical mapping. | PRD TBD-002/003/004 at `:56-58`; legacy `inti` at `EPROC0_0116.java:50`; legacy `COMMOM_MSG_NO_DATA` at `EPROC0_0116_mod.java:386-387`; JS key `EPROI00119_MSG_VALIDATE_004` at `EPROC00116_JS.jsp:270-272`; refactor `epl-*` artifacts at `docs/refactor-spec/02_modules/EPROC00116.md:22-28`. | Closed: `LEGACY_ACTIONS_NO_ADAPTER_MESSAGE_ALIAS_ONLY`. Public API remains `epl-*` only; typo/message keys are compatibility aliases/provenance, with canonical migrated reference no-data as `E116` / `FAILED_E116`. |
| PENDING-EPROC00116-FIVE-YEAR-REQUIREDNESS | PM / SA / QA | Finished validation can diverge between full five-year requirement and at-least-one-year behavior. | PRD TBD-005 at `:59`; JS validates `BALANCE_DATE_0..4` at `EPROC00116_JS.jsp:213-217`; payload skips blank dates at `:612-617`; current save DTO initializes missing arrays at `SaveCsuFinancialStatementCommentsRequest.java:612-623`. | Closed: `FINISHED_AT_LEAST_ONE_YEAR_MAX_FIVE`. Finished requires at least one submitted annual row and allows up to five; only submitted rows are validated/persisted, and omitted years are removed by delete-and-rebuild. |
| PENDING-EPROC00116-FIELD-TYPO-MAPPING | DBA / SA / RD | Public API names, entity names, and physical DB names can drift during migration. | PRD TBD-006 at `:60`; db-diff typo columns `IMCOME_DATE`, `IMPAIMENT_LOSS`, `SHARDHOLDERS_LOAN`, `AP_RELATED_PARITIES` at `TB_FIN_STATEMENT_INCOME_GI.md:40`, `:70`, `TB_FIN_STATEMENT_BALANCE_GI.md:69`, `:80`; current BE/FE canonical DTO names. | Closed: `DB_TYPO_PHYSICAL_CANONICAL_API_MAPPING`. Keep physical DB typo columns in persistence/schema; expose canonical API/FE names and require BE mapping plus code-stage tests. |
| PENDING-EPROC00116-HIGHLIGHT-MIGRATION | DBA / SA / RD | Old pipe-delimited `HIGHLIGHT`, latest split fields, and `HIGHLIGHT_CLOB` can lose or mis-split text. | PRD TBD-007 at `:61`; legacy pipe split at `EPROC00116_JS.jsp:711-721`; latest main columns at reverify `:1372-1393`; current split and null `HIGHLIGHT_CLOB` at `CsuFinancialStatementServiceImpl.java:2106-2151`. | Closed: `HIGHLIGHT_STRUCTURED_FIELDS_RAW_CLOB_PROVENANCE`. Public API uses five structured fields; split columns are current source of truth; raw legacy `HIGHLIGHT` goes to `HIGHLIGHT_CLOB` for provenance during migration, with ambiguous delimiter cases flagged for RD/DBA tests. |
| PENDING-EPROC00116-REPORT-TEMPLATE-OWNERSHIP | SA / RD / QA | PDF/Excel output may use i0-owned template names for c0 GI. | PRD TBD-008 at `:62`; PRD template-maintenance scope exclusion at `:105`; refactor c0 report artifacts and API calls; PDF/Excel paths at `CsuFinancialStatementServiceImpl.java:381-385`, `:441-461`; FE build task note at `phase-f-step2-00116-financial-statement-gi.md:27`. | Closed: `C0_REPORT_API_SHARED_I0_TEMPLATE_ASSET`. Public API/output ownership remains EPROC00116 c0; existing i0/EPROI00116 template paths are approved as shared report assets and are not a rename DoD for this bundle. |
| PENDING-EPROC00116-CALC-CHECKPOINT-SIDE-EFFECT | SA / RD | Calculation currently mutates checkpoint, while PRD frames checkpoint update under Save/Finished. | PRD flow at `:136-137`; legacy calc return-only at `EPROC0_0116_mod.java:426-439`; legacy save checkpoint update at `:543-547`; refactor calc/save split; current calc checkpoint write at `CsuFinancialStatementServiceImpl.java:247-257`. | Closed: `CALC_ENDPOINT_READ_ONLY_NO_CHECKPOINT_WRITE`. Calculation must not mutate checkpoint or financial-statement tables; current calc checkpoint write is a code-stage RD gap with test DoD. |
| PENDING-EPROC00116-PXLS-AUTHZ-SEED | RD / Security / Ops | Excel endpoint can return 403 or remain unseeded even though route exists. | Auth finding identifies c0 pxls route at `docs/build-tasks/c0-authz-sql-findings.md:159`; repo-visible DB precheck found the exact i0 pxls source absent, while current c0 ppdf export auth exists. | Closed: `PXLS_AUTH_SEED_PPDF_EQUIVALENT`. Owner ratified 2026-06-25 (was RD-proposed) copying `ROLE` from existing c0 `epl-ppdf-c0-financial-statement-comments` to `epl-pxls-c0-financial-statement-comments` with `REF_FUNCTION_ID=EPROC00116` (same export action → pxls roles == ppdf-c0); DB apply/recheck is RD/DBA code-stage DoD. |
| PENDING-EPROC00116-SAVE-LIST-BE-VALIDATION | RD | Save with missing or empty main/detail lists may produce unclear validation semantics. | PRD save map/list contract at `:425-430`, `:590-594`; DTO fields are `@Valid` only at `SaveCsuFinancialStatementCommentsRequest.java:30-47`; `request.init()` converts missing maps/lists into empty objects/lists at `:611-623`; current service loops after init at `CsuFinancialStatementServiceImpl.java:1358-1397`; legacy save uses `balancePar.size()` and same-index detail arrays at `EPROC0_0116_mod.java:477-511`. | Closed: `SAVE_LISTS_REQUIRED_ALIGNED_NONEMPTY`. Save/Finished require non-null main and three non-empty same-length detail arrays with one to five non-null rows; invalid shape is controlled validation before mutation, with implementation/tests as RD DoD. |
| PENDING-EPROC00116-PARITY-CODE-VERIFY | RD / QA / Controller | Final keep/fix/rebuild disposition and save-time Finished balance-gate hardening needed owner closure. | Matrix row marks EPROC00116 parity `?` at `per-page-reinventory-matrix.md:38`; parity task lists this page for calc/comments recheck at `c0-legacy-parity-recheck.md:27`; current save path `CsuFinancialStatementServiceImpl.java:271-340` does not show save-time `checkResult` / balance-cashflow revalidation. | Closed: `PARITY_FIX_KEEP_BE_FINISHED_HARDENING`. Keep current FE/BE architecture and require BE save-time Finished balance/cashflow hardening as RD code-stage DoD with QA-030. |
| PENDING-EPROC00116-HAVEDATA-CASHFLOW-PARITY | SA / RD / QA | Current info can return `haveData=Y` even when Cashflow rows are missing, while export requires Cashflow. | PRD requires three detail groups at `:216-218`, `:226-227`, `:727`; legacy query uses Balance/Income/Cashflow at `EPROC0_0116_mod.java:349-353`; refactor info requires SQL5 at `epl-info-c0-financial-statement-comments.md:160-162`; current service sets `haveData` without `sql5Entities` at `CsuFinancialStatementServiceImpl.java:185-186`, while export requires SQL5 at `:419-422`. | Closed: `HAVEDATA_REQUIRES_CASHFLOW_COMPLETENESS`. `haveData=Y` requires main plus Balance, Income, and Cashflow; missing Cashflow returns `N` and hides export actions. Current info predicate is RD code-stage gap with QA-031. |
| PENDING-EPROC00116-SERVICE-AUTHZ | RD / Security | Save/calc/export may rely on platform API auth without proven backend case/edit or case ownership guard. | PRD page permission/query-mode at `:166-168`, `:718`, `:772-773`; `TB_API_AUTH` covers endpoint-role at `docs/build-tasks/c0-authz-sql-findings.md:19-22`, `:153-159`; current controller dispatch at `CsuFinancialStatementController.java:73-88`, `:99-114`; current service save writes/checkpoint at `CsuFinancialStatementServiceImpl.java:271-340`; current FE pageAuth filtering at `financial-statement-gi.component.ts:278-285`, `:634-688`. | Closed: `SERVICE_LEVEL_CASE_PAGE_AUTH_REQUIRED`. Backend must enforce case/page read access for info/query/export and edit access for calc/save/Finished before business execution; implementation/tests are RD/Security DoD with QA-032. |

## New-vs-Old DB Delta / Reconcile
| Source fact | Disposition | Rule impact | Evidence |
|---|---|---|---|
| `TB_FIN_STATEMENT_MAIN` in db-diff/legacy has 12 columns, old PK `APPLICATION_NO`, and `HIGHLIGHT LONG`; latest reverify/current entity has 22 columns, PK `APPLICATION_NO, CURRENCY`, split 3000-char highlight fields, and `HIGHLIGHT_CLOB`. | carried with closed migration contract | R3, R7, R12 | db-diff `TB_FIN_STATEMENT_MAIN.md:23-41`; latest reverify `legacy_schema_reverify_new02_columns.tsv:1372-1393`, PK `..._pk.tsv:159-160`; current entity/service split at `CsuFinancialStatementServiceImpl.java:2106-2151`; closed decision `HIGHLIGHT_STRUCTURED_FIELDS_RAW_CLOB_PROVENANCE`. |
| `TB_FIN_STATEMENT_BALANCE_GI`, `TB_FIN_STATEMENT_INCOME_GI`, and `TB_FIN_STATEMENT_CASHFLOW_GI` remain active/exact detail tables with PK `APPLICATION_NO, DATA_SEQ`; latest reverify confirms columns and PKs. | carried | R4, R5, R6, R7, R9 | db-diff active/exact evidence; latest reverify columns `:1223-1273`, `:1287-1299`, `:1330-1371`; PKs `..._pk.tsv:151-158`. |
| Physical typo columns are present in active schema and must remain in DDL, with canonical API aliases required by the closed mapping contract. | carried with closed mapping contract | R5, R12 | PRD TBD-006; db-diff typo lines; latest reverify lines `:1247`, `:1254`, `:1265`, `:1332`, `:1362`; closed decision `DB_TYPO_PHYSICAL_CANONICAL_API_MAPPING`. |
| New checkpoint tables expose `TB_CHECK_POINTS_CS.EPROC00116` and `TB_CHECK_POINTS_CU.EPROC00116`; legacy PRD still references CORP/CU and RC_CORP/RC_CU names. Latest reverify has the full active CS/CU column set and is wider than older db-diff. | carried with stale-source note | R7, R9 | PRD checkpoint table `:155-158`; db-diff CS/CU `EPROC00116` at `TB_CHECK_POINTS_CS.md:47`, `TB_CHECK_POINTS_CU.md:43`; latest reverify checkpoint columns `legacy_schema_reverify_new02_columns.tsv:120-151`, PKs `:35-36`; current service route `CsuFinancialStatementServiceImpl.java:331-340`. |
| `TB_API_AUTH` exists with PK `API_ID`; six of seven target endpoint rows were present before RD seed, and pxls target was absent. The exact i0 pxls source row is absent in repo-visible DB precheck, so owner ratified 2026-06-25 (was RD-proposed) using the existing c0 ppdf export row as the reviewed-equivalent role source for this RD cycle. | close by DBA/RD seed apply and recheck | R10, R12 | reverify `TB_API_AUTH` columns `:1-5`, PK `:1`; auth findings `docs/build-tasks/c0-authz-sql-findings.md:153-159`, `:192`, `:196`; owner 2026-06-25 ratified pxls equals ppdf. |

## Decision Register
| ID | Owner | Impact | Evidence | Status |
|---|---|---|---|---|
| PENDING-EPROC00116-REFERENCE-STATUS-CODES | PM / SA | Reference application copy can accept or reject the wrong cases. | PRD TBD-001; legacy status/contract check; current reference query path. | Closed: `REFERENCE_COPY_CURRENT_REPOSITORY_ELIGIBILITY` -> R2/R12. |
| PENDING-EPROC00116-LEGACY-ACTION-AND-MESSAGE-COMPAT | SA / RD | Legacy action and message typo compatibility can break conversion/regression tests. | PRD TBD-002/003/004; `inti`; `COMMOM_MSG_NO_DATA`; `EPROI00119_MSG_VALIDATE_004`; refactor `epl-*` artifacts. | Closed: `LEGACY_ACTIONS_NO_ADAPTER_MESSAGE_ALIAS_ONLY` -> R10/R12. |
| PENDING-EPROC00116-FIVE-YEAR-REQUIREDNESS | PM / SA / QA | Finished validation can require all five years or allow partial-year payloads. | PRD TBD-005; JS date validation vs payload skip. | Closed: `FINISHED_AT_LEAST_ONE_YEAR_MAX_FIVE` -> R7/R12. |
| PENDING-EPROC00116-FIELD-TYPO-MAPPING | DBA / SA / RD | API/entity/schema naming can diverge for typo physical columns. | PRD TBD-006; db-diff/reverify typo columns; current BE/FE DTO mapping. | Closed: `DB_TYPO_PHYSICAL_CANONICAL_API_MAPPING` -> R4/R5/R12. |
| PENDING-EPROC00116-HIGHLIGHT-MIGRATION | DBA / SA / RD | Highlight data can lose delimiter structure or ignore `HIGHLIGHT_CLOB`. | PRD TBD-007; legacy pipe highlight; latest split/CLOB columns; current `setHighlightClob(null)`. | Closed: `HIGHLIGHT_STRUCTURED_FIELDS_RAW_CLOB_PROVENANCE` -> R3/R12. |
| PENDING-EPROC00116-REPORT-TEMPLATE-OWNERSHIP | SA / RD / QA | Export/PDF output can use wrong i0/c0 template ownership. | PRD TBD-008; refactor c0 report APIs; current template paths; FE task note. | Closed: `C0_REPORT_API_SHARED_I0_TEMPLATE_ASSET` -> R8/R12. |
| PENDING-EPROC00116-CALC-CHECKPOINT-SIDE-EFFECT | SA / RD | Calculation mutates checkpoint before Save/Finished decision. | Current calc writes checkpoint; PRD/legacy/refactor place checkpoint in save flow. | Closed: `CALC_ENDPOINT_READ_ONLY_NO_CHECKPOINT_WRITE` -> R7/R12. |
| PENDING-EPROC00116-PXLS-AUTHZ-SEED | RD / Security / Ops | Excel route may be unauthorized in target DB. | c0 auth findings show c0 target missing and repo-visible i0 source absent; current c0 ppdf auth row exists. | Closed: `PXLS_AUTH_SEED_PPDF_EQUIVALENT` -> R10/R12. |
| PENDING-EPROC00116-SAVE-LIST-BE-VALIDATION | RD | Missing save maps/lists may produce unclear validation semantics. | DTO `@Valid` only plus `init()` empty-list behavior. | Closed: `SAVE_LISTS_REQUIRED_ALIGNED_NONEMPTY` -> R7/R12. |
| PENDING-EPROC00116-PARITY-CODE-VERIFY | RD / QA / Controller | Final parity disposition and save-time Finished balance-gate hardening required owner decision. | Matrix and c0 legacy parity recheck keep EPROC00116 gated; current save path has required-field validation but no save-time balance/cashflow recheck. | Closed: `PARITY_FIX_KEEP_BE_FINISHED_HARDENING` -> R7/R9/R12. |
| PENDING-EPROC00116-HAVEDATA-CASHFLOW-PARITY | SA / RD / QA | `haveData` can disagree with export completeness when only Cashflow is missing. | PRD/legacy/refactor require Cashflow completeness; current info omits Cashflow from `haveData`, while export requires Cashflow. | Closed: `HAVEDATA_REQUIRES_CASHFLOW_COMPLETENESS` -> R1/R8/R12. |
| PENDING-EPROC00116-SERVICE-AUTHZ | RD / Security | Mutating/export endpoints need proven case/edit authorization beyond API seed rows. | Service-level guard proof UNFOUND before save/checkpoint writes and export generation; FE pageAuth is UI-only. | Closed: `SERVICE_LEVEL_CASE_PAGE_AUTH_REQUIRED` -> R10/R12. |

## Traceability
> ⚠️ **QA 2026-06-24 暫拔除**：`qa-cases.md` 已刪。本 bundle 所有 QA-0XX 引用（下表 QA 欄、metadata「through QA-021..QA-032」closeout 佐證、R12 各列「with QA-0XX」驗證掛鉤）均為 **dormant、不得視為已驗證**；R12 closeout 以 owner/RD-contract 規格決策為據、實作+測試為 code-stage DoD。REQ↔Rn 追溯仍有效；恢復 QA 後重建。
| PRD requirement | SRS rules | QA |
|---|---|---|
| FR-001 | R1, R10, R12 | QA-001, QA-002, QA-015, QA-021, QA-030, QA-031 |
| FR-002 | R2, R10, R12 | QA-003, QA-004, QA-015, QA-021, QA-022 |
| FR-003 | R3, R7, R10, R12 | QA-005, QA-010, QA-019, QA-020, QA-022, QA-025 |
| FR-004 | R4, R7, R12 | QA-006, QA-010, QA-017, QA-018, QA-020, QA-023, QA-024 |
| FR-005 | R5, R7, R12 | QA-007, QA-010, QA-023, QA-024 |
| FR-006 | R6, R7, R12 | QA-008, QA-010, QA-017, QA-018, QA-020, QA-023 |
| FR-007 | R4, R5, R6, R7, R10, R11, R12 | QA-006, QA-007, QA-008, QA-015, QA-016, QA-017, QA-018, QA-019, QA-020, QA-027 |
| FR-008 | R7, R9, R10, R12 | QA-009, QA-010, QA-013, QA-014, QA-015, QA-017, QA-018, QA-020, QA-023, QA-027, QA-028, QA-029, QA-030, QA-032 |
| FR-009 | R8, R10, R11, R12 | QA-011, QA-012, QA-016, QA-026, QA-028, QA-031, QA-032 |
| FR-010 | R8, R10, R11, R12 | QA-011, QA-012, QA-016, QA-026, QA-028, QA-031, QA-032 |
| FR-011 | R9, R12 | QA-013, QA-030 |

## Source Gaps
- No finalized full c0 parity findings file for EPROC00116 was found in this worker scope. Owner closed the SRS disposition as `PARITY_FIX_KEEP_BE_FINISHED_HARDENING`: keep the current architecture, and treat missing backend save-time balance/cashflow revalidation for direct imbalanced Finished payloads as RD code-stage DoD covered by QA-030.
- Latest DB evidence conflicts with older db-diff/legacy `TB_FIN_STATEMENT_MAIN.HIGHLIGHT` shape. `schema.sql` follows latest reverify/current entity; migration semantics are closed by R3 as `HIGHLIGHT_STRUCTURED_FIELDS_RAW_CLOB_PROVENANCE`.
