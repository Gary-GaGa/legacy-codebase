# SRS - EPROISU0922 Individual Summary

## Metadata
| Field | Value |
|---|---|
| funcId | EPROISU0922 |
| Status | In Review |
| Source PRD | `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md` |
| Bundle | `docs/specs/srs/EPROISU0922/` |
| Generated | 2026-06-21 |
| Review stop | SRS orchestrator stops at `in-review`; open decisions require PM/SA/RD review before approval. |

## Scope
- Covers the Individual Disbursement Process Summary page: summary query, CAD Maker submit, CAD Checker return, CAD Checker authorize, T24 file generation/upload, T24 deal result refresh, and three report downloads.
- The contract is brownfield: existing refactor endpoints are kept as RPC-style POST endpoints, and legacy/T24 details are evidence for parity and delta triage, not automatic public API fields.
- Out of scope: EPROISU0920/0921 data-entry authoring, async batch scheduler ownership, and final T24 field-by-field signoff beyond the deltas listed here.

## Sources
| Source | Evidence |
|---|---|
| PRD snapshot | `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md` |
| Legacy controller | `legacy-epro/JavaSource/com/cathaybk/epro/is/trx/EPROIS_0922.java:53`, `144`, `178`, `215`, `261`, `302`, `343` |
| Legacy T24 helper | `legacy-epro/JavaSource/com/cathaybk/epro/is/module/EPRO_IS0922.java:774`, `982`, `994`, `1090`, `1154` |
| Refactor artifact map | `docs/refactor-spec/02_modules/EPROISU0922.md:20`-`26` |
| Refactor authorize flow | `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/epl-case-isu-summary-auth.md:146`-`154`, `164`-`181` |
| Refactor T24 function | Primary T24 composition source per owner decision 2026-06-21: `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/funcisut24authorize.md:6`, `10`-`13`, `31`, `144`-`149`, `160`-`188` |
| Current backend endpoints | `backend/src/main/java/khd/svc/epro/controller/individual/SummaryController.java:43`, `56`, `82`, `95`, `108`, `121` |
| Current backend service | `backend/src/main/java/khd/svc/epro/service/individual/impl/SummaryServiceImpl.java:197`, `684`, `719`, `801`, `887`, `2359` |
| Legacy module errors | `legacy-epro/JavaSource/com/cathaybk/epro/is/module/EPROIS_0922_mod.java:707`, `714`, `972`, `979`, `1112`; `backend/src/main/java/khd/svc/epro/service/common/impl/FunctionServiceImpl.java:1192`, `1222` |
| Current FE calls | `frontend/src/app/pages/case-edition/sub-pages/individual/disbursement-process/services/api.service.ts:112`-`134` |
| DB snapshot | `docs/db-diff/02_tables/TB_LON_SUMMARY_INFO.md:38`-`85`, `docs/db-diff/02_tables/TB_DISBUR_DATE.md:38`-`66`, `docs/db-diff/02_tables/TB_LOAN_CONDITION_FEE.md:38`-`59`, `docs/db-diff/02_tables/TB_MESS_RECORD.md:38`-`45`, `docs/db-diff/02_tables/TB_MESS_UPLOAD_SFTP_RECORD.md:38`-`41` |
| Pending register | `docs/pending-register.md:14` |
| Disbursement decisions | `docs/disbursement/disbursement-triage.md:17`-`30`, `78` |

## Endpoints
| Endpoint | Method | Purpose | Covers |
|---|---|---|---|
| `epl-info-isu-summary` | POST | Summary page initialization. | R1, R2, R11 |
| `epl-case-isu-summary-submit` | POST | CAD Maker submits Summary to CAD Checker. | R3, R8, R11 |
| `epl-retu-isu-summary` | POST | CAD Checker returns Summary to Maker. | R4, R8, R11 |
| `epl-case-isu-summary-auth` | POST | CAD Checker authorizes disbursement, generates T24 file, uploads SFTP, and moves case to T24 waiting state. | R5, R6, R7, R8, R9, R11 |
| `epl-case-isu-summary-t24-result` | POST | Target contract for T24 deal result refresh; maps the legacy `t24DealResult` action. | R9, R11 |
| `funcConfCheckDate` | POST | Internal function to check T24 borrower/co-borrower check date freshness. | R6 |
| `funcIsuT24Authorize` | POST | Internal function to compose the T24 import file and return `t24UpPath`. | R7, R9, R10 |
| `epl-ppdf-isu-summary-report` | POST | Summary PDF download. | R12 |
| `epl-ppdf-isu-transaction-result-report` | POST | Transaction-result PDF download. | R12 |
| `epl-ppdf-isu-message-code-record-report` | POST | Message-code-record PDF download. | R12 |

### R1 Summary initialization **強制點: both**
covers-prd: FR-INIT-001, FR-INIT-002, FR-INIT-003, FR-UI-004, AC-001

Given a valid `applicationNo`, `epl-info-isu-summary` must return summary header, collateral, other collateral, role, current `CASE_PROGRESS`, CAD Checker option list `empList`, `t24ResultTime`, T24 status/result fields, and display data needed by the Summary page. When `CASE_PROGRESS=24` and the user has Maker role `404`, `empList` must contain active role-`405` CAD Checker employees only; inactive employees or non-405 roles must not be selectable for submit. BE must read from `TB_LON_SUMMARY_INFO`, `TB_DISBUR_DATE`, `TB_DISBUR_COLL`, `TB_DISBUR_OTHER_COLL`, `TB_T24_MAIN_BORROWER_INFO`, and `TB_T24_CO_BORROWER_INFO` as applicable. The query must not mutate state. Owner decision on 2026-06-21 closes `TBD-0922-005`: when `DISBURSING_DATE` is blank/null or the case is not at `CASE_PROGRESS=26`, return `t24ResultTime=N` and do not fail Summary initialization; return `t24ResultTime=Y` only when `CASE_PROGRESS=26`, `DISBURSING_DATE` is present, and elapsed time is greater than 10 minutes.

`TB_LON_SUMMARY_INFO.SECURE_ATTRIBUTE` is carried as server-read routing and audit context for Summary. EPROISU0922 does not create a separate S/U branch inside the Summary page: upstream ISU routing and collateral-page availability already decide whether secured collateral data is applicable. BE must not accept any client-supplied secure attribute as authority, and all Summary/authorize rules in this bundle apply to both `S` and `U` individual cases unless a later PRD/SRS delta explicitly introduces an S/U split.

Error-code contract: blank `applicationNo` returns `COMMON_MSG_ERROR_LON`; query/data failure returns `MSG_QUERY_FAIL`; no summary data returns `MSG_DATA_NOT_FOUND`.

### R2 Summary display and role/page auth **強制點: both**
covers-prd: FR-UI-001, FR-UI-002, FR-UI-003

The page must derive button and area visibility from server-provided page authorization plus case state. Role `404` at `CASE_PROGRESS=24` may submit to CAD Checker; role `405` at `CASE_PROGRESS=25` may return or authorize; states `26`, `27`, and `C1` show T24-related result/report actions. FE-hidden controls are not authoritative for mutating operations.

### R3 Maker submit **強制點: BE**
covers-prd: AC-002, AC-003

`epl-case-isu-summary-submit` must validate `applicationNo`, selected CAD Checker employee, and fresh T24 borrower/co-borrower check-date state before submit. On success, BE updates `TB_LON_SUMMARY_INFO.CASE_PROGRESS` to `25`, sets `CADC_CODE` and current handler for the checker, inserts `TB_APP_HISTORY`, and creates the notification task in `TB_NOTIFICATION_INFO` or the current notification mechanism. Owner decision on 2026-06-21 closes `TBD-0922-003`: the notification recipients must include the selected checker and any effective proxy users from `TB_EMP_PROXY`; actual email delivery, status `S/F`, template rendering, and retry are owned by the shared notification mechanism and are not synchronous success criteria for this endpoint after the task is persisted. The state update, history insert, and notification-task creation must be atomic.

Submit must preserve the submit-Maker identity for the later four-eyes authorize guard. If `TB_LON_SUMMARY_INFO.CURRENT_USER_ID` is overwritten to the selected checker during submit, BE must still retain the pre-submit Maker identity through the existing state/history/audit data available to the authorize service; the authorize comparison must not accidentally compare the checker against the post-submit current handler.

Error-code contract: blank `applicationNo` returns `COMMON_MSG_ERROR_LON`; stale T24 borrower/co-borrower check date on submit returns the bare legacy key `EPROIS0922_CLICK_CHECK`; other validation failure returns a 400 platform validation response; submit failure returns `MSG_SUBMIT_FAIL`.

### R4 Checker return **強制點: BE**
covers-prd: AC-005

`epl-retu-isu-summary` must validate that the authenticated user can act as CAD Checker for the case. On success, BE updates `TB_LON_SUMMARY_INFO.CASE_PROGRESS` to `24`, clears checker ownership as needed, restores current handler to Maker-side ownership, inserts `TB_APP_HISTORY`, and creates the notification task in `TB_NOTIFICATION_INFO` or the current notification mechanism. Recipients must include the Maker owner and any effective proxy users from `TB_EMP_PROXY`. Actual email delivery, status `S/F`, template rendering, and retry are owned by the shared notification mechanism and are not synchronous success criteria for this endpoint after the task is persisted. Return must not generate or upload any T24 file.

Error-code contract: blank `applicationNo` returns `COMMON_MSG_ERROR_LON`; return failure returns `MSG_RETURN_FAIL`; query/data failure returns `MSG_QUERY_FAIL`.

### R5 Checker authorize transaction **強制點: BE**
covers-prd: AC-006, AC-007

`epl-case-isu-summary-auth` must run as one transactional business operation around validation, T24 file generation, SFTP upload, `TB_MESS_UPLOAD_SFTP_RECORD` insert, `TB_LON_SUMMARY_INFO` update, and `TB_APP_HISTORY` insert. On successful SFTP upload, BE sets `TB_LON_SUMMARY_INFO.CASE_PROGRESS` to `26`, clears current handler, and writes `DISBURSING_DATE`. It must not move to `27` until T24 deal result is confirmed. Owner decision on 2026-06-21 closes `TBD-0922-004`: T24 file generation failure or blank `t24UpPath` returns safe category `T24_FILE_GENERATION_FAIL`; SFTP connection failure returns `SFTP_CONNECT_FAIL`; SFTP upload/file-transfer failure returns `SFTP_UPLOAD_FAIL`. These client-facing categories must not include host, user, credential, full path, stack trace, or raw customer data; the generated file name may be included for upload/file-transfer failure only. All three failure classes must stop before Summary `26`, `TB_MESS_UPLOAD_SFTP_RECORD`, and success history are committed, or must roll back to a consistent pre-authorize state.

Before any T24 file generation or SFTP action, authorize must enforce state `CASE_PROGRESS=25`, CAD Checker role `405`, assigned-checker/case ownership, and four-eyes separation. If the authenticated checker employee id equals the Maker who submitted the case into `CASE_PROGRESS=25`, BE must reject the request with named code `EPROIS0922_SELF_APPROVAL` and leave Summary, history, T24 file, SFTP upload record, and generated file side effects unchanged. This guard is service-level mandatory even if the user has endpoint seed access, both roles in test data, or a visible button.

Caller identity for authorize, including checker `empId`, must come from the authenticated context (session/JWT or equivalent platform principal). It must not be accepted from `ApplicationLangRequest` or any other client-supplied request body field, and the authorize request DTO must remain limited to application/language context.

Error-code contract: blank `applicationNo` returns `COMMON_MSG_ERROR_LON`; stale T24 borrower/co-borrower check date on authorize returns the bare legacy key `EPROIS0922_AUTHORIZE_CHECK`; self-approval/four-eyes violation returns `EPROIS0922_SELF_APPROVAL`; exchange-rate lookup failure is carried as `FAILED_E304` with bare key `EPROIS0921_UI_RAET_FIND_ERROR`; authorize failure returns `MSG_AUTHORIZE_FAIL`; T24 file generation or blank path failure returns `T24_FILE_GENERATION_FAIL`; SFTP connection failure returns `SFTP_CONNECT_FAIL`; SFTP upload/file-transfer failure returns `SFTP_UPLOAD_FAIL`.

### R6 T24 prerequisite checks **強制點: BE**
covers-prd: AC-004, FR-INIT-005

Before submit or authorize, BE must verify the latest T24 borrower and co-borrower `CHECK_DATE` values from `TB_T24_MAIN_BORROWER_INFO` and `TB_T24_CO_BORROWER_INFO`. Owner decision on 2026-06-21 closes `TBD-0922-006`: every required `CHECK_DATE` must equal the system calendar date; there is no cross-day grace period and no business-day/holiday exception. If the main borrower or any co-borrower check date is not the system calendar date, the operation must fail before state mutation. This carries the refactor `funcConfCheckDate` behavior and prevents stale borrower information from driving a T24 file.

Error-code contract: stale check date returns `EPROIS0922_CLICK_CHECK` for submit and `EPROIS0922_AUTHORIZE_CHECK` for authorize; query/data failure returns `MSG_QUERY_FAIL`.

### R7 T24 import file composition **強制點: BE**
covers-prd: AC-006

`funcIsuT24Authorize` must compose T24 records using refactor-spec `funcisut24authorize` v1.5 as the primary T24 composition source per owner decision on 2026-06-21. Brownfield parity evidence remains the fallback baseline only where v1.5 is silent, ambiguous, or explicitly escalated for owner confirmation. The public response exposes only `t24UpPath`; individual T24 A-H record fields are internal file content and QA evidence, not client-editable API fields. Owner decision on 2026-06-21 closes the T24 file-format subdecision: field delimiter is `!@#`, multi-value delimiter is `|`, record separator is CRLF, and outbound file encoding follows legacy behavior by not specifying an explicit charset and using the runtime JVM default charset. Do not hard-code UTF-8 unless a later owner decision supersedes legacy parity. Records must preserve max lengths and reject missing required source data before upload. Missing required T24 source data, including required branch-profile identity fields such as `T24_COMPANY`, must return `FAILED_E999` before SFTP upload or Summary success mutation.

The file must source monetary and rate fields from `TB_DISBUR_DATE`, `TB_LOAN_CONDITION_DETAIL`, `TB_LOAN_CONDITION_FEE`, and related T24 borrower/account tables. Current parity evidence re-opened the SRS-layer decision for exact T24 A-H to-be fields; v1.5 is now the main source for version and to-be composition, delimiter/CRLF/runtime-default encoding are closed, and null/default policy follows v1.5 explicit field rules per owner decision on 2026-06-21. Do not generalize all nulls to `N/A` or all nulls to empty string: for example, v1.5 sets `NAME_OF_BOREY` and `CUBC_RELATED_CIF_NO` null/blank values to `N/A`, while `HOUSING_PROJECT_CODE` null/blank becomes empty string. Owner decision on 2026-06-21 closes A16 loan type literal by following v1.5: `PRODUCT_CODE=01` and `MTG_PRODUCT_CODE=02` outputs `NORMAL.LAON`; this is also consistent with legacy evidence and must not be corrected to `NORMAL.LOAN`. Owner decision on 2026-06-21 closes `AGREEMENT_NO` truncation for T24 A31 and G7 only: both fields output the rightmost 16 characters when the source value exceeds 16 characters, and values shorter than or equal to 16 characters are preserved. This rule must not be applied to other T24 fields unless separately approved. Owner decision on 2026-06-21 closes G11/G12 law-firm fee remark mapping by fallback to legacy because the extracted v1.5 artifact is silent: note `00` uses `Registration Fee`, `01` uses `De-register fee`, `02` uses `Due diligent fee`, `03` uses `Ownership change`, and `90` uses `LAW_FIRM_90_OTHER_REMARK`; do not use `Legal fee`, `Other fee`, or `Special fee` for these notes. Owner decision on 2026-06-21 closes C12/C13: C12 reads sequence-specific `TB_COLL_VALUE_INFO.SUG_VAL` by `APPLICATION_NO` plus current `COLL_DATA_SEQ` and must not read `TB_COLL_INFO.SUG_VAL` or the generic C-row collateral map; C13 reads `TB_LON_SUMMARY_INFO.DECISION_DATE` formatted as `YYYYMMDD` and must not use `TB_T24_MAIN_BORROWER_INFO.CHECK_DATE`. Owner decision on 2026-06-21 closes C26 by fallback to legacy because the extracted v1.5 artifact does not expose C26: C26 reads only `TB_COLL_TITLE_DETAIL.TITLE_DEED_NO` rows for the current C row's `APPLICATION_NO` plus `COLL_DATA_SEQ`, joins multiple rows with `|`, and must not pre-merge all application title deeds into every C row. Owner decision on 2026-06-21 closes A52 by fallback to legacy and DB-exact keys because the extracted v1.5 artifact does not expose A52: A52 reads borrower district name from `TB_DISTRICT.DISTRICT_NAME` using the main borrower's `RESIDENT_PROVINCE_CODE` plus `RESIDENT_DISTRICT_CODE` and the existing disbursing-date `UPD_DATE` selection convention; no district match outputs empty string. Owner decision on 2026-06-21 closes C20 by fallback to DB-exact and legacy because the extracted v1.5 artifact only summarizes C19-C25: C20 reads the current C row's `TB_DISBUR_COLL.INS_EXPIRY_DATE`, formats it with the T24 date rule, outputs empty string when absent, and must not use non-schema `INS_END_DATE`. Owner decision on 2026-06-22 closes E21 by preserving the original new-backend branch from commit `afbfe75`: v1.5 exposes `DISBURSEMENT_CURRENCY` but is silent on E21 formula, `CBC_FEE`, and unsupported currencies; E21 therefore reads `TB_LOAN_CONDITION_FEE.CBC_FEE`, `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`, `EX_RATE_BUY`, and `EX_RATE_SELL`, outputs raw `CBC_FEE` for `USD`, outputs `CBC_FEE * ((EX_RATE_BUY + EX_RATE_SELL) / 2)` for `KHR` using backend average scale 4 `HALF_UP` and final scale 0 `HALF_UP`, and outputs `0` for any other currency only as a lower-level fallback under the A-5 owner decision that valid disbursement currencies are USD/KHR only and non-USD-non-KHR is by-design unreachable. Do not restore legacy all-non-USD exchange conversion unless A-5 is explicitly reopened. All known T24 A-H deltas identified during this SRS pass are closed by the itemized decisions below; newly discovered field deltas must be opened as new named pending items rather than reviving a generic umbrella.

Owner decision on 2026-06-22 closes G4/G10/H8 as target-contract deltas: G4 and H4 output `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`; G10 and H8 decide the rate field by `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`, not by `TB_MAIN_BORROWER_ACC.CURRENCY`. If `DISBURSEMENT_CURRENCY=USD`, G10/H8 output empty string. If `DISBURSEMENT_CURRENCY=KHR`, G10/H8 output `TB_DISBUR_DATE.EX_RATE_BUY`. Any non-USD-non-KHR value remains by-design unreachable under A-5 and must not be promoted to a supported currency path. Implementation closeout on 2026-06-22 aligns the current backend to this target by routing G10/H8 through `DISBURSEMENT_CURRENCY`.

Residual T24 re-open closeout on 2026-06-22 closes the remaining field gap: A15 follows v1.5 and legacy by outputting empty string when `TB_LON_SUMMARY_INFO.HOUSING_PROJECT_CODE` is null or blank; segment B and C9 both use `TB_BRANCH_PROFILE.T24_COMPANY`, segment D uses the seven legacy SSI fields, and segment H1-H7 follows the v1.5 branch/shared-field evidence plus legacy/backend parity. `T24_COMPANY` is resolved by DB-live schema-diff and new DB reverify as `VARCHAR2(5 BYTE)`, the current native-map code path reads `T24_COMPANY`, and `TBBranchProfileEntity` now maps the column for schema-model consistency while also reflecting the DB-reverified T24 column lengths. The generated `docs/db-diff/02_tables/TB_BRANCH_PROFILE.md` snapshot is stale for this column and carries a correction note. Owner instruction on 2026-06-22 confirms B8/C9 should follow the legacy/current source with no separate T24 contract override.

### R8 Authorization and service-level guards **強制點: BE**
covers-prd: role/security rules

Mutating endpoints must enforce BE-side authorization for the case, role, page, and state. `TB_API_AUTH` endpoint rows are necessary but not sufficient: service methods must reject unauthorized state transitions even if a FE button is visible or a user posts directly. CAD Maker-only and CAD Checker-only operations must leave `TB_LON_SUMMARY_INFO`, `TB_APP_HISTORY`, `TB_NOTIFICATION_INFO`, `TB_MESS_UPLOAD_SFTP_RECORD`, and T24 files unchanged on rejection.

Four-eyes enforcement is part of the service-level guard: a user cannot both submit as Maker and authorize as Checker for the same Summary transition, even if role mappings or endpoint seed rows would otherwise allow the direct API call.

### R9 T24 deal result refresh **強制點: BE**
covers-prd: AC-008, AC-009

The endpoint `epl-case-isu-summary-t24-result` is the online refresh/read layer for the T24 result source persisted in `TB_MESS_RECORD`. It must return `t24DealResultList` items with `DATA_SEQ`/`dataSeq`, `MESSAGE_CODE`, `REFERENCE_NO`, `STATUS`, and `ERROR_CODE` so the API contract matches the PRD result list and DB row identity. It must be idempotent for already-recorded T24 result rows: if the same application/result identity already exists in `TB_MESS_RECORD`, BE must not insert duplicate rows and may only return the existing result set or update derived Summary state according to the approved state rule. When the T24 done flag/result is successful, BE sets `TB_LON_SUMMARY_INFO.CASE_PROGRESS` to `27`. Owner decision on 2026-06-21 closes `TBD-0922-002`: every non-success T24 result sets `TB_LON_SUMMARY_INFO.CASE_PROGRESS` to `C1`, inserts `TB_CLO_REASON.REASON_CODE=C10`, returns `t24DealResultList`, `updateMap`, and `RESULT`, and must not move the case to success state `27`. Current backend route evidence is carried by `SummaryController`/`SummaryServiceImpl`: the route locks `TB_LON_SUMMARY_INFO`, limits refresh to the assigned CAD Checker on T24 result states, reads `TB_MESS_RECORD` by `APPLICATION_NO`/`DATA_SEQ`, returns the stored result list, and writes only the derived `27`/`C1` state and `TB_CLO_REASON=C10` when the persisted result set requires it.

### R10 Monetary precision, truncation, and exchange-rate handling **強制點: BE**
covers-prd: T24 monetary fields

Money fields must use DB precision as the hard contract: core disbursement, fee, collateral, and valuation amounts are `NUMBER(17,2)` in `TB_DISBUR_DATE`, `TB_DISBUR_COLL`, and `TB_DISBUR_OTHER_COLL`; exchange-rate fields in the Summary flow use the documented DB scale and must not be silently rounded beyond the source precision policy. Agreement numbers and T24-specific remarks must be truncated or rejected only according to T24 field-specific evidence, not arbitrary UI truncation.

Known parity decisions and re-open status: A-1 exchange-rate stub was implemented and conformance passed; B-group T24 parity fixes were accepted as as-is evidence. The EPROISU0922 T24 SRS re-open pass is now closed by the itemized REF-D8/TBD rows below; newly discovered field deltas must open new named pending items rather than reviving this closed worksheet.

Precision and truncation matrix:

| Field group | DB / T24 evidence | Required handling | Decision |
|---|---|---|---|
| `TB_DISBUR_DATE.DISBURSEMENT_AMOUNT`, `LAW_FIRM_AMOUNT`, `FACILITY_FEE`, `REFINANCING_FEE`, `LAW_FIRM_AMOUNT_01`, `LAW_FIRM_AMOUNT_02`, `LAW_FIRM_AMOUNT_03`, `LAW_FIRM_AMOUNT_90` | `NUMBER(17,2)` in `docs/db-diff/02_tables/TB_DISBUR_DATE.md:45`, `49`, `51`-`60`. | BE validates scale <= 2 and total precision <= 17 before DB write or T24 file generation. Overflow or extra scale is rejected; no silent rounding/truncation. | carried |
| `TB_DISBUR_DATE.EX_RATE_BUY`, `EX_RATE_SELL` | `NUMBER(17,2)` in `docs/db-diff/02_tables/TB_DISBUR_DATE.md:53`-`54`; current backend uses these fields for KHR and non-USD fee/rate output at `SummaryServiceImpl.java:2157`-`2162`, `2228`-`2229`, `2292`-`2294`. | BE reads persisted DB rate values as authoritative for Summary/T24; any derived average must use explicit rounding mode and scale from code/spec evidence, currently HALF_UP scale 4 for the KHR average path. No hidden conversion from client input. | carried |
| T24 G4/G10/H8 currency/rate source | PRD confirms T24 file and KHR exchange lookup but not field-level G/H rules at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:72`, `116`, `136`, `143`; v1.5 exposes shared T24 currency source `DISBURSEMENT_CURRENCY` at `funcisut24authorize.md:187`-`188`; legacy G4/G10/H8 use `DISBURSEMENT_CURRENCY` to decide blank-vs-`EX_RATE_BUY` at `EPRO_IS0922.java:1076`-`1079`, `1090`-`1094`, `1133`-`1138`, `1154`-`1165`; prior backend branched G10/H8 by `mainBorrowerAcc.CURRENCY`, and implementation closeout now routes both rows through `DISBURSEMENT_CURRENCY` at `SummaryServiceImpl.java:2211`-`2228`, `2281`-`2297`; owner decision keeps A-5 USD/KHR-only while closing G/H source as legacy `DISBURSEMENT_CURRENCY`. | G4 and H4 output `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`. G10/H8 branch on `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`: USD outputs empty string; KHR outputs `TB_DISBUR_DATE.EX_RATE_BUY`; non-USD-non-KHR remains by-design unreachable under A-5. RD must not use `TB_MAIN_BORROWER_ACC.CURRENCY` to decide G10/H8. | closed |
| `TB_DISBUR_COLL.INSURANCE_AMOUNT`, `PRICE_PROPERTY`, `PRICE_LAND`, `SUG_VAL`, `BUILT_WIDTH`, `BUILD_AREA`, `LAND_AREA` | `NUMBER(17,2)` in `docs/db-diff/02_tables/TB_DISBUR_COLL.md:46`, `56`-`61`. | Summary display and T24 generation preserve DB values; BE rejects values beyond `NUMBER(17,2)` before any writeback. | carried |
| `TB_DISBUR_OTHER_COLL.OTHER_PRICE_PROPERTY`, `OTHER_PRICE_LAND`, `OTHER_SUG_VAL`, `OTHER_BUILT_WIDTH`, `OTHER_BUILD_AREA`, `OTHER_LAND_AREA` | `NUMBER(17,2)` in `docs/db-diff/02_tables/TB_DISBUR_OTHER_COLL.md:48`-`53`. | Same policy as collateral fields: no silent rounding, no UI-only enforcement, sequence-specific rows remain authoritative. | carried |
| T24 C12 `SUG_VAL` | `TB_COLL_VALUE_INFO` is active/exact, keyed by `APPLICATION_NO` plus `COLL_DATA_SEQ`, and `SUG_VAL` is `NUMBER(19,2)` in `docs/db-diff/02_tables/TB_COLL_VALUE_INFO.md:13`-`17`, `31`-`39`, `67`; legacy writes this value to C12 at `EPRO_IS0922.java:660`-`662`, `729`; parity findings record the wrong-source fix. | T24 C12 reads the current collateral's `TB_COLL_VALUE_INFO.SUG_VAL`. Do not read from `TB_COLL_INFO.SUG_VAL`, `TB_DISBUR_COLL.SUG_VAL`, or a merged collateral map for this field. | closed |
| T24 C13 `DECISION_DATE` | v1.5 specifies `@SQL2` `DECISION_DATE` formatted `YYYYMMDD`; DB source `TB_LON_SUMMARY_INFO.DECISION_DATE` is `TIMESTAMP(6)` in `docs/db-diff/02_tables/TB_LON_SUMMARY_INFO.md:48`; legacy writes it to C13 at `EPRO_IS0922.java:642`, `650`, `729`; parity findings record the `CHECK_DATE` regression fix. | T24 C13 reads `TB_LON_SUMMARY_INFO.DECISION_DATE` and formats it as `YYYYMMDD`. Do not use `TB_T24_MAIN_BORROWER_INFO.CHECK_DATE`. | closed |
| T24 C20 `INS_EXPIRY_DATE` | `TB_DISBUR_COLL.INS_EXPIRY_DATE` is the DB insurance expiry date at `docs/db-diff/02_tables/TB_DISBUR_COLL.md:43`-`44`; legacy initializes C20 from `INS_EXPIRY_DATE`, formats it, and writes it at `EPRO_IS0922.java:674`-`687`, `732`-`733`; current backend uses `INS_EXPIRY_DATE` at `SummaryServiceImpl.java:1932`-`1934` in commit `85783d5`. | T24 C20 reads `TB_DISBUR_COLL.INS_EXPIRY_DATE` for the current `COLL_DATA_SEQ`, formats it with the T24 date rule, and outputs empty string if absent. `INS_END_DATE` is not a valid source. | closed |
| T24 E21 `CHRG_AMOUNT` | PRD only states USD/KHR Summary formatting and KHR exchange lookup at `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:83`, `116`; v1.5 only exposes `DISBURSEMENT_CURRENCY` at `funcisut24authorize.md:188`; `TB_LOAN_CONDITION_FEE.CBC_FEE` is `VARCHAR2(300 BYTE)` at `docs/db-diff/02_tables/TB_LOAN_CONDITION_FEE.md:53`; current backend branch is original from `afbfe75` and still present at `SummaryServiceImpl.java:2151`-`2170`; legacy converted every non-USD at `EPRO_IS0922.java:933`-`939`, `994`; A-5 owner decision says valid currencies are USD/KHR only and non-USD-non-KHR is unreachable. | E21 outputs raw `CBC_FEE` for USD; for KHR it uses persisted `EX_RATE_BUY` and `EX_RATE_SELL`, average scale 4 `HALF_UP`, then `CBC_FEE * average` scale 0 `HALF_UP`; for any other currency it outputs `0` only as by-design-unreachable fallback. Do not restore legacy all-non-USD conversion without reopening A-5. | closed |
| T24 C26 `TITLE_DEED_NO` | `TB_COLL_TITLE_DETAIL` is active/exact, keyed by `APPLICATION_NO`, `COLL_DATA_SEQ`, `COLL_DOC_SEQ`, and `TITLE_DEED_NO` is `VARCHAR2(100 BYTE)` in `docs/db-diff/02_tables/TB_COLL_TITLE_DETAIL.md:13`-`17`, `31`-`42`; legacy builds B/C per `COLL_DATA_SEQ`, queries title deeds with the current seq, joins with `|`, and writes C26 at `EPRO_IS0922.java:529`-`550`, `617`-`620`, `702`-`716`, `738`; parity findings record the all-title-deeds regression fix. | T24 C26 reads only the current C row's `TB_COLL_TITLE_DETAIL.TITLE_DEED_NO` rows and joins multiple rows with `|`. Do not merge all application title deeds into every C row. | closed |
| T24 A52 `DISTRICT_NAME` | `TB_MAIN_BORROWER_PERSONAL_INFO` carries `RESIDENT_PROVINCE_CODE` and `RESIDENT_DISTRICT_CODE` in `docs/db-diff/02_tables/TB_MAIN_BORROWER_PERSONAL_INFO.md:13`-`17`, `31`-`38`, `65`-`66`; `TB_DISTRICT` is active/exact, keyed by `PROVINCE_CODE`, `DISTRICT_CODE`, `UPD_DATE`, and carries `DISTRICT_NAME` in `docs/db-diff/02_tables/TB_DISTRICT.md:13`-`17`, `31`-`42`; legacy writes borrower district name to A52 at `EPRO_IS0922.java:472`-`480`, `516`-`517`; current backend implements the DB-exact lookup at `SummaryServiceImpl.java:1736`-`1748` in commit `446bdfb`. | T24 A52 reads `TB_DISTRICT.DISTRICT_NAME` by main borrower resident province/district and the existing disbursing-date `UPD_DATE` convention. If no matching district row exists, output empty string. Do not omit A52 when resident district data is resolvable. | closed |
| `TB_DISBUR_DATE.AGREEMENT_NO` used in T24 A31 and G7 | DB `VARCHAR2(30 BYTE)` at `docs/db-diff/02_tables/TB_DISBUR_DATE.md:43`; v1.5 extracted artifact does not expose A31/G7 `AGREEMENT_NO` detail; legacy writes A31 from the rightmost 16 characters at `legacy-epro/JavaSource/com/cathaybk/epro/is/module/EPRO_IS0922.java:301`-`302`, `505`, and writes G7 from the rightmost 16 characters at `legacy-epro/JavaSource/com/cathaybk/epro/is/module/EPRO_IS0922.java:1072`-`1073`, `1093`-`1094`; parity findings record the same fix. | Public API/DB retains up to 30 bytes; T24 A31 and G7 output the rightmost 16 characters only when source length exceeds 16. Do not apply this truncation to other T24 fields by default. | closed |
| `TB_DISBUR_DATE.LAW_FIRM_90_OTHER_REMARK` | DB `VARCHAR2(16 BYTE)`, note says T24上线16碼 at `docs/db-diff/02_tables/TB_DISBUR_DATE.md:61`. | BE enforces <= 16 bytes/chars before T24 output. Longer values are rejected or require explicit owner-approved truncation rule; silent truncation is not allowed. | carried |
| `TB_DISBUR_DATE.CUBC_RELATED_CIF_NO` | DB `VARCHAR2(10 BYTE)` with note `6個字元` at `docs/db-diff/02_tables/TB_DISBUR_DATE.md:66`. | BE must validate the stricter documented T24 value length when composing related-CIF T24 fields; if DB and T24 notes conflict for an input, reject and surface validation rather than trim silently. | carried |
| T24 A-H field semantic mapping | Legacy/refactor evidence spans `EPRO_IS0922.java:774`, `982`, `994`, `1090`, `1154`; refactor T24 artifact latest is `funcisut24authorize` in `docs/refactor-spec/02_modules/EPROISU0922.md:23`. | Precision/length policy above is fixed for SRS; all known legacy/refactor semantic-source deltas identified in this pass are itemized and closed. Newly discovered field deltas must be opened as named pending items. | closed |

### R11 Auditability and transaction consistency **強制點: BE**
covers-prd: NFR audit/data consistency

All mutating operations must write audit/history records after successful state transitions and must avoid partial commits. Submit and return must roll back summary state and history if notification-task creation fails; async email delivery failure after the task is persisted is handled by the shared notification mechanism and does not roll back the already-committed submit/return state. Submit, return, authorize, and T24 result update must have deterministic rollback behavior across summary state, history, notification/SFTP logs, and generated/uploaded files.

Logging must not write complete request or response bodies. T24 import payload content, `DRAWDOWN_ACCOINT` / `drawdownAccount`, and `DISBURSEMENT_AMOUNT` / `disbursementAmount` are customer data and must be masked or omitted in application, integration, and error logs. Logs may record correlation ids, endpoint ids, safe result codes, and generated file names, but must not log credentials, full secret paths, stack traces, host/user secrets, or raw customer data beyond existing safe audit policy.

### R12 Report downloads **強制點: both**
covers-prd: AC-010

Summary PDF, transaction result report, and message code record report downloads must accept `applicationNo` plus language/context and return an encrypted temporary file path or existing report-service response shape. Download failures return `COMMON_MSG_DOWNLOAD_FAIL`; success carries the existing platform download success response. These downloads map to `epl-ppdf-isu-summary-report`, `epl-ppdf-isu-transaction-result-report`, and `epl-ppdf-isu-message-code-record-report`. Report endpoints must not mutate summary state.

## 新舊 DB / 更動 delta
| Delta | 三判 | SRS action | Source |
|---|---|---|---|
| DB-D1 `TB_LON_SUMMARY_INFO` is active/exact; key Summary state columns include `APPLICATION_NO`, `CASE_PROGRESS`, `CURRENT_USER_ID`, `CADC_CODE`, and `DISBURSING_DATE`; table note says `DISBURSING_DATE` is an auto-disbursement added field. | carried | R1/R3/R4/R5/R9 use `TB_LON_SUMMARY_INFO` as state authority; no schema rollback. R3/R5 also require preserving the submit-Maker identity for four-eyes authorize comparison even if `CURRENT_USER_ID` is reassigned to the checker. | `docs/db-diff/02_tables/TB_LON_SUMMARY_INFO.md:13`-`16`, `38`, `49`, `62`, `83`-`85`; Bible `docs/specs/bible/bible-eproposal.md:79`, `306`, `463` |
| DB-D13 `TB_LON_SUMMARY_INFO.SECURE_ATTRIBUTE` is an active case-routing attribute. | carried + page-level disclaim | R1 carries it as server-read routing/audit context and explicitly disclaims any intra-0922 S/U split. | Bible `docs/specs/bible/bible-eproposal.md:230`, `326`; `schema.sql` `TB_LON_SUMMARY_INFO.SECURE_ATTRIBUTE` |
| DB-D2 `TB_DISBUR_DATE` is active/exact and carries disbursement currency/amount, law-firm fees, exchange rates, repayment day, and T24-related values. | carried | R7/R10 use DB precision as hard limit; schema.sql includes money and rate columns with change hints. G4/H4 and G10/H8 use `DISBURSEMENT_CURRENCY` as the currency source; G10/H8 use `EX_RATE_BUY` only when that source currency is KHR. | `docs/db-diff/02_tables/TB_DISBUR_DATE.md:13`-`16`, `38`-`66` |
| DB-D16 `TB_DISBUR_DATE.DRAWDOWN_ACCOINT` keeps the physical typo but stores drawdown account text. | DB-resolvable correction | `schema.sql` uses `VARCHAR2(25 BYTE)` and OpenAPI exposes `drawdownAccount` as string max 25. This intentionally corrects the SRS schema type from stale db-diff `DATE` evidence while preserving the physical column name. | Current entity `TBDisburDateEntity.java:60`-`61`; refactor summary artifact `epl-info-isu-summary.md:179`; FE summary artifact `eproisu0922-summary.md:177`; `schema.sql` `DRAWDOWN_ACCOINT` |
| DB-D3 `TB_DISBUR_COLL` and `TB_DISBUR_OTHER_COLL` are active/exact, keyed by `APPLICATION_NO` plus sequence, and amount/area valuation fields are `NUMBER(17,2)`. | carried | Summary displays and T24 file generation use per-row sequence data; no FE-provided sequence may override persisted PK rows. | `docs/db-diff/02_tables/TB_DISBUR_COLL.md:13`-`16`, `38`-`67`; `docs/db-diff/02_tables/TB_DISBUR_OTHER_COLL.md:13`-`16`, `38`-`59` |
| DB-D11 `TB_DISBUR_COLL.INS_EXPIRY_DATE` is active/exact insurance expiry date for the collateral row. | carried | R7/R10 use this column as the authoritative T24 C20 source; `INS_END_DATE` is not a valid source column. | `docs/db-diff/02_tables/TB_DISBUR_COLL.md:13`-`16`, `38`-`45` |
| DB-D12 `TB_LOAN_CONDITION_FEE` is active/exact, keyed by `APPLICATION_NO` plus `CON_TYPE`, and carries `CBC_FEE` as `VARCHAR2(300 BYTE)`. | carried | R7/R10 use `CBC_FEE` as the E21 charge amount source. Because the DB type is text, BE must parse it only for the KHR calculation path and fail file generation on non-numeric KHR values rather than silently outputting a malformed T24 amount. | `docs/db-diff/02_tables/TB_LOAN_CONDITION_FEE.md:13`-`16`, `31`-`39`, `53` |
| DB-D8 `TB_COLL_VALUE_INFO` is active/exact, keyed by `APPLICATION_NO` plus `COLL_DATA_SEQ`, and `SUG_VAL` is `NUMBER(19,2)`. | carried | R7/R10 use this table as the authoritative source for T24 C12; `TB_COLL_INFO` and generic collateral maps are not valid C12 sources. | `docs/db-diff/02_tables/TB_COLL_VALUE_INFO.md:13`-`17`, `31`-`39`, `67` |
| DB-D9 `TB_COLL_TITLE_DETAIL` is active/exact, keyed by `APPLICATION_NO`, `COLL_DATA_SEQ`, and `COLL_DOC_SEQ`; `TITLE_DEED_NO` is `VARCHAR2(100 BYTE)`. | carried | R7/R10 use this table as the authoritative source for T24 C26 by current `COLL_DATA_SEQ`; all-application pre-merge is not valid. | `docs/db-diff/02_tables/TB_COLL_TITLE_DETAIL.md:13`-`17`, `31`-`42` |
| DB-D10 `TB_MAIN_BORROWER_PERSONAL_INFO` and `TB_DISTRICT` are active/exact sources for borrower resident district name; `TB_DISTRICT` PK includes `PROVINCE_CODE`, `DISTRICT_CODE`, and `UPD_DATE`. | carried | R7/R10 use main borrower resident province/district plus the existing disbursing-date `UPD_DATE` convention as the authoritative A52 lookup. | `docs/db-diff/02_tables/TB_MAIN_BORROWER_PERSONAL_INFO.md:13`-`17`, `31`-`38`, `65`-`66`; `docs/db-diff/02_tables/TB_DISTRICT.md:13`-`17`, `31`-`42` |
| DB-D4 `TB_MESS_UPLOAD_SFTP_RECORD` is active/exact and stores uploaded file name, generated path, and upload execution time by `APPLICATION_NO`. | carried | R5 records SFTP upload only after generated file upload succeeds; client receives no secret SFTP credentials. | `docs/db-diff/02_tables/TB_MESS_UPLOAD_SFTP_RECORD.md:13`-`16`, `38`-`41` |
| DB-D5 `TB_MESS_RECORD` is active/exact and stores T24 result fields `MESSAGE_CODE`, `REFERENCE_NO`, `STATUS`, `ERROR_CODE`, `VALUE_DATE`, and `REFERENCE_REMARK`. | carried | R9 persists new T24 results idempotently, must not re-insert already-recorded result rows, and drives `CASE_PROGRESS=27/C1` from result semantics after the open failure-mapping decision is closed. | `docs/db-diff/02_tables/TB_MESS_RECORD.md:13`-`16`, `38`-`45`; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:120` |
| DB-D6 `TB_API_AUTH` is active/exact, note `重構新增`, and transpose method `重新寫入`. | changed | R8 requires endpoint seed rows plus service-level state/role guards. SRS does not treat auth seed rows alone as sufficient. | `docs/db-diff/02_tables/TB_API_AUTH.md:9`-`16`, `38`-`40` |
| DB-D14 `TB_T24_MAIN_BORROWER_INFO` and `TB_T24_CO_BORROWER_INFO` are active check-date sources. | carried | R6 checks current-day `CHECK_DATE` before submit and authorize; stale rows block state mutation before T24 generation. | `schema.sql` `TB_T24_MAIN_BORROWER_INFO`, `TB_T24_CO_BORROWER_INFO`; PRD `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:114`, `170` |
| DB-D15 `TB_LOAN_CONDITION_DETAIL` is a referenced T24/source table for loan-condition fields. | carried | R7/R10 keep it in the T24 generation source set and schema notes so detail-derived T24 fields are not treated as client input. | PRD `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:144`; `schema.sql` referenced source table list |
| DB-D7 `TB_EMP_PROXY` is active/exact, keyed by `EMP_ID`, and stores proxy recipient `PROXY_ID` plus active time bounds. | carried | R3/R4 read `TB_EMP_PROXY` to include effective proxy recipients in submit/return notification tasks; EPROISU0922 does not mutate proxy rows. | `docs/db-diff/02_tables/TB_EMP_PROXY.md:13`-`16`, `31`-`41`; refactor `epl-case-isu-summary-submit.md:189`-`198`; refactor `epl-retu-isu-summary.md:186`-`193` |
| REF-D1 Latest artifact map has six BE artifacts and one FE screen artifact for EPROISU0922. | carried | Endpoints are based on latest refactor artifacts, not legacy action names. | `docs/refactor-spec/02_modules/EPROISU0922.md:3`-`26` |
| REF-D2 Refactor authorize flow explicitly calls check-date, exchange-rate, T24 file generation, SFTP record insert, summary update, and history insert. | carried | R5/R6/R7 keep this sequence and require transaction/rollback semantics. | `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/epl-case-isu-summary-auth.md:146`-`154`, `164`-`181` |
| REF-D3 T24 field behavior has SRS-layer re-open despite earlier "follow legacy" closeout. | closed | Owner decisions 2026-06-21 and 2026-06-22 set `funcisut24authorize` v1.5 as the primary T24 composition source; close file format as delimiter `!@#`, multi-value `|`, CRLF record separator, and legacy runtime-default encoding; close null/default policy as v1.5 explicit field rules first; close A16 as v1.5/legacy `NORMAL.LAON`; close A31/G7 `AGREEMENT_NO` as rightmost 16 characters only for those two fields; close G11/G12 law-firm fee remarks as legacy mapping; close C12/C13 as `TB_COLL_VALUE_INFO.SUG_VAL` by `COLL_DATA_SEQ` and `TB_LON_SUMMARY_INFO.DECISION_DATE` formatted `YYYYMMDD`; close C20 as `TB_DISBUR_COLL.INS_EXPIRY_DATE` by current `COLL_DATA_SEQ`; close C26 as `TB_COLL_TITLE_DETAIL.TITLE_DEED_NO` by current `COLL_DATA_SEQ`, joined with `|`; close A52 as `TB_DISTRICT.DISTRICT_NAME` by main borrower resident province/district plus disbursing-date `UPD_DATE` convention; and close E21 as the original new-backend USD/KHR/other branch from `afbfe75`, with non-USD-non-KHR `0` treated as by-design-unreachable under A-5. R7/R10 keep as-is evidence visible only as fallback or implementation-delta evidence. Residual worksheet status is now superseded by REF-D8: A15, B/D/H rows, `T24_COMPANY` B8/C9, `TBD-0922-007`, and `TBD-0922-008` are closed. | `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/funcisut24authorize.md:6`, `10`-`13`, `31`, `116`, `125`-`126`, `151`-`152`, `187`-`189`, `192`-`206`; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:31`, `83`, `116`, `133`, `136`, `147`; `docs/db-diff/02_tables/TB_DISBUR_COLL.md:13`-`16`, `38`-`45`; `docs/db-diff/02_tables/TB_LOAN_CONDITION_FEE.md:13`-`16`, `31`-`39`, `53`; `docs/db-diff/02_tables/TB_COLL_VALUE_INFO.md:13`-`17`, `31`-`39`, `67`; `docs/db-diff/02_tables/TB_COLL_TITLE_DETAIL.md:13`-`17`, `31`-`42`; `docs/db-diff/02_tables/TB_MAIN_BORROWER_PERSONAL_INFO.md:13`-`17`, `31`-`38`, `65`-`66`; `docs/db-diff/02_tables/TB_DISTRICT.md:13`-`17`, `31`-`42`; `docs/db-diff/02_tables/TB_LON_SUMMARY_INFO.md:48`; legacy `EPRO_IS0922.java:70`, `176`, `183`-`184`, `290`, `306`-`307`, `93`, `96`, `249`-`253`, `301`-`302`, `472`-`480`, `505`, `516`-`517`, `529`-`550`, `617`-`620`, `642`, `650`, `660`-`662`, `674`-`687`, `702`-`716`, `729`, `732`-`733`, `738`, `933`-`939`, `994`, `1025`-`1049`, `1072`-`1073`, `1093`-`1096`; current backend `SummaryServiceImpl.java:1736`-`1748`, `1932`-`1934`, `2151`-`2170`; `docs/build-tasks/done/t24-bgroup-legacy-parity-fix-findings.md:16`-`25`; `docs/disbursement/disbursement-triage.md:30`, `61`, `94`, `107`, `112`; `docs/pending-register.md:14` |
| REF-D6 G/H fee-transfer currency and exchange-rate branch source differs between legacy target and prior backend. | closed | R7/R10 close G4/G10/H8 to legacy/refactor-source target: G4/H4 output `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`, and G10/H8 branch on `DISBURSEMENT_CURRENCY` rather than `TB_MAIN_BORROWER_ACC.CURRENCY`. USD outputs blank rate, KHR outputs `EX_RATE_BUY`, and non-USD-non-KHR remains A-5 unreachable. Implementation closeout on 2026-06-22 updates the current backend to use this rule for both law-firm fee and EIR/compensation fee rows. | PRD `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:72`, `116`, `136`, `143`; refactor `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/funcisut24authorize.md:187`-`188`; legacy `EPRO_IS0922.java:1076`-`1079`, `1090`-`1094`, `1133`-`1138`, `1154`-`1165`; current backend `SummaryServiceImpl.java:2211`-`2228`, `2281`-`2297`; owner decision `docs/disbursement/disbursement-triage.md:5`, `42` |
| REF-D7 Prior backend T24 file writing hard-coded UTF-8 while the closed target follows legacy runtime-default encoding. | closed | R7 keeps the target as no explicit charset, matching legacy `FileWriter` behavior. Implementation closeout on 2026-06-22 removes the explicit UTF-8 charset and writes bytes with the JVM default charset to preserve legacy behavior. | legacy `EPRO_IS0922.java:183`-`184`; current backend `SummaryServiceImpl.java:1027`-`1028`; `docs/specs/srs/EPROISU0922/spec.md:90` |
| REF-D8 Residual T24 re-open worksheet closeout for A15 and B/D/H segment rows. | closed | A15 is closed by v1.5 and legacy/backend parity: null/blank `HOUSING_PROJECT_CODE` outputs empty string, not `N/A`. Segment D is closed as `CUSTOMER.SSI`, borrower T24 no, disbursement currency, drawdown account, transaction branch, `ALL`, and application no. Segment H1-H7 is closed as EIR/refinancing fee rows using transaction types `FT.LOAN.EIR.52153`/`FT.LOAN.EIR.52155`, branch, drawdown account, disbursement currency, fee amount, credit account, and application no; H8 remains governed by REF-D6. Segment B is closed with B8 sourced from `TB_BRANCH_PROFILE.T24_COMPANY`; C9 uses the same source. Owner instruction on 2026-06-22 confirms no separate T24 contract override and to follow the legacy/current source. DB schema, current native-map read path, required-value handling, and JPA entity mapping are established. Close `TBD-0922-007` and `TBD-0922-008`. | refactor `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/funcisut24authorize.md:6`, `13`, `22`, `116`, `151`-`152`, `185`-`188`, `199`-`201`; dispatch `docs/build-tasks/disbursement-reopen-srs-dispatch.md:17`-`21`, `31`, `39`-`40`; db correction `docs/db-diff/02_tables/TB_BRANCH_PROFILE.md:47`-`50`; DB-live schema-diff `docs/build-tasks/schema-diff-findings.md:246`-`249`; new DB `epro-db/out/legacy_schema_reverify_new02_columns.tsv:37`; legacy `EPRO_IS0922.java:306`-`307`, `493`-`494`, `529`-`555`, `569`-`603`, `645`-`652`, `726`-`727`, `749`-`777`, `1107`-`1172`; current backend `SummaryServiceImpl.java:906`-`907`, `1111`-`1112`, `1808`-`1822`, `1879`-`1881`, `1909`-`1921`, `1990`-`2015`, `2271`-`2290`, `2293`-`2297`; entity/repo `TBBranchProfileEntity.java:44`-`47`, `TBBranchProfileRepository.java:37`-`47`; escalation `docs/disbursement/disbursement-domain-escalations.md:17`, `23` |
| REF-D4 Current backend exposes `funcIsuT24Authorize` as an internal endpoint returning only `t24UpPath`. | carried | OpenAPI keeps `t24UpPath`; T24 record contents are verified through file/DB side effects, not client fields. | `backend/src/main/java/khd/svc/epro/controller/individual/SummaryController.java:108`-`110`; `backend/src/main/java/khd/svc/epro/dto/response/individual/summary/FuncIsuT24AuthorizeResponse.java:10`-`11` |
| REF-D5 Current backend upload path writes `TB_MESS_UPLOAD_SFTP_RECORD` then updates Summary to `CASE_PROGRESS=26`. | carried | QA must assert SFTP log insert and summary state mutation happen together. | `backend/src/main/java/khd/svc/epro/service/individual/impl/SummaryServiceImpl.java:2384`-`2403` |

## @PENDING
Historical note: `TBD-0922-001` is intentionally left as the umbrella closure record for the 2026-06-21/2026-06-22 pass. Its carve-out sentence explains why `TBD-0922-007`/`TBD-0922-008` were opened; current residual status is authoritative in REF-D8 and the rows below: `TBD-0922-007` and `TBD-0922-008` are now closed.
| ID | Status | Decision needed | Owner | Source |
|---|---|---|---|---|
| TBD-0922-001 | closed | Owner decisions 2026-06-21/2026-06-22 close the T24 umbrella item: `funcisut24authorize` v1.5 is the primary T24 composition source; delimiter is `!@#`, multi-value delimiter is `|`, record separator is CRLF, encoding follows legacy runtime-default charset with no explicit charset, null/default policy follows v1.5 explicit field rules without global inference, A16 loan type literal is `NORMAL.LAON`, A31/G7 `AGREEMENT_NO` use rightmost 16 characters only for those two fields, G11/G12 law-firm fee remarks follow legacy mapping, C12 uses `TB_COLL_VALUE_INFO.SUG_VAL` by `COLL_DATA_SEQ`, C13 uses `TB_LON_SUMMARY_INFO.DECISION_DATE` as `YYYYMMDD`, C20 uses `TB_DISBUR_COLL.INS_EXPIRY_DATE`, C26 uses current-collateral `TB_COLL_TITLE_DETAIL.TITLE_DEED_NO` joined with `|`, A52 uses `TB_DISTRICT.DISTRICT_NAME` by main borrower resident province/district plus disbursing-date `UPD_DATE` convention, E21 keeps the original new-backend USD/KHR/other branch with non-USD-non-KHR `0` documented as A-5 by-design-unreachable, and G4/G10/H8 use `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY` as the target currency source with G10/H8 blank for USD and `EX_RATE_BUY` for KHR rather than branching on account currency. Precision/length/truncation policy is fixed in R10 and should only change by explicit owner decision. At the time of the umbrella closure, the re-open worksheet fields A15, T24_COMPANY/B8/C9, and B/D/H1-H7 were carved out for separate review; REF-D8 and the rows below now close `TBD-0922-007` and `TBD-0922-008`. | PM/SA/RD | `docs/refactor-spec/03_artifacts/be-individual/EPROISU0922/funcisut24authorize.md:6`, `10`-`13`, `31`, `116`, `125`-`126`, `151`-`152`, `187`-`189`, `192`-`206`; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:31`, `72`, `83`, `116`, `133`, `136`, `143`, `147`, `195`; `docs/db-diff/02_tables/TB_DISBUR_COLL.md:13`-`16`, `38`-`45`; `docs/db-diff/02_tables/TB_LOAN_CONDITION_FEE.md:13`-`16`, `31`-`39`, `53`; `docs/db-diff/02_tables/TB_COLL_VALUE_INFO.md:13`-`17`, `31`-`39`, `67`; `docs/db-diff/02_tables/TB_COLL_TITLE_DETAIL.md:13`-`17`, `31`-`42`; `docs/db-diff/02_tables/TB_MAIN_BORROWER_PERSONAL_INFO.md:13`-`17`, `31`-`38`, `65`-`66`; `docs/db-diff/02_tables/TB_DISTRICT.md:13`-`17`, `31`-`42`; `docs/db-diff/02_tables/TB_LON_SUMMARY_INFO.md:48`; legacy `EPRO_IS0922.java:70`, `176`, `183`-`184`, `290`, `306`-`307`, `93`, `96`, `249`-`253`, `301`-`302`, `472`-`480`, `505`, `516`-`517`, `529`-`550`, `617`-`620`, `642`, `650`, `660`-`662`, `674`-`687`, `702`-`716`, `729`, `732`-`733`, `738`, `933`-`939`, `994`, `1025`-`1049`, `1072`-`1079`, `1090`-`1096`, `1133`-`1138`, `1154`-`1165`; current backend `SummaryServiceImpl.java:1027`-`1028`, `1736`-`1748`, `1932`-`1934`, `2151`-`2170`, `2211`-`2228`, `2281`-`2297`; `docs/build-tasks/done/t24-bgroup-legacy-parity-fix-findings.md:16`-`26`; `docs/disbursement/disbursement-triage.md:5`, `30`, `42`, `61`, `94`, `107`, `112`; `docs/pending-register.md:14` |
| TBD-0922-002 | closed | Owner decision 2026-06-21: every non-success T24 result writes `CASE_PROGRESS=C1` and `TB_CLO_REASON.REASON_CODE=C10`; no display-only non-success branch. | PM/SA | PRD TBD-002/TBD-006; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:119`, `175`; legacy `EPROIS_0922_mod.java:1186`-`1195`, `1356`-`1364` |
| TBD-0922-003 | closed | Owner decision 2026-06-21: submit/return must create notification tasks with primary recipient plus effective `TB_EMP_PROXY` recipients; actual email delivery, status `S/F`, template rendering, and retry belong to the shared notification mechanism. Notification-task creation failure rolls back the submit/return transaction. | SA/RD | PRD TBD-008/AC-002/AC-005; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:34`, `149`-`150`, `184`; refactor `epl-case-isu-summary-submit.md:91`-`93`, `153`-`155`, `196`-`198`; refactor `epl-retu-isu-summary.md:88`-`90`, `147`-`149`, `190`-`193`; legacy `EPROIS_0922_mod.java:818`-`880`, `909`-`936` |
| TBD-0922-004 | closed | Owner decision 2026-06-21: T24 file generation/blank path returns `T24_FILE_GENERATION_FAIL`; SFTP connection returns `SFTP_CONNECT_FAIL`; SFTP upload/file-transfer returns `SFTP_UPLOAD_FAIL`; no host/user/credential/full path/stack trace/customer data in client message. | RD | PRD TBD-004/AC-007; `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:30`, `117`, `173`; refactor `epl-case-isu-summary-auth.md:149`-`154`; legacy `EPROIS_0922.java:239`-`244`, `EPRO_IS0922.java:188`-`201` |
| TBD-0922-005 | closed | Owner decision 2026-06-21: follow PRD; blank/null `DISBURSING_DATE` or non-`26` state returns `t24ResultTime=N` without failing initialization; only `CASE_PROGRESS=26` and elapsed time greater than 10 minutes returns `Y`. | SA/RD | `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:27`, `86`, `118`, `160`; legacy risk `EPROIS_0922_mod.java:516`-`552` |
| TBD-0922-006 | closed | Owner decision 2026-06-21: every required borrower/co-borrower `CHECK_DATE` must equal the system calendar date; no cross-day grace period and no business-day/holiday exception. Submit returns `EPROIS0922_CLICK_CHECK`; authorize returns `EPROIS0922_AUTHORIZE_CHECK`. | PM/SA | `docs/specs/prd/PRD-CDC-EPRO-0001-EPROISU0922-v1.0.md:33`, `114`, `170`; refactor `funcconfcheckdate.md:97`, `102`-`107`; legacy `EPROIS_0922_mod.java:704`-`714`, `969`-`979` |
| TBD-0922-007 | closed | **Owner instruction on 2026-06-22: follow the legacy/current source for `T24_COMPANY` feeding T24 `B8`/`C9`; no separate T24 contract override.** A15 is closed by v1.5 and legacy/backend parity as blank-preservation (`""`, not `N/A`). The DB fact is resolved by DB-live schema-diff and new DB reverify: `TB_BRANCH_PROFILE.T24_COMPANY` is present as `VARCHAR2(5 BYTE)`. The current backend read path is present: `findByDepCode` runs native `SELECT * FROM TB_BRANCH_PROFILE`, `funcIsuT24Authorize` calls that repository path, and B8/C9 read `T24_COMPANY`. `TBBranchProfileEntity` now maps `T24_COMPANY` for JPA schema-model consistency and aligns DB-reverified T24 column lengths, while the native-map path remains unchanged. The implementation no longer silently falls back to blank for this identity/routing value: missing branch-profile row or blank/missing `T24_COMPANY` raises the existing `FAILED_E999` T24 authorize error path. Final to-be: feed both B8 and C9 from `TB_BRANCH_PROFILE.T24_COMPANY` for the disbursing department. | 撥貸 domain owner | owner instruction 2026-06-22; refactor `funcisut24authorize.md:185`-`188`, `199`-`201`; db correction `docs/db-diff/02_tables/TB_BRANCH_PROFILE.md:47`-`50`; schema-diff `docs/build-tasks/schema-diff-findings.md:246`-`249`; new DB `epro-db/out/legacy_schema_reverify_new02_columns.tsv:37`; legacy `EPRO_IS0922.java:590`-`601`, `645`-`652`, `726`-`727`; current backend `SummaryServiceImpl.java:906`-`907`, `1808`-`1822`, `1879`-`1881`, `1909`-`1921`; entity/repo `TBBranchProfileEntity.java:44`-`47`, `TBBranchProfileRepository.java:37`-`47`; dispatch `docs/build-tasks/disbursement-reopen-srs-dispatch.md:17`-`20`, `39`; escalation `docs/disbursement/disbursement-domain-escalations.md:23` |
| TBD-0922-008 | closed | **Segments B / D / H1-H7 reconciled under §5b.** The visible v1.5 artifact defines the shared T24 delimiter/multi-value rules and shared transaction-branch/currency sources but does not expose a contrary per-field delta for the remaining B/D/H rows. Therefore the to-be keeps the legacy/backend field order for D and H1-H7, with H8 covered separately by REF-D6 and B8/C9 carved out to TBD-0922-007. Segment B non-B8 fields remain legacy/backend parity: `COLLATERAL.RIGHT`, borrower T24 no, blank limit product, collateral code, blank collateral type, application no, and transaction branch. Segment D uses `CUSTOMER.SSI`, borrower T24 no, disbursement currency, drawdown account, transaction branch, `ALL`, and application no. Segment H1-H7 uses the two EIR/refinancing transaction types, transaction branch, drawdown account, disbursement currency, fee amount, credit account, and application no. No `REF-Dn` delta is created because no v1.5-visible intentional change was found for these non-B8/C9 rows. | 撥貸 domain / RD | refactor `funcisut24authorize.md:151`-`152`, `185`-`188`; legacy `EPRO_IS0922.java:569`-`603`, `749`-`777`, `1107`-`1172`; current backend `SummaryServiceImpl.java:1847`-`1867`, `1990`-`2015`, `2271`-`2290`, `2293`-`2297`; dispatch `docs/build-tasks/disbursement-reopen-srs-dispatch.md:18`-`21`, `40` |

## Traceability Matrix
| PRD / source | SRS | QA |
|---|---|---|
| Summary initialization | R1, R2 | QA-001, QA-002, QA-003, QA-023, QA-027 |
| Maker submit | R3, R6, R8, R11 | QA-004, QA-005, QA-006, QA-024, QA-025, QA-029 |
| Checker return | R4, R8, R11 | QA-007, QA-008, QA-025 |
| Checker authorize | R5, R6, R7, R8, R10, R11 | QA-009, QA-010, QA-011, QA-012, QA-013, QA-021, QA-024, QA-026, QA-028, QA-029, QA-031 |
| T24 result | R9, R11 | QA-014, QA-015, QA-022 |
| Downloads | R12 | QA-016, QA-017 |
| DB/refactor reconcile | R1-R12 | QA-018, QA-019, QA-020, QA-028, QA-030 |

## NFR
- Security: service-level authorization is mandatory for all mutating endpoints; `TB_API_AUTH` seed rows alone are not sufficient.
- Security: Maker-Checker four-eyes separation is mandatory for authorize; the submit Maker cannot authorize the same case as Checker.
- Data integrity: state transitions must be atomic and auditable; partial Summary/T24/SFTP/history writes are blockers.
- Precision: money/rate values follow DB precision and T24 field policy; silent overflow, hidden truncation, or client-side-only numeric enforcement is a blocker.
- Privacy: logs and error responses must not expose SFTP credentials, full secret paths, stack traces, complete request/response bodies, T24 import payloads, drawdown account, disbursement amount, or real customer data.

## Verification Status
| Gate | Result |
|---|---|
| Mechanical `check-srs-bundle` | PASS on 2026-06-22 for `python scripts/check-srs-bundle.py docs/specs/srs/EPROISU0922`; warning is advisory only (missing Bible-PRD trace file). |
| N-axis A Comprehensive/spec-review | PASS after latest read-only spec-reviewer recheck; no SRS completeness blocker found, and status remains In Review/no self-Approved. |
| N-axis B as-is parity | PASS after latest regression recheck; SRS carries Maker identity preservation, authorize state/four-eyes guards, and DB-D16 drawdown account correction. |
| N-axis C error-code carriage | PASS after latest contract recheck; `EPROIS0922_SELF_APPROVAL` is carried in spec/OpenAPI/QA. |
| N-axis D security/auth | PASS by local implementation evidence on 2026-06-23; status remains In Review/no self-Approved. Backend now locks the case summary row for authorize, enforces state `25`, CAD Checker role `405`, assigned checker/current user from authenticated context, self-approval rejection, stale `CHECK_DATE` named code `EPROIS0922_AUTHORIZE_CHECK`, and quantified outbound request/response logging masking before T24/SFTP side effects proceed. |
| N-axis E DB/refactor reconcile | PASS after latest contract recheck; Summary OpenAPI carries `drawdownAccount`, `bankExpense.facilityFees`, and `bankExpense.refinancingFee`, and schema uses `DRAWDOWN_ACCOINT VARCHAR2(25 BYTE)`. |
| N-axis F money/precision/truncation | PASS after latest regression recheck; no SRS money/precision/truncation blocker remains. |
| N-axis G testability/trace | PASS after latest spec-reviewer recheck; QA-029, QA-030, and QA-031 cover the blocker fixes. |
