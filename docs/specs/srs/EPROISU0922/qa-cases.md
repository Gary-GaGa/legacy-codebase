# QA Cases - EPROISU0922 Individual Summary

| QA ID | covers | Type | Given | When | Then |
|---|---|---|---|---|---|
| QA-001 | R1 | happy | Existing application with disbursement and collateral rows. | POST `epl-info-isu-summary`. | Response includes summary, collateral, other collateral, role, `caseProgress`, CAD Checker `empList`, `t24ResultTime`, and no DB mutation occurs. |
| QA-002 | R1 R2 | error | Request has blank `applicationNo`. | POST `epl-info-isu-summary`. | API returns `COMMON_MSG_ERROR_LON`; no Summary state changes. |
| QA-003 | R2 | edge | Role/state matrix includes 404 at 24, 405 at 25, and T24 states 26/27/C1. | Load Summary page. | Visible actions match server page auth and state; hidden FE controls are not treated as authorization. |
| QA-004 | R3 R6 R8 R11 | happy | CAD Maker role 404, `CASE_PROGRESS=24`, selected checker, all T24 check dates current, and selected checker has zero or more effective `TB_EMP_PROXY` rows. | POST `epl-case-isu-summary-submit`. | `TB_LON_SUMMARY_INFO.CASE_PROGRESS=25`, checker/current handler set, `TB_APP_HISTORY` inserted, and notification task persisted for selected checker plus effective proxy recipients; actual email delivery/retry is not asserted by this endpoint test. |
| QA-005 | R3 R6 R11 | error | Same case but at least one borrower/co-borrower T24 `CHECK_DATE` is stale. | POST `epl-case-isu-summary-submit`. | Request returns `EPROIS0922_CLICK_CHECK` and is rejected before summary/history/notification mutation. |
| QA-006 | R3 R8 | edge | Non-Maker or direct API caller posts submit request. | POST `epl-case-isu-summary-submit`. | BE rejects by role/state guard even if payload is otherwise valid. |
| QA-007 | R4 R8 R11 | happy | CAD Checker role 405, `CASE_PROGRESS=25`, and Maker owner has zero or more effective `TB_EMP_PROXY` rows. | POST `epl-retu-isu-summary`. | `CASE_PROGRESS=24`, checker ownership cleared/restored as specified, `TB_APP_HISTORY` inserted, notification task persisted for Maker plus effective proxy recipients, and no T24 file generated; actual email delivery/retry is not asserted by this endpoint test. |
| QA-008 | R4 R8 | error | Maker or unrelated role posts return request. | POST `epl-retu-isu-summary`. | BE rejects and all Summary/history rows remain unchanged. |
| QA-009 | R5 R6 R7 R8 R11 | happy | CAD Checker role 405, `CASE_PROGRESS=25`, check dates current, T24 file generation and SFTP upload succeed. | POST `epl-case-isu-summary-auth`. | T24 file is generated, uploaded, `TB_MESS_UPLOAD_SFTP_RECORD` inserted, Summary moves to `CASE_PROGRESS=26`, `DISBURSING_DATE` set, history inserted atomically. |
| QA-010 | R5 R6 R11 | error | CAD Checker authorize finds stale borrower/co-borrower T24 `CHECK_DATE`. | POST `epl-case-isu-summary-auth`. | Request returns `EPROIS0922_AUTHORIZE_CHECK`; no T24 file, SFTP record, Summary update, or history insert occurs. |
| QA-011 | R7 R10 | happy | USD disbursement with complete T24 source data and `DISBURSEMENT_AMOUNT`/fee/collateral amounts within `NUMBER(17,2)`. | Call `funcIsuT24Authorize`. | Response returns `t24UpPath`; generated file respects delimiter, record order, `NUMBER(17,2)` precision, and no rate conversion is applied to USD fields. |
| QA-012 | R7 R10 | edge | KHR disbursement with `EX_RATE_BUY` and `EX_RATE_SELL`, plus `LAW_FIRM_90_OTHER_REMARK` <= 16 and related CIF within approved length. | Call `funcIsuT24Authorize`. | Rate fields follow the explicit R10 scale/rounding policy, remark/CIF length is enforced, and no overflow or arbitrary truncation occurs. |
| QA-013 | R7 R10 | error | T24 source field is missing, a money field exceeds `NUMBER(17,2)`, exchange-rate lookup returns no rate, file generation fails, `t24UpPath` is blank, `LAW_FIRM_90_OTHER_REMARK` exceeds 16, or `AGREEMENT_NO` truncation is requested outside approved T24 A31/G7 fields. | Call `funcIsuT24Authorize`. | Generation fails before SFTP upload; exchange-rate lookup failure carries `FAILED_E304` with `EPROIS0921_UI_RAET_FIND_ERROR`; file generation or blank path failure carries `T24_FILE_GENERATION_FAIL`; no `TB_MESS_UPLOAD_SFTP_RECORD` insert occurs. |
| QA-014 | R9 R11 | happy | Case at `CASE_PROGRESS=26`, T24 result reports success. | POST `epl-case-isu-summary-t24-result`. | Response carries `t24DealResultList` with `dataSeq`, `messageCode`, `referenceNo`, `status`, and `errorCode`, plus `updateMap` and `RESULT`; `TB_MESS_RECORD` rows are persisted and `TB_LON_SUMMARY_INFO.CASE_PROGRESS=27`. |
| QA-015 | R9 R11 | error | Case at `CASE_PROGRESS=26`, T24 result reports non-success. | POST `epl-case-isu-summary-t24-result`. | Response carries `t24DealResultList` with `dataSeq`, `messageCode`, `referenceNo`, `status`, and `errorCode`, plus `updateMap` and `RESULT`; non-duplicate `TB_MESS_RECORD` rows are persisted; Summary moves to `CASE_PROGRESS=C1`; `TB_CLO_REASON.REASON_CODE=C10` is inserted; case must not move to success state `27`. |
| QA-016 | R12 | happy | Authorized user requests Summary PDF. | Trigger Summary report download. | Response contains encrypted temp-file path and no Summary state mutation. |
| QA-017 | R12 | error | Report generation fails. | Trigger any Summary/T24 report download. | API returns `COMMON_MSG_DOWNLOAD_FAIL` and does not mutate Summary state. |
| QA-018 | R1 R3 R4 R5 R9 R11 | edge | Compare DB state before and after each mutation. | Run submit, return, authorize, and result flows. | Only the tables listed in schema.sql are mutated for the corresponding flow, with history/audit evidence. |
| QA-019 | R7 R10 | edge | Compare generated T24 file against `funcisut24authorize` v1.5 as the primary source and as-is baseline examples for USD and KHR, including agreement number, A16 loan type, A52 borrower district name, law-firm remark, fees, E21 charge amount, collateral valuations, C12/C13/C20/C26 source fields, and exchange-rate fields. | Generate T24 import file for USD and KHR cases, including null/blank `NAME_OF_BOREY`, `CUBC_RELATED_CIF_NO`, `HOUSING_PROJECT_CODE`, `PRODUCT_CODE=01` with `MTG_PRODUCT_CODE=02`, an `AGREEMENT_NO` longer than 16 characters, main-borrower `RESIDENT_PROVINCE_CODE`/`RESIDENT_DISTRICT_CODE` resolving to a `TB_DISTRICT.DISTRICT_NAME` at the disbursing-date `UPD_DATE`, law-firm amount notes `00/01/02/03/90`, `TB_LOAN_CONDITION_FEE.CBC_FEE`, `DISBURSEMENT_CURRENCY=USD`, `DISBURSEMENT_CURRENCY=KHR` with `EX_RATE_BUY`/`EX_RATE_SELL`, a lower-level unsupported non-USD-non-KHR fixture if test harness can bypass the A-5 valid-currency constraint, a C-row with `TB_DISBUR_COLL.INS_EXPIRY_DATE` present and no `INS_END_DATE`, a C-row with `TB_COLL_VALUE_INFO.SUG_VAL` differing from any collateral-map `SUG_VAL`, `TB_LON_SUMMARY_INFO.DECISION_DATE` differing from `TB_T24_MAIN_BORROWER_INFO.CHECK_DATE`, and two collateral rows whose `TB_COLL_TITLE_DETAIL.TITLE_DEED_NO` values are different by `COLL_DATA_SEQ`. | Version and to-be composition follow v1.5; field delimiter is `!@#`, multi-value delimiter is `|`, records are joined with CRLF, and file writing does not specify an explicit charset so it follows the runtime default like legacy `FileWriter`; null/blank `NAME_OF_BOREY` and `CUBC_RELATED_CIF_NO` become `N/A`, null/blank `HOUSING_PROJECT_CODE` becomes empty string, A16 outputs `NORMAL.LAON`, A31/G7 output only the rightmost 16 characters of `AGREEMENT_NO`, A52 equals the resolved borrower `TB_DISTRICT.DISTRICT_NAME` or empty string if no district row matches, G11/G12 map `00/01/02/03/90` to `Registration Fee`/`De-register fee`/`Due diligent fee`/`Ownership change`/`LAW_FIRM_90_OTHER_REMARK`, E21 for USD equals raw `CBC_FEE`, E21 for KHR equals `CBC_FEE * avg(EX_RATE_BUY, EX_RATE_SELL)` with average scale 4 `HALF_UP` and final scale 0 `HALF_UP`, unsupported non-USD-non-KHR outputs `0` only as documented by-design-unreachable fallback under A-5, C12 equals the current `COLL_DATA_SEQ` row's `TB_COLL_VALUE_INFO.SUG_VAL`, C13 equals `TB_LON_SUMMARY_INFO.DECISION_DATE` formatted `YYYYMMDD`, C20 equals current-collateral `TB_DISBUR_COLL.INS_EXPIRY_DATE` formatted for T24 or empty string if absent, and C26 contains only the current `COLL_DATA_SEQ` row's title deed values joined by `|`; every remaining changed field is classified as carried, refactor delta, fallback-to-legacy, or pending owner confirmation, while R10 precision/length rules stay enforced. |
| QA-020 | R8 R11 | error | Caller has endpoint seed access but invalid role/state for the operation. | Post each mutating endpoint directly. | Service-level guard rejects the operation; `TB_API_AUTH` presence alone does not authorize state transition. |
| QA-021 | R5 R11 | error | SFTP connect fails, or SFTP upload/file transfer fails after T24 file generation. | POST `epl-case-isu-summary-auth`. | Connect failure returns `SFTP_CONNECT_FAIL`; upload/file-transfer failure returns `SFTP_UPLOAD_FAIL`; client response leaks no host/user/credential/full path/stack trace/customer data; no Summary `26`, success history, or `TB_MESS_UPLOAD_SFTP_RECORD` commit remains after rollback/consistency handling. |
| QA-022 | R9 R11 | edge | Case has existing `TB_MESS_RECORD` rows for the same application/result identity, including `dataSeq` and message code `LD.LOANS.AND.DEPOSITS`. | POST `epl-case-isu-summary-t24-result` again with the same result source. | API returns the existing/current result view with `dataSeq` and message codes up to DB `VARCHAR2(35)`; no duplicate `TB_MESS_RECORD` rows are inserted; Summary state remains consistent with the approved result rule. |
| QA-023 | R1 R2 | edge | Case is not at `CASE_PROGRESS=26`, or `DISBURSING_DATE` is blank/null. | POST `epl-info-isu-summary`. | Summary initialization succeeds, does not parse a blank date, and returns `t24ResultTime=N`; only a `CASE_PROGRESS=26` case with `DISBURSING_DATE` older than 10 minutes returns `t24ResultTime=Y`. |
| QA-024 | R3 R5 R6 R11 | edge | Main borrower or any co-borrower `CHECK_DATE` is not equal to the system calendar date, including cross-day or holiday/weekend scenarios. | POST submit and authorize endpoints separately. | Submit returns `EPROIS0922_CLICK_CHECK`; authorize returns `EPROIS0922_AUTHORIZE_CHECK`; no business-day exception is applied; no Summary/history/notification/T24/SFTP mutation occurs. |
| QA-025 | R3 R4 R11 | error | Submit or return reaches notification-task creation, but `TB_NOTIFICATION_INFO` or the current notification mechanism rejects the task. | POST `epl-case-isu-summary-submit` and `epl-retu-isu-summary` separately. | The endpoint returns the existing submit/return failure category, rolls back Summary and `TB_APP_HISTORY`, and leaves no partial notification task; simulated async email delivery failure after a task is persisted is handled outside this endpoint and does not roll back state. |
| QA-026 | R7 R10 | edge | G/H fee-transfer fixture where `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY` and `TB_MAIN_BORROWER_ACC.CURRENCY` differ, plus USD and KHR disbursement cases with `EX_RATE_BUY` populated. | Generate T24 import file with law-firm fee and EIR fee rows. | G4/H4 equal `TB_DISBUR_DATE.DISBURSEMENT_CURRENCY`; G10/H8 branch on `DISBURSEMENT_CURRENCY`, not account currency. USD disbursement outputs blank G10/H8 even if account currency is KHR; KHR disbursement outputs `EX_RATE_BUY` even if account currency is USD; non-USD-non-KHR remains by-design-unreachable under A-5 and is not promoted to a supported path. |
| QA-027 | R1 R2 | happy | Maker role `404`, `CASE_PROGRESS=24`, and employee master/auth data includes active role-`405` users, inactive role-`405` users, and active non-405 users. | POST `epl-info-isu-summary`. | Summary initialization returns populated Summary/Collateral/Other Collateral sections for AC-001 and `empList` contains only active role-`405` CAD Checker employees; inactive employees and non-405 roles are excluded and no DB mutation occurs. |
| QA-028 | R7 R10 | edge | T24 residual segment fixture with at least one collateral row, a drawdown account whose `IS_SSI` is not `Y`, non-zero facility/refinancing fees, and branch-profile cases with present, blank, and missing-row `T24_COMPANY`. | Generate T24 import file and inspect B/D/H records by field position; repeat with blank `T24_COMPANY` and with no matching branch-profile row. | Segment B non-B8 fields output `COLLATERAL.RIGHT`, borrower T24 no, blank limit product, collateral code, blank collateral type, application no, and transaction branch from `T24_BRANCH_CODE`; B8 and C9 output `TB_BRANCH_PROFILE.T24_COMPANY` when present. Missing branch-profile row or blank/missing `T24_COMPANY` raises the existing `FAILED_E999` T24 authorize path instead of producing empty B8/C9. Owner confirmed no separate T24 contract override under closed `TBD-0922-007`. Segment D outputs `CUSTOMER.SSI`, borrower T24 no, disbursement currency, drawdown account, transaction branch, `ALL`, and application no. Segment H1-H7 outputs `FT.LOAN.EIR.52153` or `FT.LOAN.EIR.52155`, transaction branch, drawdown account, disbursement currency, fee amount, credit account `PL52153`/`PL52155`, and application no; H8 follows QA-026/REF-D6. |
| QA-029 | R3 R5 R8 R11 | error | Maker employee `M001` submits the case from `CASE_PROGRESS=24` to `25`; test data also grants `M001` checker-role endpoint access or role `405` to simulate self-approval pressure. | Authenticate as `M001` and POST `epl-case-isu-summary-auth` for the same application. | BE returns HTTP 400 code `EPROIS0922_SELF_APPROVAL`; no T24 file is generated, no SFTP upload occurs, `TB_MESS_UPLOAD_SFTP_RECORD` and success `TB_APP_HISTORY` are not inserted, and `TB_LON_SUMMARY_INFO.CASE_PROGRESS` remains `25`. |
| QA-030 | R1 R2 R8 | edge | Seed otherwise identical individual Summary cases with `TB_LON_SUMMARY_INFO.SECURE_ATTRIBUTE='S'` and `'U'`, and attempt to spoof `secureAttribute` in the request body if a client field exists. | Call summary query, submit, return, and authorize through normal service-level authorization. | EPROISU0922 behavior does not branch inside the page by S/U; BE reads server-side `SECURE_ATTRIBUTE` only as routing/audit context, ignores client spoofing, and still enforces the same Summary state, role, T24, and four-eyes rules. |
| QA-031 | R5 R7 R11 | error | Enable test log capture for authorize and seed a case whose request, T24 import data, `DRAWDOWN_ACCOINT`, and `DISBURSEMENT_AMOUNT` would be visible if raw payload logging were enabled. | POST `epl-case-isu-summary-auth` through success and failure paths, including a required-source-data failure that returns `FAILED_E999`. | Application, integration, and error logs do not contain a complete request/response body, complete T24 import payload, unmasked drawdown account, or unmasked disbursement amount; logs may contain correlation id, endpoint id, safe result code, and generated file name only. |
