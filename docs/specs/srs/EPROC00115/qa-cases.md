# QA Cases - EPROC00115 Borrower Group Exposure

| QA ID | Covers | Type | Given | When | Then | Test-ready |
|---|---|---|---|---|---|---|
| QA-001 | covers: R1 | happy | User has `TB_API_AUTH` access for `epl-sele-c0-borrower-group-exposure`; common-field option seeds exist for `FACILITY_TYPE`, `LOAN_LIMIT_TYPE`, `CCY`, and `TITLE_DEED_TYPE`. | POST `epl-sele-c0-borrower-group-exposure` with `langType`. | EPRO response envelope has `code=0000`; `data.facilityList`, `data.loanLimitTypeList`, `data.ccyList`, and `data.titleDeedTypeList` are non-null; DB exposure rows and checkpoints are unchanged. | Yes |
| QA-002 | covers: R1 | error | Same user sends blank `langType`. | POST `epl-sele-c0-borrower-group-exposure`. | EPRO response envelope carries `E102` or equivalent field validation; no DB mutation occurs. | Yes |
| QA-003 | covers: R2 | happy | `TB_LON_SUMMARY_INFO` and CS/CU checkpoint row exist; `TB_GROUP_EXPOSURE` has two rows with `DATA_SEQ=2` and `DATA_SEQ=1`. | POST `epl-info-c0-borrower-group-exposure` with `applicationNo` and `isQuery=false`. | EPRO response envelope has `code=0000`; `data.facilitiesList` is ordered by `DATA_SEQ` 1 then 2; `data.facility` equals first-row `FACILITY_VALUE`; DB is unchanged. | Yes |
| QA-004 | covers: R2 | edge | Summary/checkpoint exists; no `TB_GROUP_EXPOSURE` rows exist for the case. | POST `epl-info-c0-borrower-group-exposure`. | EPRO response envelope has `code=0000`, `data.facility=""`, and `data.facilitiesList=[]`; the API does not insert an empty row. | Yes |
| QA-005 | covers: R3 | happy | Existing case has old rows with mixed `FACILITY_VALUE`. Request has `facility="01"` and two detail rows. | POST `epl-save-c0-borrower-group-exposure` with `isFinish=false`. | All inserted `TB_GROUP_EXPOSURE` rows for that `APPLICATION_NO` have `FACILITY_VALUE='01'`; response envelope has `code=0000`, `message=Success`, and empty `data`. | Yes |
| QA-006 | covers: R4 | edge | UI displays one detail row. | User clicks delete on the final row. | UI blocks deletion with the one-row message and does not call `epl-save-c0-borrower-group-exposure`; existing DB rows are unchanged. | Yes |
| QA-007 | covers: R5 | happy | UI has rows in USD and KHR only, including LCC and Outstanding values with decimals. | User edits amount/currency fields before save. | Visible totals for USD/KHR equal the row sums used in the pending save payload; no calculate endpoint is called. | Yes |
| QA-008 | covers: R6 | happy | Case is editable; request has partial row data valid for draft Save. | POST `epl-save-c0-borrower-group-exposure` with `isFinish=false`. | Save succeeds; `TB_GROUP_EXPOSURE` is replaced; `TB_CHECK_POINTS_CS.EPROC00115` or `TB_CHECK_POINTS_CU.EPROC00115` is `Y`; response envelope has `code=0000`, `message=Success`, and empty `data`. | Yes |
| QA-009 | covers: R6 | happy | Case is editable; request has all non-pending required Finished fields and valid date format. | POST `epl-save-c0-borrower-group-exposure` with `isFinish=true`. | Save succeeds; selected checkpoint column becomes `N`; response envelope has `code=0000`, `message=Success`, and empty `data`. | Yes |
| QA-010 | covers: R7 | happy | Existing DB has three old exposure rows. Request has two rows. | POST `epl-save-c0-borrower-group-exposure`. | DB has exactly two rows after commit; `DATA_SEQ` is 1 and 2 in request order; old row 3 is gone. | Yes |
| QA-011 | covers: R7 | rollback | Existing DB has one exposure row and checkpoint `Y`. Force insert failure after delete or checkpoint update failure after insert. | POST `epl-save-c0-borrower-group-exposure`. | Response carries `COMMON_MSG_SAVE_FAIL` or platform DB error; the original exposure row and checkpoint value remain unchanged after rollback. | Yes |
| QA-012 | covers: R7 | security | Request-level `applicationNo` is `APP-A`; a tampered row payload contains any nested or stale application number for `APP-B` if the client model can carry it. | POST `epl-save-c0-borrower-group-exposure`. | Only rows for `APP-A` are deleted/inserted; no `APP-B` row is inserted, updated, or deleted. | Yes |
| QA-013 | covers: R8 | happy | `TB_LON_SUMMARY_INFO.LON_ATTRIBUTE + SECURE_ATTRIBUTE = CS`; CS and CU checkpoint rows both exist for visibility. | POST Finished save. | `TB_CHECK_POINTS_CS.EPROC00115` changes according to `isFinish`; `TB_CHECK_POINTS_CU.EPROC00115` is unchanged. | Yes |
| QA-014 | covers: R8 | happy | `TB_LON_SUMMARY_INFO.LON_ATTRIBUTE + SECURE_ATTRIBUTE` routes to CU. | POST Finished save. | `TB_CHECK_POINTS_CU.EPROC00115` changes according to `isFinish`; `TB_CHECK_POINTS_CS.EPROC00115` is unchanged. | Yes |
| QA-015 | covers: R9 | security | Auth seed rows exist but the caller lacks a role allowed by the endpoint `ROLE` allow-list. | POST each EPROC00115 endpoint directly. | Response is 401 with `E405` or access-denied message; `TB_GROUP_EXPOSURE` and checkpoint tables are unchanged. | Yes |
| QA-016 | covers: R9 | regression | Save succeeds for a case used by score/report modules. | Re-query report/score consumers and direct SQL after save. | Consumers still read rows by `APPLICATION_NO` and `DATA_SEQ`; `LCC_AMOUNT_CUR/LCC_AMOUNT`, `OUTSTAND_AMOUNT_CUR/OUTSTAND_AMOUNT`, and collateral fields retain meaning and precision. | Yes |
| QA-017 | covers: R9 | error | `applicationNo` is blank, summary row is missing, or query limit is forced. | POST info/save as applicable. | EPRO response envelope carries `E102` or `COMMON_MSG_ERROR_LON` for blank request; missing summary returns `MSG_DATA_NOT_FOUND` or `E126` per branch; over-limit returns `MSG_OVER_COUNT_LIMIT`; no DB mutation occurs. | Yes |
| QA-018 | covers: R9 | error | Repository query exception is forced for select or info. | POST select/info endpoint. | Response carries `MSG_QUERY_FAIL`; no exposure or checkpoint mutation occurs; logs omit full sensitive payload. | Yes |
| QA-P01 @PENDING | covers: R10 | pending | `loanLimitType` is `1`, blank, `2`, and another nonblank value in separate Finished requests. | POST `epl-save-c0-borrower-group-exposure` after SA/RD decision. | Expected Outstanding/Maturity requiredness follows the approved decision; until then this cannot be used as a pass/fail case. | Blocked by PENDING-EPROC00115-LOAN-LIMIT-TYPE-SEMANTICS |
| QA-P02 @PENDING | covers: R11 | pending | `CCY` includes a value other than USD/KHR. | Edit totals and save/re-query after PM/SA/RD decision. | Query totals and live totals follow the approved third-currency rule without divergence. | Blocked by PENDING-EPROC00115-THIRD-CURRENCY-TOTALING |
| QA-P03 @PENDING | covers: R12 | pending | UI has row count exactly at the approved maximum. | User clicks add row. | Add is blocked with the approved limit message, or allowed if below limit. | Blocked by PENDING-EPROC00115-ROW-LIMIT |
| QA-P04 @PENDING | covers: R13 | pending | Existing DB row has `LOAN_LIMIT_TYPE` null. | POST info and render page after SA/RD decision. | Approved remediation is visible: carry legacy parent-incomplete flag, show warning, or intentionally no-op with documented decision. | Blocked by PENDING-EPROC00115-BLANK-LOAN-LIMIT-FLAG |
| QA-P05 @PENDING | covers: R14 | pending | Renewal/change RC case equivalent to legacy `EPROC0_0215` is available. | POST Finished save after checkpoint mapping decision. | Approved checkpoint target is updated exactly once; non-target checkpoint tables stay unchanged. | Blocked by PENDING-EPROC00115-RC-0215-CHECKPOINT |
| QA-P06 @PENDING | covers: R15 | pending | Old RC case equivalent to legacy `${attrMap.isOld}=true` is available. | Render page and try direct Finished call after PM/SA decision. | Finished is hidden/blocked or allowed according to approved old-case rule; parent done state follows the approved rule. | Blocked by PENDING-EPROC00115-RC-OLD-CASE-FINISH |
| QA-P07 @PENDING | covers: R16 | pending | Report output includes rows where `COLLATERAL_CUR` differs from `OUTSTAND_AMOUNT_CUR`. | Generate CS0180/CS0181/CU0180/CU0181 equivalent reports after SA/RD decision. | Collateral currency displays according to approved defect/fix decision. | Blocked by PENDING-EPROC00115-REPORT-COLLATERAL-CURRENCY |
| QA-P08 @PENDING | covers: R17 | pending | UI renders the facility/exposure field. | Inspect label/i18n key after PM/SA decision. | Display label matches approved wording and API still sends `facility`. | Blocked by PENDING-EPROC00115-FACILITY-LABEL |
| QA-P09 @PENDING | covers: R18 | pending | Final route/menu seed data is available. | Open the page from menu and inspect page/doc identifiers. | Menu route, document references, and function id align with approved naming. | Blocked by PENDING-EPROC00115-ROUTE-MENU-NAMING |
| QA-P10 @PENDING | covers: R19 | pending | Save request omits `facilitiesList`. | POST `epl-save-c0-borrower-group-exposure` after RD closes the DTO validation gap. | Controlled validation response is returned and no delete/insert/checkpoint mutation occurs. | Blocked by PENDING-EPROC00115-FACILITIES-LIST-BE-VALIDATION |
| QA-P11 @PENDING | covers: R20 | pending | Caller has API auth but lacks case ownership or edit permission according to the approved backend policy. | POST `epl-save-c0-borrower-group-exposure` directly after RD/Security closes the guard decision. | Save is rejected before delete/insert/checkpoint updates; existing exposure and checkpoint rows remain unchanged. | Blocked by PENDING-EPROC00115-SAVE-SERVICE-AUTHZ |
| QA-P12 @PENDING | covers: R21 | pending | Amount/rate/LTV values have 13-15 integer digits and at most 2 decimals, matching the backend/DB contract. | Enter values in FE and submit after RD/SA closes the numeric limit decision. | FE and BE either accept the 15-integer-digit contract without truncation/rounding, or both reject according to an approved narrower limit. | Blocked by PENDING-EPROC00115-FE-NUMERIC-INTEGER-LIMIT |
