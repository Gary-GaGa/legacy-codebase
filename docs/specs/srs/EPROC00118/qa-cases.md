# QA Cases - EPROC00118 Corporate Scorecard

| QA ID | Covers | Type | Given | When | Then | Test-ready |
|---|---|---|---|---|---|---|
| QA-001 | covers: R1 | happy | Existing `APPLICATION_NO` has `TB_LON_SUMMARY_INFO`, `TB_MAIN_BORROWER_INFO_CORP`, checkpoint, and `TB_CORP_SCRCARD` rows. | POST `epl-info-c0-corporateScorecard`. | Response includes `mainBorrowerName`, `checkpointStatus`, AO/CR maps, comment fields, and current scorecard values. | Yes |
| QA-002 | covers: R1 | happy | Existing `APPLICATION_NO` has loan summary, main borrower, and checkpoint rows but no `TB_CORP_SCRCARD` row. | POST `epl-info-c0-corporateScorecard`. | Response includes `mainBorrowerName`, `checkpointStatus`, empty AO/CR maps for insert path, and option data is still obtainable. | Yes |
| QA-002A | covers: R1 | edge | Existing scorecard row has null optional CR comment/comment date. | POST `epl-info-c0-corporateScorecard`. | Response still returns valid AO/CR maps and nullable comment fields without failing query. | Yes |
| QA-003 | covers: R1 | error | `applicationNo` is blank. | POST `epl-info-c0-corporateScorecard`. | Response is 400 with `COMMON_MSG_ERROR_LON`; DB remains unchanged. | Yes |
| QA-004 | covers: R1 | error | `APPLICATION_NO` has no loan summary row. | POST `epl-info-c0-corporateScorecard`. | Response is 404 with `MSG_DATA_NOT_FOUND`; DB remains unchanged. | Yes |
| QA-005 | covers: R1 | error | Query exceeds system limit. | POST `epl-info-c0-corporateScorecard`. | Response is 429 with `MSG_OVER_COUNT_LIMIT`. | Yes |
| QA-006 | covers: R1 | error | Repository/query exception is forced. | POST `epl-info-c0-corporateScorecard`. | Response carries `MSG_QUERY_FAIL`; DB remains unchanged. | Yes |
| QA-007 | covers: R2 | happy | Approved `TB_SCORE_CARD_PARAM_DETAIL` seed rows exist for all select-backed items and scoring vars. | Query page data and option lists. | Each select item has options from the approved DB seed baseline; AO/CR code fields accept max length 2; labels/scores/risk levels match the exported seed. | Yes |
| QA-008 | covers: R2 | happy | Page renders 23 corporate scorecard items. | Query page data. | DTO shape includes code/input/risk/date/comment fields required for all 23 items, including numeric-only items. | Yes |
| QA-009 | covers: R2 | edge | CR comment is exactly 3000 characters, then 3001 characters. | Save and re-query. | Exactly 3000 characters are stored and returned; 3001 characters are rejected by BE validation before DB write. | Yes |
| QA-009A | covers: R2, R3, R5 | error | User lacks authorization for read/calc endpoint APIs. | POST each of `epl-sele-c0-corporateScorecard-list`, `epl-info-c0-corporateScorecard`, and `epl-calc-c0-corporateScorecard`. | Each response is 401 with `ACCESS_DENIED`; no scorecard, summary, or checkpoint data is mutated. Missing `TB_API_AUTH` seed before testing deployment is a blocker. | Blocked by DB seed evidence |
| QA-009B | covers: R2 | error | Option-list query fails. | POST `epl-sele-c0-corporateScorecard-list`. | Response carries `MSG_QUERY_FAIL`; no scorecard data is mutated. | Yes |
| QA-010 | covers: R3, R8 | happy | Non-default scorecard contains all required code/input values and approved seed rows selected by `MAX(ST_DATE <= APP_DATE)`; `END_DATE` is present only as seed metadata. | POST `epl-calc-c0-corporateScorecard`. | Response `riskLevel` matches the expected range from `TB_SCORE_CARD_PARAM_DETAIL`; `scoreDatetime` is generated from an explicit Asia/Taipei / UTC+8 clock and matches `dd/MM/yyyy HH:mm:ss`; calc does not write `TB_CORP_SCRCARD`, `TB_LON_SUMMARY_INFO`, or checkpoint tables. | Blocked by RD timezone fix/test evidence |
| QA-010A | covers: R3 | edge | PRD/legacy consumer expects `totalScore` in calc response. | POST `epl-calc-c0-corporateScorecard`. | Response includes `riskLevel` and `scoreDatetime` only and does not expose `totalScore`; BE still computes total score internally to select the correct risk level. | Yes |
| QA-011 | covers: R3 | error | `funcGetRate` returns a deterministic module-level validation message. | POST `epl-calc-c0-corporateScorecard`. | Response carries that exact module message; no DB write occurs. | Yes |
| QA-011A | covers: R3 | error | Score/risk lookup query fails. | POST `epl-calc-c0-corporateScorecard`. | Response carries `MSG_QUERY_FAIL`; no DB write occurs. | Yes |
| QA-011B | covers: R3 | error | `TB_SCORE_CARD_PARAM_DETAIL.SCORE` contains a nonnumeric seed value for a selected option/range. | POST `epl-calc-c0-corporateScorecard`. | Response carries `MSG_QUERY_FAIL`, safe logs identify the seed issue without full payload, and no DB write occurs; it must not silently contribute zero. | Blocked by RD fix/test evidence |
| QA-012 | covers: R3 | error | Rate calculation throws a general exception. | POST `epl-calc-c0-corporateScorecard`. | Response carries `COMMON_MSG_RATE_FAIL`; no DB write occurs. | Yes |
| QA-013 | covers: R3, R8 | edge | Numeric value is exactly at a `LOW_RANGE` boundary and then exactly at an `UP_RANGE` boundary. | Calculate score. | `LOW_RANGE <= input < UP_RANGE` is honored; `UP_RANGE` value does not match the lower range. | Yes |
| QA-014 | covers: R4 | happy | AO sets `loanDefDayFlag=Y`, accepts `EPROI00118_MSG_DATE` prompt, and finishes the page. | POST `epl-save-c0-corporateScorecard` with `isFinish=true`. | `AO_RISK_LEVEL=Default`, `AO_SCORE=-1`; `CR_RISK_LEVEL=Default`, `CR_SCORE=-1`, and `CR_DATE` equals the AO rating date without requiring CR re-rating first. | Yes |
| QA-015 | covers: R4 | happy | CR sets `loanDefDayFlag=Y`. | POST `epl-save-c0-corporateScorecard`. | `CR_RISK_LEVEL=Default`, `CR_SCORE=-1`, and CR rating date is stored. | Yes |
| QA-016 | covers: R4 | error | Current role default flag is blank. | POST `epl-save-c0-corporateScorecard`. | Response is 400 with `EPROI00118_MSG_ERROR_FLG`; DB remains unchanged. | Yes |
| QA-017 | covers: R4 | edge | User changes Default from Yes back to No after Rate. | Save or Finished is attempted. | Risk/date/rate state is cleared and `COMMON_MSG_RATE` blocks Finished until Rate is rerun; BE rejects bypassed Finished calls before DB write. | Yes |
| QA-017D | covers: R4, R8 | edge | User changes Default from Yes back to No and re-enters numeric fields. | UI validates numeric input length/decimal. | UI/BE applies the same SRS numeric patterns and DB precision after initial load and after Default Y-to-N reset; legacy path-specific display differences are not preserved. | Yes |
| QA-017C | covers: R4 | error | Default date lookup fails while handling `loanDefDayFlag=Y`. | POST `epl-save-c0-corporateScorecard`. | Response carries `COMMON_MSG_RATE_FAIL`; scorecard, summary, and checkpoint remain unchanged. | Yes |
| QA-017A | covers: R4, R6 | error | `applicationNo` is blank on Save/Finished. | POST `epl-save-c0-corporateScorecard`. | Response is 400 with `COMMON_MSG_ERROR_LON`; no scorecard, summary, or checkpoint update occurs. | Yes |
| QA-017B | covers: R4, R5 | error | Required scorecard selection is blank before request submission. | User attempts Rate/Save/Finished. | UI/validation raises `COMMON_UI_PLEASE_SELECT`; no mutating API request is sent, and BE returns 400 if bypassed. | Yes |
| QA-018 | covers: R5 | happy | Role `001` or `002` opens the page. | Query page data. | AO fields/actions are editable; CR fields/comment are readonly. | Yes |
| QA-019 | covers: R5 | happy | Role `102` or `103` opens the page. | Query page data. | CR fields/comment/actions are editable; AO fields are readonly. | Yes |
| QA-020 | covers: R5 | edge | Non-editor role opens query mode. | Query page data. | Response is readonly; no mutating UI state is enabled. | Yes |
| QA-020A | covers: R5 | edge | Role `003` opens the page. | Query page data and attempt AO-side Save/Finished if UI/API is bypassed. | AO fields/actions are readonly and mutating save is rejected; role `003` is not treated as an AO editor for this page. | Yes |
| QA-021 | covers: R5 | error | Non-AO/non-CR role calls Save/Finished directly. | POST `epl-save-c0-corporateScorecard`. | Response is 401 from TB_API_AUTH or explicit BE role rejection; no scorecard, summary, or checkpoint update occurs. Missing `TB_API_AUTH` seed or missing service-level reject before testing deployment is a blocker. | Blocked by DB seed/RD guard evidence |
| QA-021A | covers: R5, R6 | error | AO role submits valid AO values plus tampered CR-side map, or CR role submits valid CR values plus tampered AO-side map. | POST `epl-save-c0-corporateScorecard`. | BE rejects the request with the platform role-authorization response; no scorecard, summary, or checkpoint data changes. AO-to-CR copy/reset/default side effects are allowed only when produced by server-side rules. | Blocked by RD guard evidence |
| QA-022 | covers: R6 | happy | AO non-default save succeeds. | POST `epl-save-c0-corporateScorecard` with `isFinish=false`. | `TB_CORP_SCRCARD` updates; AO score is stored; CR score/risk/date reset rules follow PRD; `CR_SCORE_CARD_COMPLETED` is reset to `NN`. | Yes |
| QA-022A | covers: R6 | edge | Save is repeated with unchanged non-default values, including renewal/change branch. | POST `epl-save-c0-corporateScorecard` with `isFinish=false` twice. | Second save updates the existing row idempotently by one `APPLICATION_NO` condition; no duplicate row is inserted and the legacy RC duplicate where-field detail is not preserved. | Yes |
| QA-022B | covers: R6 | regression | AO non-default Finished has `AO_AR_CODE`, `AO_AR_SCR`, `AO_INVENTORY_CODE`, and `AO_INVENTORY_SCR` populated. | POST `epl-save-c0-corporateScorecard` with `isFinish=true` and re-query DB. | CR-side copied fields include `CR_AR_CODE`, `CR_AR_SCR`, `CR_INVENTORY_CODE`, and `CR_INVENTORY_SCR`; code/score pairs remain consistent and the legacy missing-score omission is not preserved. | Yes |
| QA-023 | covers: R6 | happy | CR Finished succeeds from prior `CR_SCORE_CARD_COMPLETED=NY`. | POST `epl-save-c0-corporateScorecard` with `isFinish=true`. | `CR_SCORE_CARD_COMPLETED` becomes `YY`: first character becomes `Y` and second character is preserved. | Yes |
| QA-024 | covers: R6 | happy | Save succeeds. | POST `epl-save-c0-corporateScorecard`. | Response carries `COMMON_MSG_SAVE_SUCCESS`; response body does not promise `checkpointTable` or `isAllTabsCheck`. | Yes |
| QA-025 | covers: R6 | error | Force summary update failure after scorecard update. | POST `epl-save-c0-corporateScorecard`. | Response carries `COMMON_MSG_SAVE_FAIL`; `TB_CORP_SCRCARD`, `TB_LON_SUMMARY_INFO`, and checkpoint updates all roll back. | Yes |
| QA-025A | covers: R6, R7 | error | Force checkpoint update failure after scorecard and summary updates. | POST `epl-save-c0-corporateScorecard`. | Response carries `COMMON_MSG_SAVE_FAIL`; scorecard, summary, and checkpoint changes all roll back in one transaction. | Yes |
| QA-026 | covers: R7 | happy | Corporate secured case has `LON_ATTRIBUTE + SECURE_ATTRIBUTE = CS`. | Finished save succeeds. | `TB_CHECK_POINTS_CS.EPROC00118` updates; parent frame is not marked done unless all-tabs check passes. | Yes |
| QA-027 | covers: R7 | happy | Corporate unsecured/CU case routes to CU checkpoint. | Finished save succeeds. | `TB_CHECK_POINTS_CU.EPROC00118` updates; CS table is unchanged. | Yes |
| QA-028 | covers: R7 | edge | Other required tabs are incomplete. | Finished save succeeds for this tab. | Parent page remains not done even though this page checkpoint updates. | Yes |
| QA-029 | covers: R7 | edge | Renewal/change branch equivalent to legacy `EPROC0_0218`. | Finished save succeeds. | The active new checkpoint mapping reuses `TB_CHECK_POINTS_CS.EPROC00118` or `TB_CHECK_POINTS_CU.EPROC00118` by the CS/CU case branch; no RC-specific checkpoint column/table is written. | Yes |
| QA-030 | covers: R6, R7 | regression | AO re-saves a previously completed case. | POST save endpoint. | `CR_SCORE_CARD_COMPLETED` is reset to `NN`, and parent completion must be rechecked. | Yes |
| QA-030A | covers: R7 | regression | CU branch returns/resets after corporate scorecard completion. | Trigger the return/reset path and re-query checkpoint state. | CU checkpoint state is updated on `TB_CHECK_POINTS_CU.EPROC00118`; CS is not the only branch cleared. | Yes |
| QA-031 | covers: R8 | edge | UI submits integer `curRatio=80`. | Save and query again. | DB stores `0.80` in `*_CUR_RATIO`; query response displays `80`. | Yes |
| QA-031A | covers: R8 | error | UI or API submits decimal `curRatio=80.5`. | POST calc/save. | BE validation rejects the request before DB write; no silent truncation to `80` or `0.80` occurs. | Yes |
| QA-032 | covers: R8 | error | `debtRatio` exceeds `NUMBER(6,2)` or declared max length. | POST calc/save. | Request is rejected before DB write; no silent truncation occurs. | Yes |
| QA-032A | covers: R8 | error | `totalAsset` or `totalLoanAmt` exceeds `NUMBER(19,2)` or declared max length. | POST calc/save. | Request is rejected before DB write; no silent truncation or rounding occurs. | Yes |
| QA-033 | covers: R8 | error | A derived item `_SCR` would exceed `999`, or `totalScore` / `AO_SCORE` / `CR_SCORE` would exceed `NUMBER(7,2)`. | POST calc/save. | BE rejects before DB write with validation/rate failure, logs the bounded overflow reason safely, and does not silently round/truncate. | Yes |
| QA-034 | covers: R8 | edge | `TB_SCORE_CARD_PARAM_DETAIL.LOW_RANGE/UP_RANGE` includes decimal boundary values. | Calculate score. | Repository/range comparison preserves DB range precision even though option-list API exposes only `ValueLabel`. | Yes |
| QA-035 | covers: R2, R4 | linkage | `MAX_OVERDUE_CODE = 01`. | User changes late-transaction field. | `TRANS_12MON_CODE` is auto-set to `01` and disabled through the canonical late-transaction linkage. Legacy non-existent DOM ids `CR_TRA_LST_MON_CODE*` / `CR_MAX_DUE_CODE*` are not required. | Yes |
| QA-036 | covers: R2, R4 | linkage | `TRANS_12MON_CODE = 05`. | User changes late-transaction field. | `MAX_OVERDUE_CODE` is auto-set to `07` and disabled through the canonical late-transaction linkage. Legacy non-existent DOM ids `CR_TRA_LST_MON_CODE*` / `CR_MAX_DUE_CODE*` are not required. | Yes |
