# QA Cases - EPROZ00800 Revised Item

| QA ID | Covers | Type | Given | When | Then | Test-ready |
|---|---|---|---|---|---|---|
| QA-001 | covers: R1 | happy | User opens EPROZ00800 for a supported renewal/change-condition case with edit permission. | GET `/api/eproposal/eproz00800/prompt`. | Success envelope includes `code`, `message`, and `data`; `data` includes page attributes, application number context, editability, case-family predicates, and no mutation occurs. | Yes |
| QA-002 | covers: R1, R16 | error | Test fixture uses an otherwise reachable case whose backend-owned `TB_LON_SUMMARY_INFO.LON_TYPE_CODE` is not `03` or `04`. | GET `/api/eproposal/eproz00800/prompt`, then directly call query/save APIs with the same `applicationNo`. | Prompt/tab-control does not expose EPROZ00800; direct query/save returns HTTP 403 `ACCESS_DENIED` or the platform canonical forbidden-page envelope before Revised Item data is returned or DB is mutated. | Yes |
| QA-003 | covers: R1 | error | Init attribute lookup fails. | GET `/api/eproposal/eproz00800/prompt`. | Error envelope includes `code=MSG_INITIAL_FAIL`, `message`, and nullable/error `data`; the form does not render as editable. | Yes |
| QA-004 | covers: R2 | happy | `TB_LON_SUMMARY_INFO` and `TB_REVISED_ITEM` exist for the application. | GET `epl-case-query-reviseditem` with `applicationNo`. | Success envelope includes `code`, `message`, and `data`; `data` returns `revisedType`, `revisedTypeSize`, `lonTypeCode`, `secureAttribute`, backend-computed `isCorporateUnsecured`, item1-item14, `reasonMemo`, and `updDate` matching DB. | Yes |
| QA-005 | covers: R2 | edge | Case exists but has no `TB_REVISED_ITEM` row. | GET `epl-case-query-reviseditem`. | Item fields return blank values for display while execute still must send Y/N. | Yes |
| QA-006 | covers: R6 | regression | Test data includes both CU (`LON_ATTRIBUTE='C'`, `SECURE_ATTRIBUTE='U'`) and IU (`LON_ATTRIBUTE='I'`, `SECURE_ATTRIBUTE='U'`) cases. | Query and render the form. | CU returns `isCorporateUnsecured=true` and forces item3 unchecked and non-editable. IU returns `isCorporateUnsecured=false` and must not be locked solely because `SECURE_ATTRIBUTE='U'`. | Yes |
| QA-007 | covers: R7 | error | User has no edit permission but can reach the page URL, or lacks `epl-auth-page-column` `canEditList` / button permission for Revised Item. | Render page and directly call POST `epl-case-insert-reviseditem`. | FE disables/hides save/finish and fields from page-column auth, and backend rejects direct mutation through backend-owned page/edit authorization before DB writes. Passing QA requires config-mapping and service-level enforcement evidence; API-level `TB_API_AUTH` alone is insufficient. | Yes |
| QA-008 | covers: R3 | happy | `REVISED_ITEM` code-table/reference data contains all 14 options. | Query/render the page. | UI labels match the approved item1-item14 names and item order. | Yes |
| QA-009 | covers: R4 | error | User submits no selected item. | POST `epl-case-insert-reviseditem`. | Error envelope returns HTTP 400 with `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; request is rejected before persistence and `TB_REVISED_ITEM` is unchanged. | Yes |
| QA-010 | covers: R4 | error | `itemMap.reasonMemo` is blank or whitespace only. | POST `epl-case-insert-reviseditem`. | Error envelope returns HTTP 400 with `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; no DB write occurs. | Yes |
| QA-011 | covers: R4 | edge | `reasonMemo` is exactly 3000 characters. | POST `epl-case-insert-reviseditem`. | Save accepts the value and stores the trimmed 3000 characters without truncation. | Yes |
| QA-012 | covers: R4 | error | `reasonMemo` is 3001 characters. | POST `epl-case-insert-reviseditem`. | Error envelope returns HTTP 400 with `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; no silent truncation into the wider DB column occurs. | Yes |
| QA-013 | covers: R5 | happy | `LON_TYPE_CODE=03`. | Query/render the page and attempt to toggle item1. | item1 remains Y and non-editable; execute persists item1=Y. | Yes |
| QA-014 | covers: R8, R12 | happy | Existing item2=Y and user changes item2=N. | Save after confirming the warning. | Warning appears before save; backend uses DB comparison and restores/deletes guarantor data according to R12.1. | Yes |
| QA-015 | covers: R9 | regression | Backend receives `isNotSame=false`, but DB item state differs from request. | POST `epl-case-insert-reviseditem`. | Backend still performs side effects from DB-side comparison; `isNotSame` is not the trigger. | Yes |
| QA-016 | covers: R10 | happy | Request has valid itemMap and reason. | POST `epl-case-insert-reviseditem`. | Existing `TB_REVISED_ITEM` row is replaced by one row with Y/N flags, trimmed reason, and update date. | Yes |
| QA-017 | covers: R11 | contract | Execute DTO contract is reviewed after RP11 adjudication. | Compare OpenAPI, FE request, BE DTO, and direct POST behavior. | Request uses `applicationNo`, required boolean `isFinish`, required boolean `isNotSame`, and `itemMap`; no client `checkPointMap` is required or trusted. Missing or invalid booleans are rejected before persistence, and backend derives checkpoint/page-menu updates itself. | Yes |
| QA-018 | covers: R3 | edge | Current PRD still lists TBD-001, but RP6 evidence exists. | Review item labels against source. | SRS carries labels as closed evidence and keeps code-table maintenance out of scope. | Yes |
| QA-019 | covers: R12.1 | happy | Existing item2=Y, new item2=N, and reference application has guarantors. | POST save. | Current guarantor rows are deleted, reference rows are copied/restored, and borrower guarantor flag reflects reference state. | Yes |
| QA-020 | covers: R12.2 | happy | Existing item2=N, new item2=Y, and reference application has no guarantor. | POST save. | Borrower guarantor flag is set to Y without requiring reference guarantor rows. | Yes |
| QA-021 | covers: R12.3 | happy | Existing item3=Y, new item3=N, and reference application has collateral. | POST save. | Current collateral/provider/title/site data is deleted/restored and borrower collateral-provider flag is updated. | Yes |
| QA-022 | covers: R13.1, R13.2 | happy | item1/item4-item11 change triggers loan-condition restore/delete rules. | POST save. | `TB_LOAN_CONDITION_DETAIL` and `TB_REVISED_ITEM_DETAIL` match the RI-MAT delete/restore decision, with numeric precision preserved. | Yes |
| QA-023 | covers: R13.3, R13.4, R14 | happy | `LON_TYPE_CODE=04`, item12 changes N to Y, and item13/item14 also change. | POST save. | Fee rows are deleted for item12; item13/item14 persist only; the active checkpoint is updated and tab-control refresh reflects approved downstream keys. | Yes |
| QA-024 | covers: R2, R15 | error | Query omits `applicationNo` or sends a blank/invalid value such as over-length or malformed input. | GET `epl-case-query-reviseditem`. | Error envelope includes HTTP 400, `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; mapped field-validation detail may appear under `data`; no mutation occurs. | Yes |
| QA-025 | covers: R9, R15, R17 | error | Exception occurs after Revised Item replacement but before side-effect/checkpoint update. | POST save. | Error envelope includes `code=COMMON_MSG_SAVE_FAIL`, `message`, and nullable/error `data`; all DB writes roll back; audit records failure without full payload. | Yes |
| QA-026 | covers: R15 | error | Query repository fails. | GET `epl-case-query-reviseditem`. | Error envelope includes HTTP 500, `code=MSG_QUERY_FAIL`, `message`, and nullable/error `data`; no mutation occurs. | Yes |
| QA-027 | covers: R15 | error | Query exceeds configured limit. | GET `epl-case-query-reviseditem`. | Error envelope includes HTTP 400, `code=MSG_OVER_COUNT_LIMIT`, `message`, and nullable/error `data`. | Yes |
| QA-028 | covers: R1, R16 | error | User has no platform route/API authorization for EPROZ00800. | Call prompt/query/save endpoints directly. | Error envelope includes HTTP 403, `code=ACCESS_DENIED`, `message`, and nullable/error `data`; backend rejects before returning sensitive data or mutating DB. | Yes |
| QA-029 | covers: R12, R13, R17 | regression | Save changes guarantor/collateral/loan condition and the request includes a long reason. | POST save while capturing logs. | Logs contain action/result/table categories/row counts only; no full reason or borrower data is logged. | Yes |
| QA-030 | covers: R14 | regression | Case type is IS, IU, CS, and CU in separate test data. | POST save for each case type. | Only the matching active checkpoint table is updated, old `TB_CHECK_POINT_RC` family tables are untouched, and tab refresh is driven by a post-save `epl-auth-tab-control` call using new funcId names. | Yes |
| QA-031 | covers: R5 | happy | `LON_TYPE_CODE<>03` and stored item1 is Y from prior data. | Query/render the page and attempt to toggle item1. | item1 is forced to N, remains non-editable, and execute persists item1=N. | Yes |
| QA-032 | covers: R4 | error | Execute request contains blank item flag or an item flag outside `Y/N`. | POST `epl-case-insert-reviseditem`. | Error envelope returns HTTP 400 with `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; mapped validation detail may appear under `data`; no DB write occurs and no blank/non-`Y/N` flag is stored by execute. | Yes |
| QA-033 | covers: R8 | edge | No prior `TB_REVISED_ITEM` row exists and all prior item values are blank/N. | Save the first valid Revised Item selection. | Warning is skipped, save proceeds, and side effects are driven only by DB-side comparison. | Yes |
| QA-034 | covers: R8 | error | Prior item state differs from the request and the user does not confirm the warning. | Attempt destructive save from UI. | UI blocks the save; backend mutation endpoint is not called, and related tables remain unchanged. | Yes |
| QA-035 | covers: R9, R17 | regression | Exception occurs after side-effect restore/delete or checkpoint update has executed inside the transaction. | POST save with a controlled failure after those writes. | Response carries `COMMON_MSG_SAVE_FAIL`; Revised Item, side-effect tables, checkpoint table, and tab-control-visible state roll back together. | Yes |
| QA-036 | covers: R13.1 | happy | Loan-condition item set has at least one prior Y and request sends all loan-condition items N. | POST save. | `TB_LOAN_CONDITION_DETAIL` and `TB_REVISED_ITEM_DETAIL` rows are deleted, approved checkpoint keys are marked for reprocessing, and tab-control refresh exposes the resulting page state. | Yes |
| QA-037 | covers: R13.1 | happy | Loan-condition item set is all N and request changes at least one loan-condition item to Y. | POST save. | `TB_LOAN_CONDITION_DETAIL` and `TB_REVISED_ITEM_DETAIL` rows are deleted, approved checkpoint keys are marked for reprocessing, and tab-control refresh exposes the resulting page state. | Yes |
| QA-038 | covers: R13.2 | happy | item1 changes Y to N and original detail data exists. | POST save. | Renew-loan-tenor restore path updates the mapped detail fields and `TB_REVISED_ITEM_DETAIL.item1` without affecting item10. | Yes |
| QA-039 | covers: R13.2 | happy | A parameterized row changes each of item4, item5, item6, item7, item8, item9, item10, and item11 from Y to N. | POST save for each row. | Each row restores only its mapped loan-condition fields and updates the matching `TB_REVISED_ITEM_DETAIL` flag. | Yes |
| QA-040 | covers: R13 | error | Restore or execute input contains loan amount, grace period, tenor, fixed rate, tier rate, base rate, or FD rate values exceeding DB precision/scale. | POST save. | Error envelope returns HTTP 400 with `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; backend rejects invalid precision/truncation before writing `TB_LOAN_CONDITION_DETAIL`; no silent rounding or substring truncation occurs. | Yes |
| QA-041 | covers: R2, R15 | error | Query or execute references an application number with no case row or required source row. | GET query and POST execute separately. | Error envelope includes HTTP 404, `code=MSG_DATA_NOT_FOUND`, `message`, and nullable/error `data`; query performs no mutation and execute rolls back any attempted write. | Yes |
| QA-042 | covers: R16 | error | Client submits spoofed `SECURE_ATTRIBUTE`, case type, editability, or page predicate values while backend source data disallows edit/save. | POST `epl-case-insert-reviseditem`. | Error envelope returns HTTP 403 with `code=ACCESS_DENIED`, `message`, and nullable/error `data`; backend ignores client-supplied predicates, reads backend-owned sources, rejects mutation, and writes no Revised Item/side-effect data. | Yes |
| QA-043 | covers: R1 | regression | Prompt/render fixtures include `LON_TYPE_CODE=03`, `LON_TYPE_CODE=04`, and at least one non-03/04 case. | Re-run prompt/render tests and direct query/save calls for each fixture. | EPROZ00800 is shown only for `03` and `04`; non-03/04 cases are hidden by tab-control/prompt and direct query/save is blocked before data read or mutation. | Yes |
| QA-044 | covers: R1, R5, R13, R14 | regression | Fixtures cover `LON_TYPE_CODE=01`, `02`, `03`, `04`, and `05` when present, across applicable customer/security checkpoint families. | Run prompt/tab-control, query/render, and save-side-effect tests for each fixture. | `LON_TYPE_CODE` is the only case-type axis: `03=Renew` / 展期 forces item1=Y and supports EPROZ00800; `04=Change Condition` / 展變 supports EPROZ00800 and drives the item12 fee-delete branch; non-03/04 types do not expose EPROZ00800. `LON_ATTRIBUTE`/`SECURE_ATTRIBUTE`/`caseCategory` select page/checkpoint family only and are not substituted for case type. | Yes |
| QA-045 | covers: R1, R4, R5, R13 | regression | Fixtures include `LON_TYPE_CODE=03` and `LON_TYPE_CODE=04` with editable permission. | Run the R4 validation matrix for both case types: no selected item, blank reason, overlength reason, invalid item flag, and valid save. Also run the R5 and R13.3 branch checks. | Both `03` and `04` use the same R4 validation matrix and error envelope; only `03` forces `item1=Y` / non-editable, and only `04` triggers the `item12` fee-delete branch when old `N` becomes new `Y`. No unapproved separate mandatory fields or validation rules appear. | Yes |
| QA-046 | covers: R14 | regression | Fixtures cover IS, IU, CS, and CU plus post-save tab-control refresh. | POST save for each fixture, inspect active checkpoint table, then re-call `epl-auth-tab-control`. | IS may mark `EPROISU0150`, CS may mark `EPROCSU0150`, IU/CU do not synthesize 0150 keys, and no `EPROISU0173`/`EPROCSU0173` checkpoint/page-menu key is returned or persisted by EPROZ00800. 0173 summary display is trace-only for the 0173 bundles. | Yes |
| QA-047 | covers: R1 | review | Documentation, UI copy, nav config, and fixtures include non-03/04, 03, and 04 cases. | Review SRS wording, UI/tab labels, prompt/tab-control behavior, and direct query/save guards. | `shared` / `common` is used only as module and tab-config classification; EPROZ00800 is visible/required only for `LON_TYPE_CODE in ('03','04')`, and non-03/04 cases do not show or require Revised Item. | Yes |
| QA-048 | covers: R18 | performance | Normal query data set exists and no bulk side effects are triggered. | Run prompt/query flow under SIT/integration timing capture. | Query-like init response time is <= 3 seconds and the timing evidence is retained. | Yes |
| QA-049 | covers: R18 | performance | Normal execute data set exists. | POST save under SIT/integration timing capture. | Execute response time is <= 5 seconds and transaction timeout setting/evidence is <= 30 seconds. | Yes |
| QA-050 | covers: R18 | edge | Maximum reasonable guarantor/collateral copy data set is prepared. | POST save that triggers bulk copy. | Execute completes successfully within the configured <=30 second transaction timeout and returns the success envelope; timeout or failure fails this QA case and leaves no partial DB writes. | Yes |
| QA-051 | covers: R17, R18 | audit | Prompt, query, execute success, and execute failure are run with a known `requestId`. | Inspect application/audit/error logs and API responses. | Logs include `requestId`, `applicationNo`, `userId`, `action`, result, elapsed time, and error code when present; no SQL, stack trace, full ID/account, sensitive collateral document content, full request payload, or full `reasonMemo` is exposed; failure is traceable back to DB/audit state. | Yes |
| QA-052 | covers: R13.4, R14 | regression | Existing item13/item14 change while item12 and loan-condition item set remain unchanged. | POST save. | Only `TB_REVISED_ITEM.item13/item14` are persisted for RI-MAT data-table effects; no guarantor/collateral/loan-condition/fee side-effect table is changed. Normal R14 checkpoint/tab-control refresh behavior is still applied and verified separately from data-table side effects. | Yes |
| QA-053 | covers: R12.1 | edge | Existing item2=Y, new item2=N, and reference application has no guarantor rows. | POST save. | Save succeeds; current guarantor rows are deleted, no reference guarantor rows are restored, borrower guarantor flag is set to N, and subsequent query/tab-control state reflects the change. | Yes |
| QA-054 | covers: R12.3 | edge | Existing item3=Y, new item3=N, and reference application has no collateral/provider rows. | POST save. | Save succeeds; current collateral/provider/title/site data is deleted, no reference collateral rows are restored, borrower collateral-provider flag is set to N, and subsequent query/tab-control state reflects the change. | Yes |
| QA-055 | covers: R15 | error | Execute request omits `applicationNo` or sends a blank/invalid value such as over-length or malformed input. | POST `epl-case-insert-reviseditem`. | Error envelope includes HTTP 400, `code=COMMON_MSG_ERROR_LON`, `message`, and nullable/error `data`; mapped `ErrorInputException` field-validation detail may appear under `data`; no DB write occurs. | Yes |
| QA-056 | covers: R14, R15 | happy | Valid execute request saves successfully and side-effect logic determines borrower guarantor/collateral-provider flags. | POST `epl-case-insert-reviseditem`. | Platform envelope returns `code=COMMON_MSG_SAVE_SUCCESS`, `message`, and nullable/simple `data`; tab/page state is refreshed through `epl-auth-tab-control`, and any legacy/current extra `pageMenuCondition` is ignored by FE and must not be treated as authoritative. | Yes |
