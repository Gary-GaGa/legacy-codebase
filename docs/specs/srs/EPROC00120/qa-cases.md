# QA Cases - EPROC00120 Corporate Financial Evaluation FI

| QA ID | Covers | Type | Given | When | Then | Test-ready |
|---|---|---|---|---|---|---|
| QA-001 | covers: R1, R6 | happy | Existing `APPLICATION_NO` has saved FI ratio rows. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=true`. | Response returns saved `financialList` rows from `TB_FINANCIAL_EVALUATION_FI` without recalculating source statements; PRD borrower/checkpoint response fields are checked separately by QA-024/P-012. | Yes |
| QA-002 | covers: R1 | happy | Existing `APPLICATION_NO` has summary, balance/income source rows, but no saved ratio rows. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | Response returns editable rows generated from source FI statement rows. | Yes |
| QA-003 | covers: R1, R6, R7 | error | `applicationNo` is blank. | `POST /epl-info-c0-financial-evaluation-table-fi`. | Platform envelope carries `COMMON_MSG_ERROR_LON` or validation equivalent; HTTP status follows current platform convention; DB remains unchanged. | Yes |
| QA-004 | covers: R2, R20 | happy | Balance and income source rows have matching `DATA_SEQ`, nonzero denominators, and seed values that exercise a two-decimal rounding boundary. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | Derived DTO keys `cost`, `loans`, `growthOfGrossLoans`, `roa`, and `roe` match the RD-confirmed formula and rounding policy; until P-013 closes, HALF_DOWN vs HALF_UP expected values remain blocked. | Blocked by P-013 rounding decision |
| QA-005 | covers: R2 | edge | First period source rows exist. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | First-period growth, ROA, and ROE fields are returned as `N/A` per legacy baseline. | Yes |
| QA-006 | covers: R2, R11, R12 | edge | Formula denominator is zero for cost/income or loans/deposits. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | Derived ratio returns `N/A` or the RD-confirmed pending result without unhandled exception or unsafe log output. | Blocked by P-002/P-003 decision |
| QA-007 | covers: R3 | happy | Manual ratio fields are within 12 characters and valid signed numeric text. | `POST /epl-save-c0-financial-evaluation-table-fi`, then `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=true`. | Manual values for `NPL`, `LLR`, `LLR_OVER_NPL`, exposure fields, `NET_INTEREST_MARGIN`, `LIQUIDITY_COVERAGE`, `SOLVENCY`, and `TIER_ONE_CAPITAL` are persisted and returned through the current DTO keys exactly; no rounding or truncation occurs. | Yes |
| QA-008 | covers: R3 | error | Any persisted ratio field exceeds 12 characters. | `POST /epl-save-c0-financial-evaluation-table-fi`. | BE rejects the request before DB write; no silent truncation occurs. | Yes |
| QA-009 | covers: R3, R9 | edge | Request includes current-runtime `cetOneCapitalRatio` and `totalCapitalRatio`. | `POST /epl-save-c0-financial-evaluation-table-fi`, then `POST /epl-info-c0-financial-evaluation-table-fi`. | Behavior follows RD/DB decision on whether `CET_ONE_CAPITAL`/`TOTAL_CAPITAL` are stale-snapshot columns or implementation drift; until then the fields are transitional and must not be treated as PRD/db-diff confirmed. | Blocked by P-010 decision |
| QA-010 | covers: R4 | happy | Existing application has saved FI ratio rows. | `POST /epl-save-c0-financial-evaluation-table-fi` with `isFinish=false` and a complete replacement `financialList`. | Old rows for the application are deleted, submitted rows are inserted once, checkpoint is set to draft/incomplete, and response carries `COMMON_MSG_SAVE_SUCCESS`. | Yes |
| QA-011 | covers: R4, R7 | error | Repository insert fails after delete begins. | `POST /epl-save-c0-financial-evaluation-table-fi`. | Response carries `COMMON_MSG_SAVE_FAIL`; all row replacement and checkpoint updates roll back. | Yes |
| QA-012 | covers: R4, R8 | error | Authenticated user lacks API authorization or edit role. | `POST /epl-save-c0-financial-evaluation-table-fi`. | Response is platform `ACCESS_DENIED` / HTTP 401 for API auth failure, or service-level edit-role rejection before delete/insert/checkpoint mutation. | Blocked by P-011 auth seed/service guard evidence |
| QA-013 | covers: R5 | happy | Required Finished fields pass validation for a CS case. | `POST /epl-save-c0-financial-evaluation-table-fi` with `isFinish=true`. | Rows save and `TB_CHECK_POINTS_CS.EPROC00120` is updated to completed; CU checkpoint is unchanged. | Yes |
| QA-014 | covers: R5 | happy | Required Finished fields pass validation for a CU case. | `POST /epl-save-c0-financial-evaluation-table-fi` with `isFinish=true`. | Rows save and `TB_CHECK_POINTS_CU.EPROC00120` is updated to completed; CS checkpoint is unchanged. | Yes |
| QA-015 | covers: R5, R14 | error | A required Finished field is missing and the request bypasses FE validation. | `POST /epl-save-c0-financial-evaluation-table-fi` with `isFinish=true`. | BE rejects before DB write; no row replacement or checkpoint mutation occurs. | Blocked by P-005 required-field decision |
| QA-016 | covers: R4, R7 | error | `applicationNo` has no summary row. | `POST /epl-save-c0-financial-evaluation-table-fi`. | Response carries `MSG_DATA_NOT_FOUND` or mapped save failure; DB remains unchanged. | Yes |
| QA-017 | covers: R7 | error | Query repository throws or source query fails. | `POST /epl-info-c0-financial-evaluation-table-fi`. | Response carries `MSG_QUERY_FAIL`; no DB mutation occurs. | Yes |
| QA-018 | covers: R8 | error | User has `TB_API_AUTH` seed but is not allowed to edit this page by service-level role/page state. | `POST /epl-save-c0-financial-evaluation-table-fi`. | BE rejects the mutation; DB remains unchanged. This proves `TB_API_AUTH` alone is not sufficient. | Blocked by P-011 RD guard evidence |
| QA-019 | covers: R5, R8 | regression | Non-editor tampers request body and sets `isFinish=true`. | `POST /epl-save-c0-financial-evaluation-table-fi` directly. | BE rejects before validation-side effects or checkpoint update. | Blocked by P-011 RD guard evidence |
| QA-020 | covers: R5, R7, R15 | @PENDING regression | Renewal/change old-case equivalent to legacy `EPROC0_0220` is represented by seeded case state and session/role context. | `POST /epl-save-c0-financial-evaluation-table-fi` with `isFinish=true`. | Response and `TB_CHECK_POINTS_CS/CU.EPROC00120` follow RD-confirmed old-case policy; no unauthorized completion or parent-done side effect occurs. | Blocked by P-006 decision |
| QA-021 | covers: R3, R9 | regression | Request payload contains numeric boundary strings that mirror FE integer maxlength 9 and DB VARCHAR2(12) limits. | `POST /epl-save-c0-financial-evaluation-table-fi`. | Allowed values persist in `TB_FINANCIAL_EVALUATION_FI` without truncation; rejected values are rejected before DB write and no silent widening is introduced. | Yes |
| QA-022 | covers: R2, R10 | @PENDING edge | Balance and income row counts or `DATA_SEQ` keys mismatch. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | Backend follows RD-confirmed mismatch policy and never calculates by unsafe index alignment. | Blocked by P-001 decision |
| QA-023 | covers: R1, R18 | @PENDING regression | c0 direct parity code verification is available. | Run source-level comparison for legacy `EPROC0_0120/0220` and current `POST /epl-info-c0-financial-evaluation-table-fi` / `POST /epl-save-c0-financial-evaluation-table-fi`. | Differences are classified as regression, keep-new, or pending with file:line evidence before owner approval. | Blocked by P-009 parity evidence |
| QA-024 | covers: R1, R19 | @PENDING regression | PRD/legacy query response needs applicant name/checkpoint context and the case is seeded with main borrower and checkpoint rows. | `POST /epl-info-c0-financial-evaluation-table-fi`. | Response carries the parent context, or the owning parent endpoint/FE state is documented and P-012 is closed with file:line evidence. | Blocked by P-012 implementation closeout |
| QA-025 | covers: R19 | @PENDING regression | Parent `pageCheckMap` requirement and 0119/0219/0110 reset outcomes are represented by seeded checkpoint/data states. | `POST /epl-info-c0-financial-evaluation-table-fi`. | Response and DB checks prove `isAllTabsCheck` ownership, 0119/0219 reset of 0120/0220 checkpoint, and 0110/0210 deletion of FI evaluation/statement data per RD-confirmed P-012 decision. | Blocked by P-012 implementation closeout |
| QA-026 | covers: R13 | @PENDING regression | Existing saved FI rows and changed `ratiosDate` source rows are available. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`, then `POST /epl-save-c0-financial-evaluation-table-fi`. | Manual-field preservation or recalculation follows RD-confirmed R13 behavior without silent overwrite. | Blocked by P-004 decision |
| QA-027 | covers: R16 | @PENDING regression | `EPROC00220` old-case UI and i18n sources are available. | Open the 0220 UI flow and compare function-name/i18n key rendering between parent and child pages. | Label/function-name behavior follows RD/UX-confirmed R16 decision and does not alter backend mutation. | Blocked by P-007 decision |
| QA-028 | covers: R17 | @PENDING regression | Shared `TB_FINANCIAL_EVALUATION_FI` c0/i0 data ownership decision is available. | Run c0 and i0 save/query flows on distinct applications and shared table rows, including `POST /epl-save-c0-financial-evaluation-table-fi` for c0. | Ownership and cleanup rules follow RD/DBA-confirmed R17 decision without importing i0 behavior into c0 as-is. | Blocked by P-008 decision |
| QA-029 | covers: R11 | @PENDING edge | Income source row has `PERIODS=0` with nonzero average asset/equity denominators. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | ROA/ROE denominator behavior follows RD-confirmed R11 decision and does not create unhandled divide-by-zero or misleading ratio output. | Blocked by P-002 decision |
| QA-030 | covers: R12 | @PENDING edge | Source rows are seeded to isolate the nonstandard COST_INCOME formula. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | `cost` follows the RD/domain-confirmed R12 formula, with file:line evidence deciding legacy formula vs corrected formula. | Blocked by P-003 decision |
| QA-031 | covers: R14 | @PENDING regression | Request has missing Finished-required fields but `isFinish=false`. | `POST /epl-save-c0-financial-evaluation-table-fi`. | Save branch follows RD-confirmed R14 decision: either draft save accepts without Finished-required validation, or rejects with the approved validation code before mutation. | Blocked by P-005 decision |
| QA-032 | covers: R20 | @PENDING edge | Source rows produce a value exactly on a HALF_DOWN vs HALF_UP boundary for each automatic ratio field. | `POST /epl-info-c0-financial-evaluation-table-fi` with `isQuery=false`. | `growthOfGrossLoans`, `cost`, `roa`, `roe`, and `loans` use the RD/domain-confirmed rounding mode and intermediate scale; expected values are recorded before approval. | Blocked by P-013 rounding decision |
