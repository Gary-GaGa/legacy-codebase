# Pilot SRS Pending Verification

Scope: `EPROZ00100` and `EPROC00118` pending items that can be checked from DB/config/code without product-owner signoff.

Latest verification: 2026-06-19, SELECT-only against schema `OVSLXLON02`.

Re-run command:

```powershell
& 'C:\Users\00596357\Documents\project\kh\epro\epro-db\new.cmd' '@C:\project\legacy-codebase\docs\build-tasks\pilot-srs-pending-verification.sql'
```

SQL source: `docs/build-tasks/done/pilot-srs-pending-verification.sql`.

## Closure Status

| Item | Result |
|---|---|
| `EPROC00118` `PENDING-006` | Closed locally in `spec.md`: public C0 DTO spelling is confirmed as `trans12MonCode` / `debtRatio`; internal Function1 handoff still uses legacy `debtRati`. |
| DB/code facts for remaining items | Established below and in the SQL file. These do not close PM/SA/RD/DBA decision items by themselves. |
| Approval / ledger status | Not changed. This is verification/backfill evidence only, not Approved. |

## Decision Log

| Date | Item | Decision | Impact |
|---|---|---|---|
| 2026-06-19 | `EPROZ00100` delete/close mutation role `403` | Owner chose A: role `403` is not allowed to perform delete or close mutations. | Keep SRS delete=`001/002`, close=`404/405`; DB `TB_API_AUTH` rows that include `403` need seed correction or an explicit query-only/admin exception outside these mutation APIs. |
| 2026-06-19 | `EPROZ00100` dashboard init redistribution | Owner chose A: keep automatic CA/CR redistribution on init as a POST mutation endpoint. | `epl-init-z0-todolist` remains in contract and must be implemented with `TB_API_AUTH`, transaction, history insert, and rollback; `PENDING-Z003` is closed as a decision item. |
| 2026-06-19 | `EPROZ00100` status code semantics | Owner chose A: active `TB_PROCESS_CODE` rows are the to-be baseline for `D1`, `C1`, `R0305`, `R0313`, `R0397`, and `21`-`25`. | `PENDING-Z002` is closed. QA may assert `D1=Delete`, `C1=Closed`, `R03xx=CA Redistributing`, and CAD/TLOD `21`-`25` routing status names per DB. |
| 2026-06-19 | `EPROZ00100` CAD `docNo` / `docNo1` mapping | Owner chose A: adopt the current backend/view contract. | `PENDING-Z004` is closed. `docNo` searches identity document fields plus `REGISTER_NO`; response `docNo1` is `V_MAIN_BORROWER_INFO.DOC_NO_REGISTER_NO`, with corporate `REGISTER_NO` correctness covered by QA. |
| 2026-06-19 | `EPROZ00100` CAD decision-date range limit | Owner chose A: backend must enforce the six-calendar-month limit. | `PENDING-Z005` is closed. Current backend evidence did not show this guard, so implementation remains required before regression signoff. |
| 2026-06-19 | `EPROZ00100` proposal download token/path strategy | Owner chose A: use a one-time short-lived `downloadToken`, never a reusable local filesystem path. | `PENDING-Z006` is closed. Current backend evidence still lacks the endpoint/auth row, so RD implementation and seed remain required. |
| 2026-06-19 | `EPROZ00100` delete/close reason codes and `99` other reason | Owner chose A: use the DB reason-code baseline, keep `reasonList[]`, and require `otherReason` when `D99` or `C99` is selected. | `PENDING-Z007` is closed. Current backend DTO/service evidence still needs RD alignment from single `reasonCode` to `reasonList[]` plus `99` validation. |
| 2026-06-19 | `EPROZ00100` current-application session bridge | Owner chose A: keep `epl-session-z0-current-application` as a migration-only POST/DELETE bridge. | `PENDING-Z008` is closed. Long-term target is route/API state; bridge retires when related `EPROISU` / `EPROCSU` routes no longer read server-session application context. Current backend/auth row still missing. |
| 2026-06-19 | `EPROZ00100` `TB_API_AUTH` seed baseline | Owner chose A: use the eight SRS `epl-*` API IDs as final seed baseline, with single-row `API_ID`, semicolon `ROLE`, and `REF_FUNCTION_ID=EPROZ00100`. | `PENDING-Z009` is closed. Missing rows must be inserted; delete/close rows must remove `403`; role allow-lists not already decided remain under `PENDING-Z001`. |
| 2026-06-19 | `EPROZ00100` final role allow-list | Owner chose A: least-privilege role matrix, excluding `403` from every user-facing EPROZ00100 API. | `PENDING-Z001` is closed. Seed correction remains required because current auth rows include `403` and some final rows are missing. |
| 2026-06-19 | `EPROZ00100` proxy history actor storage | Owner chose A: do not add `PROCESS_AGENT_CODE` / `PROCESS_AGENT_NAME`; use `PROCESS_EMP_CODE` / `PROCESS_EMP_NAME` for the actual acting employee. | `PENDING-Z010` is closed. Schema remains the seven-column `TB_APP_HISTORY` contract. |
| 2026-06-19 | `EPROC00118` `CR_SCORE_CARD_COMPLETED` two-character semantics | Owner chose A: first char is corporate scorecard, second char is other scorecard; CR Save/Finished updates first char and preserves second; AO Save/re-save resets full value to `NN`; CU return/reset must update CU checkpoint branch. | `PENDING-001` is closed. |
| 2026-06-19 | `EPROC00118` RC checkpoint mapping | Owner chose A: legacy `EPROC0_0218` renewal/change is merged in the new system and reuses `TB_CHECK_POINTS_CS/CU.EPROC00118`. | `PENDING-002` is closed. No RC-specific active checkpoint table/column is added. |
| 2026-06-19 | `EPROC00118` save validation strictness | Owner chose A: adopt refactor/current backend strict BE validation as the to-be contract. | `PENDING-003` is closed. Required scorecard fields, Rate-before-Finished, CR Finished comment, and max-length validation must reject before DB write even if FE validation is bypassed. |
| 2026-06-19 | `EPROC00118` role `003` AO editability | Owner chose to follow PRD page-level role evidence: AO editor is `001/002` only. | `PENDING-004` is closed. Role `003` remains readonly / non-editor for this page and must not be able to mutate AO scorecard data. |
| 2026-06-19 | `EPROC00118` AO-to-CR copy score fields | Owner chose A: correct the legacy omission and copy `_AR_SCR` / `_INVENTORY_SCR` along with their code fields. | `PENDING-005` is closed. QA must prove CR `AR` and `INVENTORY` code/score pairs remain consistent after AO Finished. |
| 2026-06-19 | `EPROC00118` scorecard parameter seed baseline | Owner chose A: adopt the current `TB_SCORE_CARD_PARAM_DETAIL` DB snapshot as the formal EPROC00118 seed baseline. | `PENDING-007` is closed. RD/DBA still need to export, version, and deploy the approved seed package. |
| 2026-06-19 | `EPROC00118` RC save duplicate where-field | Owner chose A: treat legacy `EPROC0_0218_mod.save` duplicate `whereFieldMap.APPLICATION_NO` as redundant and do not preserve it. | `PENDING-008` is closed. To-be save/update uses a single `APPLICATION_NO` condition. |
| 2026-06-20 | `EPROC00118` late-transaction linkage dead DOM ids | Owner chose A: keep the canonical `MAX_OVERDUE_CODE` / `TRANS_12MON_CODE` linkage and treat legacy `CR_TRA_LST_MON_CODE*` / `CR_MAX_DUE_CODE*` handlers as dead code. | `PENDING-009` is closed. QA validates the real linkage only; wrong-id aliases are not required. |
| 2026-06-20 | `EPROC00118` score parameter effective-date lookup | Owner chose A: follow PRD/source/current backend and use `MAX(ST_DATE <= APP_DATE)` only. | `PENDING-010` is closed. `END_DATE` remains seed metadata and is not an EPROC00118 runtime filter. |
| 2026-06-20 | `EPROC00118` AO Default CR side effect | Owner chose A: accept the legacy/current backend behavior that AO Finished with AO Default also sets CR risk/score/date. | `PENDING-011` is closed. CR baseline becomes `Default` / `-1` / AO rating date without requiring CR re-rating first. |
| 2026-06-20 | `EPROC00118` endpoint authorization and save role guard | Owner chose A: require both endpoint `TB_API_AUTH` seeds and explicit save service rejection for non-AO/non-CR roles. | `PENDING-012` is closed as a decision item. RD/DBA must still provide seed apply evidence and service-level reject/test evidence before QA regression or any testing deployment. |
| 2026-06-20 | `EPROC00118` info API initialization payload | Owner chose A: `epl-info-c0-corporateScorecard` must return both `mainBorrowerName` and `checkpointStatus`. | `PENDING-013` is closed. Current backend DTO lacks these fields, so RD must align DTO/service/OpenAPI implementation before UI/API signoff. |
| 2026-06-20 | `EPROC00118` numeric input UI compatibility | Owner chose A: do not preserve the legacy initial-load vs Default-to-N length/decimal mismatch. | `PENDING-014` is closed. UI and BE must use one SRS/OpenAPI/DB precision rule set for both paths. |
| 2026-06-20 | `EPROC00118` calc response `totalScore` exposure | Owner chose A: accept the current public calc response with only `riskLevel` and `scoreDatetime`. | `PENDING-015` is closed. `totalScore` remains a server-side calculation and is not exposed by `epl-calc-c0-corporateScorecard`. |
| 2026-06-20 | `EPROC00118` nonnumeric score seed handling | Owner chose A: nonnumeric `TB_SCORE_CARD_PARAM_DETAIL.SCORE` is a data/query failure, not zero. | `PENDING-016` is closed as a decision item. Current `parseScore` behavior remains an RD gap because it catches `NumberFormatException` and returns `0`. |
| 2026-06-20 | `EPROC00118` calc timestamp timezone | Owner chose A: generate `scoreDatetime` with an explicit Asia/Taipei / UTC+8 clock. | `PENDING-017` is closed as a decision item. Current `LocalDateTime.now()` without `ZoneId` remains an RD implementation gap. |
| 2026-06-20 | `EPROC00118` performance threshold | Owner chose A: use the PRD platform standard AJAX timeout as the acceptance threshold. | `PENDING-018` is closed. No page-specific p95/second target is added to the SRS. |

## EPROZ00100 Results

| Pending | Local evidence established | Current result | Still open |
|---|---|---|---|
| `PENDING-Z001` | `TB_ROLE_DEFINE` query for roles `001/002/003/101/102/103/201/202/203/301/302/402/403/404/405`. | Closed by owner decision on 2026-06-19. All 15 role rows exist. Final least-privilege API allow-list excludes `403` from every user-facing EPROZ00100 API. | No open human decision. RD/DBA seed correction remains required where current auth rows include `403` or missing final API IDs. |
| `PENDING-Z002` | `TB_PROCESS_CODE` query for `D1`, `C1`, `R0305`, `R0313`, `R0397`, `21`-`25`. | Closed by owner decision on 2026-06-19. All 10 rows exist. `D1=Delete`, `C1=Closed`; `21`-`25` are RC contract/disbursement statuses; `R03xx` are CA redistributing statuses. | No open human decision. Keep as DB baseline evidence. |
| `PENDING-Z003` | Backend search for init/redistribution endpoint and `TB_API_AUTH` query. | Closed as decision item: owner chose automatic init redistribution. Current backend/DB still has no `epl-init-z0-todolist` controller/auth row. | Implementation and DB seed remain required before signoff. |
| `PENDING-Z004` | Current backend `VMainBorrowerInfoRepository.findToDoList` maps `docNo` to passport/id/family/resident/visa/register fields and returns `DOC_NO_REGISTER_NO`; legacy CAD uses `NO` and corporate `docNo1=REGISTER_NO`. | Closed by owner decision on 2026-06-19. Current backend/view mapping is the to-be contract. | No open human decision. QA must include a corporate case proving `DOC_NO_REGISTER_NO` returns `REGISTER_NO`. |
| `PENDING-Z005` | `ToDoListRequest` validates date format only; repository applies `DECISION_DATE BETWEEN ...`; no six-month backend guard found. | Closed by owner decision on 2026-06-19. Backend must reject CAD decision-date ranges where `endDecisionDate` is later than `startDecisionDate` plus six calendar months. | No open human decision. RD implementation gap remains. |
| `PENDING-Z006` | Backend/auth search for proposal download draft endpoint. | Closed by owner decision on 2026-06-19. To-be contract returns one-time short-lived `downloadToken` plus `expiresAt`; no reusable local filesystem path. Current backend/auth row still missing. | No open human decision for token/path strategy. RD implementation and `TB_API_AUTH` seed remain. |
| `PENDING-Z007` | `TB_COMMON_FIELD_OPTIONS` query for `DEL_REASON` and `CLO_REASON`. | Closed by owner decision on 2026-06-19. `DEL_REASON` has `D01`-`D12` + `D99`; `CLO_REASON` has `C01`-`C11` + `C99`, all shown in `en_US`. To-be API uses `reasonList[]`; `D99/C99` require `otherReason`. | No open human decision. RD implementation gap remains because current request DTO uses single `reasonCode` and service evidence lacks `D99/C99` guard. |
| `PENDING-Z008` | Backend/auth search for session bridge endpoint. | Closed by owner decision on 2026-06-19. To-be contract keeps `epl-session-z0-current-application` as a migration-only POST/DELETE bridge; current backend/auth row still missing. | No open human decision for retirement plan. RD implementation and `TB_API_AUTH` seed remain. |
| `PENDING-Z009` | `TB_API_AUTH` query for all SRS/draft endpoint IDs and PK shape. | Closed by owner decision on 2026-06-19. DB PK is single-column `API_ID`. Existing rows: `epl-list-todolist`, `epl-case-insert-delreason`, `epl-case-insert-cloreason`. Missing rows: init, proposal download, session bridge, delete-reason select, close-reason select. Current mutation allow-lists include `403` in addition to SRS expected roles. | No open human decision. RD/DBA seed gap remains. |
| `PENDING-Z010` | `USER_TAB_COLUMNS` for `TB_APP_HISTORY` and proxy-agent probe. | Closed by owner decision on 2026-06-19. Physical columns remain 7; no `PROCESS_AGENT_CODE` or `PROCESS_AGENT_NAME`. To-be proxy delete/close writes the actual acting employee to `PROCESS_EMP_CODE` / `PROCESS_EMP_NAME`. | No open human decision. |

## EPROC00118 Results

| Pending | Local evidence established | Current result | Still open |
|---|---|---|---|
| `PENDING-001` | No DB-only closure. Current SRS separates E1 CU-return and E2 full-column overwrite. | Closed by owner decision on 2026-06-19. First char is corporate scorecard, second char is other scorecard; CR Save/Finished updates first char and preserves second; AO Save/re-save resets full value to `NN`; CU return/reset must update CU checkpoint branch. | No open human decision. |
| `PENDING-002` | `USER_TAB_COLUMNS` probe for checkpoint columns plus refactor/backend check. | Closed by owner decision on 2026-06-19. New DB has only `EPROC00118` in `TB_CHECK_POINTS_CS` and `TB_CHECK_POINTS_CU`; refactor/backend expose one `EPROC00118` corporate scorecard flow. RC renewal/change reuses that shared checkpoint field. | No open human decision. |
| `PENDING-003` | Refactor save validation and current backend `validateFields` evidence. | Closed by owner decision on 2026-06-19. Strict BE validation is the to-be contract for required scorecard fields, Rate-before-Finished, CR Finished comment, and max length. | No open human decision. |
| `PENDING-004` | Role `003` exists in `TB_ROLE_DEFINE`; PRD page-level source rule and current backend save roles use AO `001/002` only. | Closed by owner decision on 2026-06-19. Follow PRD page-level evidence: role `003` is not an AO editor on EPROC00118. | No open human decision. |
| `PENDING-005` | Legacy `COPY_FIELD` omits `_AR_SCR` / `_INVENTORY_SCR` while copying `_AR_CODE` / `_INVENTORY_CODE`; current backend evidence writes CR score values from function output. | Closed by owner decision on 2026-06-19. New system intentionally corrects the omission and must copy/write `CR_AR_SCR` and `CR_INVENTORY_SCR` consistently with their code fields. | No open human decision. |
| `PENDING-006` | Public C0 request DTOs checked. | Closed in SRS: public fields are canonical; internal `GetRateRequest` still has `debtRati`. | None for public C0 contract. |
| `PENDING-007` | `TB_SCORE_CARD_PARAM_DETAIL` query over all 22 `funcGetRate` scoring vars: `B_V1`-`B_V22` used by EPROC00118. | Closed by owner decision on 2026-06-19. All 22 vars have rows; nonnumeric score count is 0 for these vars. Current DB snapshot is the approved seed baseline for labels, scores, ranges, and risk levels. | No open human decision. RD/DBA export/version/deploy seed package remains required. |
| `PENDING-008` | PRD says the RC duplicate `whereFieldMap.APPLICATION_NO` does not affect result and is redundant; current backend uses one `whereCondition.put("APPLICATION_NO", applicationNo)`. | Closed by owner decision on 2026-06-19. New system does not preserve duplicate where-field behavior. | No open human decision. |
| `PENDING-009` | PRD marks `CR_TRA_LST_MON_CODE*` / `CR_MAX_DUE_CODE*` as non-existent DOM ids and says to treat that block as dead code; legacy also has valid handlers for `MAX_OVERDUE_CODE` / `TRANS_12MON_CODE`. | Closed by owner decision on 2026-06-20. New system keeps the canonical late-transaction linkage but does not carry wrong-id dead handlers or aliases. | No open human decision. |
| `PENDING-010` | PRD/source/current backend use `MAX(ST_DATE <= APP_DATE)`; checked 22 EPROC00118 scoring vars currently have `END_DATE` null in DB. | Closed by owner decision on 2026-06-20. Runtime parameter lookup uses `MAX(ST_DATE <= APP_DATE)` only; `END_DATE` is seed metadata and does not filter EPROC00118 results. | No open human decision. |
| `PENDING-011` | Current code sets `CR_RISK_LEVEL=Default`, `CR_SCORE=-1`, and `CR_DATE=AO actionDate` when AO Finished is submitted with AO Default. | Closed by owner decision on 2026-06-20. New system accepts this AO-to-CR Default side effect and QA must assert the CR baseline values. | No open human decision. |
| `PENDING-012` | `TB_API_AUTH` query and service role flow checked. All four C0 target auth rows are missing while i0 source rows exist. Current save flow has no explicit `!isAO && !isCR` reject; non-AO falls into CR branch if API auth misses. | Closed by owner decision on 2026-06-20. To-be requires both four endpoint seeds and service-level save rejection for non-AO/non-CR roles. | No open human decision. RD/DBA seed apply evidence and RD guard/test evidence remain required before QA regression or any testing deployment. |
| `PENDING-013` | PRD requires `MAIN_BORROWER_NAME` and `dataMap.check` in page initialization; current info response DTO lacks `mainBorrowerName` and checkpoint fields. | Closed by owner decision on 2026-06-20. The to-be `epl-info-c0-corporateScorecard` response includes `mainBorrowerName` from `TB_MAIN_BORROWER_INFO_CORP` and `checkpointStatus` from `TB_CHECK_POINTS_CS/CU.EPROC00118`. | No open human decision. RD DTO/service implementation remains required before UI/API signoff. |
| `PENDING-014` | Current DTO lengths/patterns inspected via SRS/OpenAPI/code; PRD marks the legacy UI difference as defect suspect. | Closed by owner decision on 2026-06-20. New system does not preserve the path-specific legacy display mismatch; initial load and Default Y-to-N reset use the same SRS/OpenAPI/DB precision rules. | No open human decision. |
| `PENDING-015` | C0 calc response and Function1 response checked; PRD/legacy expected `totalScore`, while current C0 public DTO has only `riskLevel` and `scoreDatetime`. | Closed by owner decision on 2026-06-20. New public calc response does not expose `totalScore`; total score remains internal and is verified indirectly through risk-level selection and saved score values. | No open human decision. |
| `PENDING-016` | DB seed and parse path checked. Current seed for the 22 EPROC00118 scoring vars has no nonnumeric `SCORE`; current `FunctionServiceImpl.parseScore` still silently coerces future parse failures to `0`. | Closed by owner decision on 2026-06-20. New contract requires fail-fast `MSG_QUERY_FAIL` for nonnumeric score seeds; zero coercion is not accepted. | No open human decision. RD code/test change remains required for the negative seed case. |
| `PENDING-017` | `getScoreDatetime()` checked. It currently uses `LocalDateTime.now()` with no explicit `ZoneId`; format is `dd/MM/yyyy HH:mm:ss`. | Closed by owner decision on 2026-06-20. New contract requires explicit Asia/Taipei / UTC+8 timestamp generation, not deployment-default timezone reliance. | No open human decision. RD code/test change remains required. |
| `PENDING-018` | No page-specific measurable p95/timeout found in PRD/refactor/spec evidence; PRD says to follow the platform standard AJAX timeout. | Closed by owner decision on 2026-06-20. EPROC00118 uses the platform AJAX timeout as the acceptance threshold and does not add a page-specific p95/second target. | No open human decision. |

## Repeat Confirmation

- Final SQL run completed cleanly through SQLPlus `@file`; no pipeline encoding warning.
- Final DB run confirmed `CURRENT_SCHEMA=OVSLXLON02`.
- `TB_API_AUTH` PK confirmed as `API_ID` only.
- EPROZ00100 reason/status/role seed checks now include role `403`.
- EPROC00118 score-param checks now cover all 22 `funcGetRate` scoring variables, not only the 17 select-backed variables.
- No SRS approval, ledger transition, or rollout scale-up was performed.
