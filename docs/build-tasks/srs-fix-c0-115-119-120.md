# SRS 修正派工卡 — EPROC00115 / 00119 / 00120（獨立 axis A–F 複審 🟡 收尾）

> **狀態**：dev 機已 finalize 三包（機械閘門 PASS、0 rule-level @PENDING、Status=ready-for-owner-finalization）。本卡＝規劃側跑 3 隻獨立跨模型 `spec-reviewer` 抓到的 **🟡 should-fix** 收尾清單 + 2 個 owner 判斷題裁定。**修完再審一輪 → owner 蓋規格定版 Approved。**
> **權威**：bundle 內容由 dev 機（母資料夾 Codex）改；規劃側只報、不改 spec（避免分叉）。規劃側已補：`pending-register` 三包列、`srs-finalize-c0-115-119-120.md` 校正（RP43 修向 + P-005 必填集）。
> **無真硬 Blocker**：三包語意大致乾淨、決策真落地（無假關）；以下為定版品質收尾。

## 0. 兩個 owner 判斷題（2026-06-24 裁定，dev 照此落地）
1. **00119 RP43 比較方向**：**End Balance 應等於 Cash & Cash Equivalent；任一期 `END_BALANCE != CASH_AND_CASH_EQU` → 阻擋 Finished**（修正 legacy 反向判斷）。R3 第二段把此條件**量化寫死**（非「true comparison」留白），標 `REF-Dn` delta + 新舊 `file:line`。
2. **00120 P-005 Finished 必填集**：採 **PRD 5.3.1**＝`dataSeq + ratiosDate + 6 個人工 required 欄`（`aggregateLargeExposureRatio` / `creditExposureToSingleBeneficiary` / `relatedPartyExposure` / `unhedgedForeignCurrencyRatio` / `liquidityCoverageRatio` / `solvencyRatio`）；**自動公式欄由系統算、非使用者必填**。R5 補一句註明此校正。

## 1. 跨包共通修（三包都做）
- **C1 code-as-baseline 行級引用補 `@SHA`（Rule 1c 格式）**：as-is 證據逐行引用標 `[CODE:<file>:<line>@<commit-SHA>]`（如 `@3dfebcf`），不只在 metadata 宣告一次。至少關鍵行（bug 行、DB drift 行）要釘版。對 `spec-architecture.md §5b Rule 1c`。
- **C2 db-diff 不可達 disclaim（消除與 code-as-baseline 的不對稱）**：凡引 `docs/db-diff/…` 當 schema/maxlength 硬權威處，加一句標準 disclaim——「**db-diff snapshot 已於母資料夾 finalize 時覆核**」（若屬實）**或**「待母資料夾複核」。⚠️ 此為**全 27 包通病**（非單包缺陷）：建議同一句 disclaim 也回補已 Approved 的 5 包（00110/112/114/116/117）+ 撥貸線，求一致。
- **C3 疑似 bug 修正一律標 `REF-Dn` delta**：policy-2 修正（方向/來源/開窗）每條補 `REF-Dn`（disposition=fix、附 legacy `file:line` + commit SHA），讓 RD/QA 有統一 delta 索引。

## 2. EPROC00115（3 🟡）
| # | 位置 | 問題 | 修法 |
|---|---|---|---|
| 1 | `openapi.yaml:223/230/237/244/256` | 金額 pattern `\\.` **雙重跳脫** → YAML 單引號內 `\\` ＝字面反斜線,regex 變「15 碼後接反斜線」 | 改 `'^[0-9]{1,15}(\.[0-9]{1,2})?$'`（契約正確性，機械 gate 不一定攔） |
| 2 | `spec.md:60-63`(R5)/`:98-103`(R11)/`:133-138`(R16) | 三條疑似 bug 修正缺 `REF-Dn` delta（policy-2 要求） | 各補 `REF-D1/D2/D3`（見 C3） |
| 3 | `spec.md:133-138`(R16) vs `:84-87`(R9) | R16 報表幣別改 cross-module（`OUTSTAND_AMOUNT_CUR`→`COLLATERAL_CUR`），未掛 R9「cross-module 須顯式決策」、未評 CS0181/CU0181 影響 | R16 補 cross-module 影響評估（CS0181/CU0181 同步修 or 標下游 report-module 各自 SRS 範圍）+ 交叉引用 R9 |
| nit | `spec.md:7-8` | Status/N-axis 自述殘留「Still not Approvable」歷史語氣 | 清殘句，避免蓋章誤讀 |

## 3. EPROC00119（3 🟡）
| # | 位置 | 問題 | 修法 |
|---|---|---|---|
| 1 | `spec.md:62`(R3)/`DEC-RP43` | **RP43 比較方向未量化釘死**（policy-2 修向） | 照 §0-1：寫死「應相等、不等阻擋 Finished」+ `REF-Dn` |
| 2 | 全 spec code 引用（`:31`/`:77`/`:94`/DB-D1/D6…） | 行級 code 證據缺 `@SHA`（只 metadata:11 宣告一次） | 見 C1 |
| 3 | `spec.md:70`(R5)/TBD-010 | first-year opening balance 能否從 DTO 收到,假設未驗 | R5 補「first-row opening 必須 request 顯式攜帶/BE 可取；RD 須驗 current DTO 確有收此值」 |
| nit | `spec.md:55`/openapi:65 | `COMMOM_MSG_NO_DATA` legacy 拼字 | 註「`COMMOM_*`＝legacy alias、to-be 統一 `COMMON_*`」 |

## 4. EPROC00120（4 🟡）
| # | 位置 | 問題 | 修法 |
|---|---|---|---|
| 1 | `spec.md:72`(R1)/`:?`(R19) | **R1/R19 恐因 P-012 誤排本頁自有 `APPLICATION_NAME`/本頁 checkpoint 值**（P-012 排的是父頁跨頁聚合,非本頁畫面標頭）→ FE 斷料 | R1 釐清:`APPLICATION_NAME` 與本頁 `EPROC00120` checkpoint 值是否由本 info endpoint 回（PRD 6.3 要求）;若 delegate 給 parent endpoint 須說明本頁載入時序如何取得 |
| 2 | `spec.md:100`(R5) | P-005 必填集（見 §0-2） | R5 採 PRD 5.3.1 人工欄集 + 註明校正 |
| 3 | Baseline disclaimer(`spec.md:12`)/DB Reconcile 表 | db-diff 當硬權威缺 disclaim（與 P-009 不對稱） | 見 C2 |
| 4 | `spec.md:77`(R2) vs `:133`(R10) | mismatch reject 規則重疊 | R2 改引用 R10，避免日後漂移 |
| nit | `spec.md:167`(R16) | P-007 未寫 literal key `EPROC00120_FUNC_NAME` | 補 literal key 名 |

## 5. 再審門檻（修完才蓋章）
1. **機械**：`python scripts/check-srs-bundle.py docs/specs/srs/EPROC00115`（+119/+120）各 exit 0、0 rule-level @PENDING。
2. **語意**：對三包**各再跑一輪獨立 axis A–F**（跨模型、非單一 reviewer）→ **0 Blocker、上述 🟡 已收**。⚠️ 採納修正後**必再審**（修正可能引入新錯）。
3. **owner 蓋章**：三包 Status `規格定版: Approved`（雙軸；`實作完成: not-started`）。
4. **SSOT 同步**：`feature-inventory`（10→13/67）、`STATUS.md`、per-page ledger、`pending-register` 三列轉 ✅、本卡標完成。

## 6. 執行紀律
- bundle 內容**只在母資料夾改**；規劃側不碰 spec/openapi/schema（避免分叉）。
- 修正涉 PRD 範圍（流程/欄名/業務規則）→ 回灌 PRD 待確認、不自決承載 to-be（`legacy-parity-sop §1a`）。
- 遇**未被本卡 + finalize 卡涵蓋**的新待決 → 停下出卡問 owner；否則一路修到再審門檻、停在「待 owner 蓋章」。
